# Sample ansible.cfg for static-analysis tests
[defaults]
inventory = ./inventory
host_key_checking = False
roles_path = ./roles

[privilege_escalation]
become = True
become_user = root
become_method = sudo
