apiVersion: v2
name: featurechart
version: 0.1.0
appVersion: "1.0"
description: Fixture exercising hooks, CRDs, lock, values schema, and lookup
type: application
dependencies:
  - name: common
    version: "1.2.3"
    repository: https://charts.example.com
    import-values:
      - child: service
        parent: svc
