// Copyright Mondoo, Inc. 2024, 2026
// SPDX-License-Identifier: BUSL-1.1

import "../../network/resources/network.lr"

option provider = "go.mondoo.com/cnquery/v9/providers/oci"
option go_package = "go.mondoo.com/mql/v13/providers/oci/resources"

// Oracle Cloud Infrastructure (OCI)
//
// Tenancy-scoped entry point for the OCI provider. The `regions` field
// lists the regions the tenancy is subscribed to, with the home-region
// flag and subscription state, and `compartments` lists the OCI
// compartment hierarchy. The active tenancy itself is exposed as
// `oci.tenancy`, and service-specific entry points (`oci.identity`,
// `oci.compute`, `oci.network`, `oci.objectStorage`, `oci.kms`, and so
// on) are reached as siblings.
oci {
  // Available regions
  regions() []oci.region
  // Compartments in the tenancy
  compartments() []oci.compartment
}

// Oracle Cloud Infrastructure (OCI) tenancy
//
// Active OCI tenancy, the top-level identity and resource container
// the provider is authenticated against. The `id` is the tenancy
// OCID, `name` and `description` carry the display metadata, and
// `retentionPeriod` reports how long audit events are kept, derived
// from the audit configuration in the tenancy's home region.
oci.tenancy @defaults("name") {
  // Tenancy OCID
  id string
  // Tenancy name
  name string
  // Tenancy description
  description string
  // Event retention period duration
  retentionPeriod() time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) region
//
// Region the tenancy is subscribed to, selected by its region key such as
// `us-phoenix-1`. Reports whether a region is the tenancy's home region,
// where identity and IAM changes originate, and its subscription status,
// which governs where resources may be provisioned. Useful for auditing
// the tenancy's geographic footprint and confirming that only approved
// regions are enabled.
private oci.region @defaults("id name") {
  // Region key (e.g., us-phoenix-1)
  id string
  // Region display name
  name string
  // Whether this is the tenancy's home region
  isHomeRegion bool
  // Region subscription status (READY, IN_PROGRESS)
  status string
}

// Oracle Cloud Infrastructure (OCI) compartment
//
// Logical container that isolates and organizes tenancy resources for
// access control, quota, and billing purposes. Compartments form the
// boundary that IAM policies grant access against, so enumerating them
// reveals the tenancy's resource-isolation structure. The `id` field is
// the compartment OCID, `state` reports the lifecycle status, and
// `created` records when the compartment was provisioned.
private oci.compartment @defaults("id name") {
  // Compartment OCID
  id string
  // Compartment name
  name string
  // Compartment description
  description string
  // Compartment creation time
  created time
  // Compartment lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Identity and Access Management
//
// IAM principals and access controls for the tenancy. users are the
// human and service identities, with their MFA state, login history,
// capabilities, API keys, customer secret keys, auth tokens, MFA
// devices, and group memberships. groups collect users, and policies
// carry the statements that grant groups access to compartments and
// resources. dynamicGroups are the rule-matched groups that grant
// access to compute and service principals, identityProviders are the
// federated SAML2 providers, networkSources are the IP and VCN
// allowlists that constrain where console and API requests may
// originate, and authenticationPolicy holds the tenancy's
// password-strength and network-restriction rules.
oci.identity {
  // IAM users
  users() []oci.identity.user
  // IAM groups
  groups() []oci.identity.group
  // IAM policies
  policies() []oci.identity.policy
  // Dynamic groups that grant access to matching compute and service principals
  dynamicGroups() []oci.identity.dynamicGroup
  // Federated SAML2 identity providers
  identityProviders() []oci.identity.identityProvider
  // Network sources that restrict where console and API requests may originate
  networkSources() []oci.identity.networkSource
  // Tenancy authentication policy (password strength and network restrictions)
  authenticationPolicy() oci.identity.authenticationPolicy
  // Identity domains in the tenancy
  //
  // The users, groups, and policies returned by the fields above come
  // from the legacy IAM API, which reaches only the tenancy's default
  // domain. Any tenancy with more than one domain holds principals those
  // fields never return, so `domains` is where the full picture lives.
  domains() []oci.identity.domain
}

// Oracle Cloud Infrastructure (OCI) identity domain
//
// Identity domain, the container OCI uses for users, groups, and the
// authentication policy applied to them. A tenancy has at least a
// Default domain and may have many, each with its own user population,
// password policy, and multi-factor settings, so a control verified only
// against the default domain says nothing about the others.
//
// `type` separates DEFAULT, PRIMARY, SECONDARY, and the free
// DEVELOPER-license domains. `isHiddenOnLogin` reports whether the
// domain appears on the tenancy sign-in page. `users`, `groups`,
// `passwordPolicies`, `authenticationFactorSettings`, and `apps` reach
// into the domain itself through its own endpoint, which is what the
// legacy IAM API cannot do.
private oci.identity.domain @defaults("name type state") {
  // Domain OCID
  id string
  // Domain display name
  name string
  // Domain description
  description string
  // Compartment containing the domain
  compartment() oci.compartment
  // Domain type (DEFAULT, PRIMARY, SECONDARY)
  type string
  // License type the domain runs under (e.g., free, oracle-apps-premium, premium)
  licenseType string
  // Region the domain is homed in
  homeRegion string
  // Regions the domain is replicated to
  replicaRegions []string
  // Whether the domain is hidden from the tenancy sign-in page
  isHiddenOnLogin bool
  // Users in the domain
  users() []oci.identity.domain.user
  // Groups in the domain
  groups() []oci.identity.domain.group
  // Password policies applied in the domain
  passwordPolicies() []oci.identity.domain.passwordPolicy
  // Multi-factor authentication settings for the domain
  authenticationFactorSettings() oci.identity.domain.authenticationFactorSettings
  // Applications registered in the domain
  apps() []oci.identity.domain.app
  // Policies evaluated when a user signs in or a password is set
  //
  // The sign-on policies are the ones carrying `policyType` SignOn, and they
  // are where multi-factor authentication is required rather than merely
  // offered. A policy is an ordered list of rules; the rules are what state
  // the requirement.
  policies() []oci.identity.domain.policy
  // Rules defined in the domain
  //
  // Every rule in the domain, whether or not a policy currently references
  // it. Read a policy's `rules` for the ones it evaluates, in the order it
  // evaluates them.
  rules() []oci.identity.domain.rule
  // Conditions rules match on
  conditions() []oci.identity.domain.condition
  // Network perimeters naming ranges of client addresses
  //
  // A rule that matches on a network perimeter says "when the client comes
  // from perimeter X", and this is what X contains.
  networkPerimeters() []oci.identity.domain.networkPerimeter
  // Whether users may keep a session open across browser restarts
  //
  // Keep-me-signed-in issues a long-lived token that lets a returning user
  // skip sign-in, so with it enabled a multi-factor requirement is satisfied
  // once and then bypassed for the life of the token.
  keepMeSignedInEnabled() bool
  // Whether users are prompted to keep the session open
  keepMeSignedInPromptEnabled() bool
  // Days a keep-me-signed-in token stays valid
  keepMeSignedInTokenValidityInDays() int
  // Days a keep-me-signed-in token survives without being used
  keepMeSignedInLastUsedValidityInDays() int
  // Concurrent keep-me-signed-in sessions allowed per user
  keepMeSignedInMaxAllowedSessions() int
  // Whether the terms-of-use prompt is suppressed
  //
  // Configured alongside the keep-me-signed-in settings.
  termsOfUsePromptDisabled() bool
  // Domain lifecycle state (CREATING, ACTIVE, DELETING, INACTIVE, FAILED)
  state string
  // Domain creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) identity domain user
//
// User account inside an identity domain, keyed by its SCIM id.
// `mfaStatus` reports whether the account is ENROLLED in multi-factor
// authentication, UN_ENROLLED, IGNORED, or DISABLED, which is the
// distinction the tenancy-wide `mfaActivated` flag on the legacy IAM
// user cannot express. `isLocked` and `loginAttempts` report lockout
// state, `lastSuccessfulLogin` and `lastFailedLogin` the account's
// recent use, and `capabilities` which credential types the account may
// hold. `isFederated` marks accounts sourced from an external identity
// provider, whose passwords and MFA are governed there rather than here.
private oci.identity.domain.user @defaults("userName active mfaStatus") {
  // User SCIM identifier
  id string
  // User OCID
  ocid string
  // Login name
  userName string
  // Display name
  displayName string
  // Description
  description string
  // Whether the account is active
  active bool
  // Account type (EMPLOYEE, CONTRACTOR, CUSTOMER, PARTNER, SERVICE)
  userType string
  // Primary email address
  primaryEmail string
  // Whether the primary email address has been verified
  primaryEmailVerified bool
  // All email addresses on the account
  //
  // Each entry has a `value`, a `type` (work, home, recovery, other), and
  // `primary` and `verified` flags.
  emails []dict
  // Whether the account is sourced from an external identity provider
  isFederated bool
  // Multi-factor enrollment status (ENROLLED, UN_ENROLLED, IGNORED, DISABLED)
  mfaStatus string
  // Preferred multi-factor method
  //
  // One of EMAIL, SMS, TOTP, PUSH, OFFLINETOTP, USERNAME_PASSWORD,
  // SECURITY_QUESTIONS, VOICE, PHONE_CALL, THIRDPARTY, FIDO_AUTHENTICATOR,
  // or YUBICO_OTP. Empty when the account is not enrolled.
  preferredAuthenticationFactor string
  // When multi-factor authentication was enrolled
  mfaEnabledOn time
  // Whether the account is locked out
  isLocked bool
  // Consecutive failed login attempts
  loginAttempts int
  // Most recent successful login
  //
  // Null for an account that has never signed in, which is a different
  // state from one that signed in long ago.
  lastSuccessfulLogin time
  // Previous successful login
  previousSuccessfulLogin time
  // Most recent failed login
  lastFailedLogin time
  // Credential types the account is permitted to hold
  //
  // Keyed by capability name, each mapping to whether the account may use
  // it: canUseApiKeys, canUseAuthTokens, canUseConsolePassword,
  // canUseCustomerSecretKeys, canUseSmtpCredentials, canUseDbCredentials,
  // and canUseConsole.
  capabilities map[string]bool
  // Groups the user belongs to
  //
  // Each entry has the group's `value` (its SCIM id), `display` name,
  // `ocid`, and the `type` of membership.
  groups []dict
  // When the account was created
  created time
}

// Oracle Cloud Infrastructure (OCI) identity domain group
private oci.identity.domain.group @defaults("displayName") {
  // Group SCIM identifier
  id string
  // Group OCID
  ocid string
  // Group display name
  displayName string
  // Description
  description string
  // Number of members in the group
  memberCount int
  // Whether the group can be requested through an access-request workflow
  isRequestable bool
  // When the group was created
  created time
}

// Oracle Cloud Infrastructure (OCI) identity domain password policy
//
// Password rules applied to accounts in a domain. Length and composition
// are set by `minLength`, `maxLength`, `minAlphas`, `minNumerals`,
// `minSpecialChars`, `minLowerCase`, `minUpperCase`, and
// `minUniqueChars`. `passwordExpiresAfter` is the maximum password age
// in days and `numPasswordsInHistory` how many previous passwords are
// blocked from reuse. `maxIncorrectAttempts` and `lockoutDuration`
// govern lockout, and `dictionaryWordDisallowed`, `userNameDisallowed`,
// `firstNameDisallowed`, and `lastNameDisallowed` block guessable
// choices. A domain may carry several policies; the one that applies to
// an account depends on its group membership.
private oci.identity.domain.passwordPolicy @defaults("name minLength passwordExpiresAfter") {
  // Password policy SCIM identifier
  id string
  // Password policy OCID
  ocid string
  // Policy name
  name string
  // Policy description
  description string
  // Minimum password length in characters
  minLength int
  // Maximum password length in characters
  maxLength int
  // Minimum alphabetic characters required
  minAlphas int
  // Minimum numeric characters required
  minNumerals int
  // Minimum alphanumeric characters required
  minAlphaNumerals int
  // Minimum special characters required
  minSpecialChars int
  // Maximum special characters permitted
  maxSpecialChars int
  // Minimum lowercase characters required
  minLowerCase int
  // Minimum uppercase characters required
  minUpperCase int
  // Minimum unique characters required
  minUniqueChars int
  // Maximum times a single character may repeat
  maxRepeatedChars int
  // Days before a password must be changed
  //
  // Zero means passwords never expire.
  passwordExpiresAfter int
  // Days before expiry that a warning is shown
  passwordExpireWarning int
  // Minimum days before a password may be changed again
  minPasswordAge int
  // Previous passwords blocked from reuse
  numPasswordsInHistory int
  // Failed attempts before the account locks
  maxIncorrectAttempts int
  // Minutes an account stays locked after too many failed attempts
  lockoutDuration int
  // Whether dictionary words are rejected
  dictionaryWordDisallowed bool
  // Whether the password may contain the user name
  userNameDisallowed bool
  // Whether the password may contain the user's first name
  firstNameDisallowed bool
  // Whether the password may contain the user's last name
  lastNameDisallowed bool
  // Whether the password must begin with a letter
  startsWithAlphabet bool
  // Substrings the password may not contain
  disallowedSubstrings []string
  // Overall strength preset the policy was built from
  //
  // One of Simple, Standard, or Custom. Simple and Standard are Oracle's
  // presets; Custom means the individual rules on this policy are what
  // actually applies.
  passwordStrength string
  // Character classes a password must draw from
  //
  // A string listing the required characters, so a policy requiring a digit
  // and a symbol reads as those characters rather than as counts. Empty when
  // the policy constrains composition only through the min and max counts.
  requiredChars string
  // Characters a password may contain
  //
  // Empty when every character is permitted.
  allowedChars string
  // Characters a password may not contain
  //
  // Empty when no character is excluded.
  disallowedChars string
  // Minimum distinct characters a new password must have from the previous one
  //
  // Zero means a user may change a password by altering a single character.
  // Null when the policy states no requirement, which the domain treats the
  // same way.
  distinctCharacters int
  // Whether every user governed by the policy must reset their password at next sign-in
  forcePasswordReset bool
  // User attributes whose values a password may not contain
  //
  // Each entry names a profile attribute, so a policy listing `emails`
  // rejects a password built from the user's own mail address. Empty when
  // the policy checks no attribute beyond the dedicated userName, firstName,
  // and lastName rules.
  disallowedUserAttributeValues []string
  // Location of the dictionary of disallowed words
  //
  // Only meaningful when dictionaryWordDisallowed is set. Empty when the
  // domain uses the built-in dictionary.
  dictionaryLocation string
  // Evaluation order when several policies could govern the same user
  //
  // The lowest priority wins, so a weak policy at priority 1 overrides a
  // strict one at priority 2 for any user in both policies' groups. Null on a
  // policy the domain returns no priority for, which is the case for the
  // built-in policies.
  priority int
  // Groups the policy governs
  //
  // Empty when the policy applies to the whole domain rather than to
  // selected groups.
  groups() []oci.identity.domain.group
}

// Oracle Cloud Infrastructure (OCI) identity domain multi-factor settings
//
// Which multi-factor authentication factors a domain permits, and how
// enrollment is handled. Each `*Enabled` field reports whether that
// factor may be used: a domain with only `emailEnabled` accepts a factor
// materially weaker than one offering `fidoAuthenticatorEnabled`, so the
// set of enabled factors is as important as whether MFA is on at all.
// `mfaEnrollmentType` reports whether enrollment is required or optional,
// and `bypassCodeEnabled` whether users can fall back to a bypass code.
private oci.identity.domain.authenticationFactorSettings @defaults("mfaEnrollmentType") {
  // Settings SCIM identifier
  id string
  // Whether SMS one-time passcodes may be used
  smsEnabled bool
  // Whether email one-time passcodes may be used
  emailEnabled bool
  // Whether time-based one-time passcodes may be used
  totpEnabled bool
  // Whether push notifications to the Oracle Mobile Authenticator may be used
  pushEnabled bool
  // Whether phone-call verification may be used
  phoneCallEnabled bool
  // Whether FIDO authenticators may be used
  fidoAuthenticatorEnabled bool
  // Whether Yubico OTP may be used
  yubicoOtpEnabled bool
  // Whether security questions may be used as a factor
  securityQuestionsEnabled bool
  // Whether bypass codes may be used in place of a factor
  bypassCodeEnabled bool
  // Whether enrollment is required or optional
  mfaEnrollmentType string
  // Which user category multi-factor authentication is enabled for
  mfaEnabledCategory string
}

// Oracle Cloud Infrastructure (OCI) identity domain application
//
// Application registered in an identity domain. `isOAuthClient` marks
// apps that hold client credentials and can obtain tokens, and
// `clientType` reports whether those credentials are confidential,
// public, or trusted, which decides whether a client secret is expected
// to be kept private. `allowedGrants` lists the OAuth grant types the
// app may use, so an app permitted the resource-owner password grant is
// distinguishable from one restricted to authorization code.
private oci.identity.domain.app @defaults("displayName isOAuthClient active") {
  // Application SCIM identifier
  id string
  // Application name
  name string
  // Application display name
  displayName string
  // Description
  description string
  // Whether the application is active
  active bool
  // Whether the application is an OAuth client and can obtain tokens
  isOAuthClient bool
  // Whether the application is an OAuth resource server
  isOAuthResource bool
  // OAuth client type (confidential, public, trusted)
  clientType string
  // OAuth grant types the application may use
  //
  // For example authorization_code, implicit, client_credentials,
  // refresh_token, password, tls_client_auth, or device_code.
  allowedGrants []string
  // URL the application lands on after sign-in
  landingPageUrl string
  // Whether a web-tier access policy is applied to the application
  isWebTierPolicy bool
}


// Oracle Cloud Infrastructure (OCI) identity domain policy
//
// Policy evaluated at sign-in, keyed by its SCIM id. A policy is an ordered
// list of rules and carries no requirement of its own, so `rules` is where a
// multi-factor requirement, an address restriction or an outright denial is
// actually stated. `policyType` separates SignOn from the password and
// notification policies the same API returns. `active` is the difference
// between a policy that is written and one that is in force.
private oci.identity.domain.policy @defaults("name policyType active") {
  // Policy SCIM id
  id string
  // Policy OCID
  ocid string
  // Policy name
  name string
  // Policy description
  description string
  // Kind of decision the policy governs
  //
  // SignOn for sign-in policies, and Password, IdentityProvider or
  // NotificationRoute for the others the domain defines.
  policyType string
  // Whether the policy is in force
  active bool
  // Rules the policy evaluates, in evaluation order
  //
  // The first rule whose condition matches decides the outcome, so the order
  // is part of the meaning: a permissive rule ahead of a restrictive one
  // makes the restrictive one unreachable.
  rules() []oci.identity.domain.rule
  // Policy creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) identity domain rule
//
// One rule within a sign-on policy, and the place a requirement is stated.
// `returns` carries the outcome as name and value pairs, so a rule allowing
// access without a second factor and one demanding multi-factor differ only
// there. `condition` is what the rule matches on, and it is null when the
// rule matches on a group of conditions instead, named by
// `conditionGroupName`. An inactive rule is never evaluated.
private oci.identity.domain.rule @defaults("name active") {
  // Rule SCIM id
  id string
  // Rule OCID
  ocid string
  // Rule name
  name string
  // Rule description
  description string
  // Kind of policy the rule belongs to
  //
  // SignOn for sign-in rules, and Password, IdentityProvider or
  // NotificationRoute for the others.
  policyType string
  // Whether the rule is evaluated
  active bool
  // Whether the rule is locked against modification
  locked bool
  // Expression the rule matches on
  conditionExpression string
  // Condition the rule matches on
  //
  // Null when the rule matches on a condition group rather than a single
  // condition, in which case `conditionGroupName` names the group.
  condition() oci.identity.domain.condition
  // Whether the rule matches a single condition or a group of them
  //
  // Condition or ConditionGroup.
  conditionGroupType string
  // Name of the condition or condition group the rule matches on
  conditionGroupName string
  // Outcome the rule produces, keyed by name
  //
  // A sign-on rule states its decision here. `effect` carries ALLOW or DENY,
  // and the factor and session settings a matching sign-in is subject to
  // appear alongside it.
  returns map[string]string
  // Rule creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) identity domain condition
//
// Single test a rule matches on, written as an attribute, an operator and a
// value. Conditions are defined once for the domain and referenced by any
// number of rules, so reading one tells you what every rule naming it
// actually tests. Common attributes are the client's network perimeter, the
// user's group membership, and the application being signed in to.
private oci.identity.domain.condition @defaults("name attributeName operator attributeValue") {
  // Condition SCIM id
  id string
  // Condition OCID
  ocid string
  // Condition name
  name string
  // Condition description
  description string
  // Attribute the condition tests
  attributeName string
  // Comparison the condition applies
  //
  // One of eq, ne, co, coany, sw, ew, gt, ge, lt, le, in, or nin.
  operator string
  // Value the attribute is compared against
  attributeValue string
  // Expression gating whether the condition is evaluated at all
  evaluateConditionIf string
  // Condition creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) identity domain network perimeter
//
// Named set of client addresses a sign-on rule can match on. A rule reading
// "allow from the corporate perimeter" means nothing without this: the
// perimeter may hold a pair of office prefixes or a range wide enough to
// cover the public internet, and `ipAddresses` is the only place that
// difference shows.
private oci.identity.domain.networkPerimeter @defaults("name") {
  // Network perimeter SCIM id
  id string
  // Network perimeter OCID
  ocid string
  // Network perimeter name
  name string
  // Network perimeter description
  description string
  // Client addresses the perimeter covers
  //
  // Each entry carries `value` with the address or range, `type` as CIDR,
  // RANGE or EXACT, and `version` as IPV4 or IPV6.
  ipAddresses []dict
  // Network perimeter creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) IAM user
private oci.identity.user @defaults("name") {
  // User OCID
  id string
  // Compartment containing the user
  compartment() oci.compartment
  // User's login name within the OCI tenancy
  name string
  // User description
  description string
  // User creation time
  created time
  // User lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Whether multi-factor authentication is enabled
  mfaActivated bool
  // User email address
  email string
  // Whether the email address has been verified
  emailVerified bool
  // Identifier of this user in a federated identity provider, empty for local users
  externalIdentifier string
  // Federated identity provider this user originates from, when the user is federated
  identityProvider() oci.identity.identityProvider
  // Authentication capabilities the user is permitted to hold
  //
  // Keyed by capability name, each mapping to whether the user may use
  // it: canUseConsolePassword, canUseApiKeys, canUseAuthTokens,
  // canUseSmtpCredentials, canUseCustomerSecretKeys, and
  // canUseOAuth2ClientCredentials.
  capabilities map[string]bool
  // Most recent successful login time
  lastLogin time
  // Previous successful login time
  previousLogin time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // API keys for API authentication
  apiKeys() []oci.identity.apiKey
  // Customer secret keys for Amazon S3 compatibility API
  customerSecretKeys() []oci.identity.customerSecretKey
  // Authentication tokens for Oracle Cloud Infrastructure APIs
  authTokens() []oci.identity.authToken
  // Groups the user belongs to
  groups() []oci.identity.group
  // MFA TOTP devices
  mfaDevices() []oci.identity.mfaDevice
  // Database password credentials for OCI database connections
  dbCredentials() []oci.identity.dbCredential
  // SMTP credentials for the Email Delivery service
  smtpCredentials() []oci.identity.smtpCredential
  // OAuth 2.0 client credentials for token-based API access
  oauth2ClientCredentials() []oci.identity.oauth2ClientCredential
}

// Oracle Cloud Infrastructure (OCI) MFA TOTP device
private oci.identity.mfaDevice @defaults("id") {
  // MFA device OCID
  id string
  // User OCID the device belongs to
  userId string
  // Whether the device has been activated
  isActivated bool
  // Lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Creation time
  created time
  // Expiration time
  expires time
}

// Oracle Cloud Infrastructure (OCI) API key (PEM-format RSA credential for API authentication)
private oci.identity.apiKey @defaults("fingerprint") {
  // API key OCID
  id string
  // Public key value in PEM format
  value string
  // MD5 fingerprint of the public key
  fingerprint string
  // API key creation time
  created time
  // API key lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
}

// Oracle Cloud Infrastructure (OCI) customer secret key for Amazon S3 compatibility API
private oci.identity.customerSecretKey @defaults("name") {
  // Customer secret key OCID
  id string
  // Display name for the key
  name string
  // Key creation time
  created time
  // Key lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
}

// Oracle Cloud Infrastructure (OCI) authentication token for API access
private oci.identity.authToken @defaults("description") {
  // Auth token OCID
  id string
  // Token description
  description string
  // Token creation time
  created time
  // Token expiration time
  expires time
  // Token lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
}

// Oracle Cloud Infrastructure (OCI) IAM group (collection of users)
private oci.identity.group @defaults("name") {
  // Group OCID
  id string
  // Compartment containing the group
  compartment() oci.compartment
  // Group name
  name string
  // Group description
  description string
  // Group creation time
  created time
  // Group lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // Users in the group
  members() []oci.identity.user
}

// Oracle Cloud Infrastructure (OCI) IAM policy that grants group permissions to resources
private oci.identity.policy @defaults("name description") {
  // Policy OCID
  id string
  // Compartment containing the policy
  compartment() oci.compartment
  // Policy name
  name string
  // Policy description
  description string
  // Policy creation time
  created time
  // Policy lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Policy statements defining permissions
  statements []string
  // Policy statements broken into their grammatical parts
  //
  // Each entry corresponds to one line of `statements`, split into the
  // subject, verb, resource type, scope, and condition that determine what
  // the line grants, so an audit can match on those parts instead of
  // pattern-matching the statement text.
  parsedStatements() []oci.identity.policy.statement
  // Policy version date
  versionDate time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Statement within an Oracle Cloud Infrastructure (OCI) IAM policy
//
// One policy statement split into the parts that determine what it grants:
// the effect that opens it, the principals named as its subject, the verb
// and resource type it permits, the compartment or tenancy it applies in,
// and any condition that narrows it. The `raw` field keeps the original
// text, so a statement written in a form the provider does not recognize
// remains readable even when the other fields come back empty.
//
// The keyword fields are lowercased, so `Allow` and `allow` both read as
// `allow`, while `subjectNames` and `scopeName` preserve the casing used in
// the policy. `groups` and `dynamicGroups` resolve the named subject
// principals, and `compartment` resolves the scope for statements written
// against a compartment rather than the whole tenancy.
private oci.identity.policy.statement @defaults("effect subjectType verb resourceType scopeType") {
  // Statement text as written in the policy
  raw string
  // Statement effect
  //
  // One of allow, endorse, admit, or define. Endorse and admit statements
  // carry cross-tenancy grants, and define statements name a tenancy or
  // group rather than granting access.
  effect string
  // Kind of principal the statement grants to
  //
  // One of group, dynamic-group, service, any-user, or any-group. Define
  // statements report the kind being named, such as tenancy. Empty when the
  // subject is written in a form the provider does not recognize.
  subjectType string
  // Principals named as the statement's subject
  //
  // Group or dynamic group names, or OCIDs where the statement uses the
  // `id` form. Empty for subjects that name no principal, such as any-user.
  subjectNames []string
  // Access level the statement grants
  //
  // One of inspect, read, use, or manage, in increasing order of access.
  // Empty on define statements, which grant nothing.
  verb string
  // Resource type the statement covers
  //
  // An aggregate such as all-resources or instance-family, or an individual
  // type such as buckets or objects.
  resourceType string
  // Kind of scope the statement applies in
  //
  // One of tenancy, compartment, or any-tenancy. A statement scoped to the
  // tenancy applies to every compartment beneath it.
  scopeType string
  // Compartment named as the statement's scope
  //
  // The compartment name, a colon-separated path for a nested compartment,
  // or an OCID where the statement uses the `compartment id` form. Empty
  // when the scope is the whole tenancy.
  scopeName string
  // Condition that narrows when the statement applies
  //
  // The text following `where`, covering both single comparisons and the
  // `any` and `all` groupings. Empty when the statement is unconditional.
  condition string
  // Groups named as the statement's subject
  groups() []oci.identity.group
  // Dynamic groups named as the statement's subject
  dynamicGroups() []oci.identity.dynamicGroup
  // Compartment the statement is scoped to
  compartment() oci.compartment
}

// Oracle Cloud Infrastructure (OCI) database password credential for a user
private oci.identity.dbCredential @defaults("description") {
  // Database credential OCID
  id string
  // Credential description
  description string
  // Credential creation time
  created time
  // Credential expiration time
  expires time
  // Credential lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
}

// Oracle Cloud Infrastructure (OCI) SMTP credential for the Email Delivery service
private oci.identity.smtpCredential @defaults("username") {
  // SMTP credential OCID
  id string
  // SMTP user name used to authenticate against the Email Delivery endpoint
  username string
  // Credential description
  description string
  // Credential creation time
  created time
  // Credential expiration time
  expires time
  // Credential lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
}

// Oracle Cloud Infrastructure (OCI) OAuth 2.0 client credential for a user
private oci.identity.oauth2ClientCredential @defaults("name") {
  // OAuth 2.0 client credential OCID
  id string
  // Credential name
  name string
  // Credential description
  description string
  // Compartment containing the oauth2 client credential
  compartment() oci.compartment
  // Allowed scopes, each with an `audience` and a `scope`
  scopes []dict
  // Credential creation time
  created time
  // Credential expiration time, empty when the credential never expires
  expires time
  // Credential lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
}

// Oracle Cloud Infrastructure (OCI) IAM dynamic group
private oci.identity.dynamicGroup @defaults("name") {
  // Dynamic group OCID
  id string
  // Compartment containing the dynamic group
  compartment() oci.compartment
  // Dynamic group name
  name string
  // Dynamic group description
  description string
  // Rule that selects the compute instances and service resources in the group
  matchingRule string
  // Dynamic group creation time
  created time
  // Dynamic group lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) federated identity provider
private oci.identity.identityProvider @defaults("name") {
  // Identity provider OCID
  id string
  // Compartment containing the identity provider
  compartment() oci.compartment
  // Identity provider name
  name string
  // Identity provider description
  description string
  // Federation protocol (SAML2)
  protocol string
  // Identity provider product, e.g., IDCS or ADFS
  productType string
  // URL of the provider's SAML2 metadata document
  metadataUrl string
  // Provider's signing certificate in PEM format
  signingCertificate string
  // URL the tenancy redirects to for federated sign-in
  redirectUrl string
  // Identity provider creation time
  created time
  // Identity provider lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) network source
private oci.identity.networkSource @defaults("name") {
  // Network source OCID
  id string
  // Compartment containing the network source
  compartment() oci.compartment
  // Network source name
  name string
  // Network source description
  description string
  // Public IP addresses and CIDR blocks allowed by this network source
  publicSourceList []string
  // Allowed virtual sources, each with a `vcnId` and its `ipRanges`
  virtualSourceList []dict
  // OCI services this network source applies to (e.g., `all`)
  services []string
  // Network source creation time
  created time
  // Network source lifecycle state (CREATING, ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) tenancy authentication policy
private oci.identity.authenticationPolicy @defaults("minimumPasswordLength") {
  // Compartment (tenancy) OCID the policy applies to
  compartmentID string
  // Minimum number of characters required in a console password
  minimumPasswordLength int
  // Whether a console password must contain an uppercase character
  passwordRequiresUppercase bool
  // Whether a console password must contain a lowercase character
  passwordRequiresLowercase bool
  // Whether a console password must contain a numeric character
  passwordRequiresNumeric bool
  // Whether a console password must contain a special character
  passwordRequiresSpecial bool
  // Whether a console password is allowed to contain the user name
  passwordUsernameContainmentAllowed bool
  // OCIDs of the network sources that restrict where console and API requests may originate
  networkSourceIds []string
}

// Oracle Cloud Infrastructure (OCI) Compute
//
// Compute and block-storage inventory for the tenancy. The `instances`
// collection covers virtual machine and bare-metal instances with their
// shape, source image, networking, metadata, agent configuration, and
// internet exposure. The `images` collection lists Oracle-provided and
// custom images available for launch, `blockVolumes` lists managed block
// storage volumes, and `bootVolumes` lists the boot volumes attached to
// instances. Use these to audit instance security posture, encryption, and
// storage across compartments.
oci.compute {
  // Compute instances
  instances() []oci.compute.instance
  // Compute images
  images() []oci.compute.image
  // Block volumes
  blockVolumes() []oci.compute.blockVolume
  // Boot volumes
  bootVolumes() []oci.compute.bootVolume
}

// OCI network internet-exposure breakdown
//
// Explains whether a resource is reachable from the internet by combining its
// public-IP assignment with the network security group ingress rules (and, for
// compute, the subnet's internet-ingress policy) that admit traffic from any
// address. Shared by compute instances and load balancers.
private oci.network.exposure @defaults("internetReachable hasPublicIp") {
  // Whether the resource is reachable from the internet
  //
  // True when the resource has a public IP, sits on a subnet that permits
  // internet ingress and routes a default route to an enabled internet gateway,
  // and an ingress rule (in an attached network security group or the subnet's
  // security list) admits traffic from any address.
  internetReachable bool
  // Whether the resource has a public IP
  hasPublicIp bool
  // Whether an attached network security group admits inbound traffic from any address
  //
  // True also when no NSG is attached (OCI's default open posture). For compute,
  // the subnet's internet-ingress policy is applied only to internetReachable,
  // not here.
  securityGroupAllowsIngress bool
  // Whether the subnet's security list admits inbound traffic from any address
  //
  // True also when no security list is resolvable, matching the network
  // security group default. OCI evaluates the union of security list and NSG
  // rules, so either layer admitting any address opens ingress.
  securityListAllowsIngress bool
  // Whether the subnet routes a default route (0.0.0.0/0 or ::/0) to an enabled internet gateway
  //
  // A public IP alone does not make a resource internet-reachable in OCI; the
  // subnet's route table must also forward internet-bound traffic to an internet
  // gateway. False when the only default route targets a NAT gateway, a DRG, or
  // nothing.
  hasRouteToInternet bool
  // Ingress rules that admit traffic from any address (0.0.0.0/0 or ::/0)
  //
  // The matching rules from every attached network security group and from the
  // subnet's security list, combined. Each entry: {source, sourceType, protocol
  // (IANA number, e.g. "6"=TCP, "17"=UDP, "1"=ICMP), description, tcpOptions,
  // udpOptions, icmpOptions}; network security group rules carry `isStateless`
  // while security list rules carry `stateless`.
  openIngressRules []dict
  // Security attributes carried by the resource, keyed by namespace
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to the resource's values. Zero Trust Packet
  // Routing decides what a resource may talk to from these attributes rather
  // than from its address, so a resource carrying none is outside ZPR's reach
  // no matter what policies the tenancy has written.
  securityAttributes map[string]dict
  // Whether Zero Trust Packet Routing is enforcing on this resource
  //
  // True when ZPR is enabled for the tenancy and the resource carries at least
  // one security attribute from a namespace whose mode includes `enforce`.
  // A namespace in `audit` mode reports its decisions without acting on them,
  // so it does not make this true.
  //
  // ZPR sits above security lists and network security groups: where it
  // enforces, traffic the other layers admit can still be denied. The other
  // fields on this resource describe those layers only, so a resource can
  // report `internetReachable` while ZPR blocks the traffic in practice. Read
  // `internetReachable && !zprEnforced` when the question is whether an
  // opening is genuinely reachable.
  zprEnforced bool
  // Zero Trust Packet Routing policies in the tenancy
  //
  // The policies that could govern this resource. Whether any given policy
  // admits or denies its traffic depends on the Zero Trust Packet Routing
  // Policy Language statements it carries, which are returned as written.
  zprPolicies() []oci.zpr.policy
}

// Oracle Cloud Infrastructure (OCI) Compute instance
//
// Virtual machine or bare-metal Compute instance in the tenancy, keyed by
// its OCID. Surfaces the instance's shape and sizing, source image and boot
// volume, attached network interfaces, platform security settings, Oracle
// Cloud Agent plugin states, and a derived internet-exposure breakdown.
// Central to auditing instance hardening, metadata-service (IMDS)
// configuration, encryption, and internet reachability.
private oci.compute.instance @defaults("name") {
  // Instance OCID
  id string
  // Instance display name
  name string
  // Region where the instance is running
  region oci.region
  // Instance creation time
  created time
  // Instance lifecycle state (e.g., MOVING, PROVISIONING, RUNNING, STARTING, STOPPING, STOPPED, CREATING_IMAGE, TERMINATING, TERMINATED)
  state string
  // Compute shape determining CPU and memory (e.g., VM.Standard2.1)
  shape string
  // Availability domain where the instance is placed (e.g., Uocm:PHX-AD-1)
  availabilityDomain string
  // Compartment containing the instance
  compartment oci.compartment
  // Fault domain for fault tolerance (e.g., FAULT-DOMAIN-1)
  faultDomain string
  // Image used to launch the instance
  image() oci.compute.image
  // Dedicated VM host OCID if running on dedicated infrastructure
  dedicatedVmHostId string
  // Platform security configuration
  //
  // Shielded-instance and confidential-computing settings. Keys: `type`
  // (the platform variant, for example AMD_VM, INTEL_VM, or AMD_MILAN_BM),
  // `isSecureBootEnabled`, `isTrustedPlatformModuleEnabled`,
  // `isMeasuredBootEnabled`, and `isMemoryEncryptionEnabled`. Absent when
  // the shape does not support these features.
  platformConfig dict
  // Launch configuration options
  //
  // Emulation and I/O settings chosen at launch. Keys: `bootVolumeType` and
  // `remoteDataVolumeType` (ISCSI, SCSI, IDE, VFIO, or PARAVIRTUALIZED),
  // `firmware` (BIOS or UEFI_64), `networkType` (E1000, VFIO,
  // PARAVIRTUALIZED, or ACCELERATEDPV), `isPvEncryptionInTransitEnabled`
  // for in-transit encryption of paravirtualized volume traffic, and
  // `isConsistentVolumeNamingEnabled`.
  launchOptions dict
  // Instance runtime options
  //
  // Runtime configuration of the instance. The single key
  // `areLegacyImdsEndpointsDisabled` reports whether the legacy instance
  // metadata service (IMDSv1) endpoints are turned off, also surfaced as
  // the `legacyImdsEndpointsDisabled` field.
  instanceOptions dict
  // Whether legacy instance metadata service (IMDSv1) endpoints are disabled
  //
  // When true, only the more secure IMDSv2 (/v2) endpoints are reachable.
  // Defaults to false, meaning the legacy /v1 endpoints remain accessible and
  // any credentials they expose can be read without a signed request.
  legacyImdsEndpointsDisabled bool
  // Whether the Oracle Cloud Agent monitoring plugins are disabled
  //
  // When true, all monitoring plugins (Compute Instance Monitoring, Custom
  // Logs Monitoring) are off regardless of per-plugin configuration.
  monitoringDisabled bool
  // Whether the Oracle Cloud Agent management plugins are disabled
  //
  // When true, all management plugins (OS Management Service Agent, Compute
  // Instance Run Command) are off regardless of per-plugin configuration.
  managementDisabled bool
  // Whether all Oracle Cloud Agent plugins are disabled
  allPluginsDisabled bool
  // Oracle Cloud Agent plugin desired states keyed by plugin name
  //
  // Each value is the plugin's desired state, ENABLED or DISABLED — for
  // example "Vulnerability Scanning", "Bastion", or "OS Management Service
  // Agent". Use it to confirm required security agents are enabled.
  agentPlugins map[string]string
  // Shape configuration details
  //
  // Resolved sizing for the instance's shape. Keys: `ocpus`, `memoryInGBs`,
  // `baselineOcpuUtilization` (BASELINE_1_8, BASELINE_1_2, or BASELINE_1_1
  // for burstable shapes), `networkingBandwidthInGbps`, `maxVnicAttachments`,
  // `gpus` with `gpuDescription`, `localDisks` with `localDisksTotalSizeInGBs`,
  // and `processorDescription`.
  shapeConfig dict
  // Boot volume or image source details
  //
  // What the instance was launched from. The `sourceType` key discriminates
  // the shape: when "image", the keys are `imageId`, `bootVolumeSizeInGBs`,
  // `bootVolumeVpusPerGB`, and `kmsKeyId`; when "bootVolume", the single key
  // is `bootVolumeId`.
  sourceDetails dict
  // Boot volume the instance was launched from, when launched from an existing boot volume
  bootVolume() oci.compute.bootVolume
  // Instance metadata key-value pairs
  metadata map[string]string
  // SSH public keys authorized for the instance
  //
  // One entry per key in the instance metadata `ssh_authorized_keys`
  // value. Each entry carries the key `algorithm` (for example ssh-rsa,
  // ecdsa-sha2-nistp256, ssh-ed25519), key size in `bits`, the raw
  // `publicKey`, and any trailing `comment`. Empty when no keys are set.
  sshAuthorizedKeys() []dict
  // Expected maintenance reboot time, if scheduled
  timeMaintenanceRebootDue time
  // Security attributes applied to the instance, keyed by namespace
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to this instance's values. Zero Trust Packet
  // Routing authorizes traffic from these rather than from the instance's
  // address, so an instance carrying none is outside ZPR's reach whatever
  // policies the tenancy has written.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Whether the instance's security attributes have finished applying
  //
  // STABLE once applied, UPDATING while a change is still propagating. During
  // an update the attributes reported here may not yet be the ones in force.
  securityAttributesState string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // Virtual network interface cards attached to the instance
  vnics() []oci.compute.vnic
  // Internet-exposure breakdown (public IP combined with NSG ingress and the subnet's internet-ingress policy)
  exposure() oci.network.exposure
  // Most recent agent-based vulnerability scan result for this instance
  vulnerabilityScanResult() oci.vulnerabilityScanning.hostAgentScanResult
  // Most recent open-port scan result for this instance
  portScanResult() oci.vulnerabilityScanning.hostPortScanResult
  // Most recent CIS benchmark scan result for this instance
  cisBenchmarkScanResult() oci.vulnerabilityScanning.hostCisBenchmarkScanResult
  // Most recent endpoint protection scan result for this instance
  endpointProtectionScanResult() oci.vulnerabilityScanning.hostEndpointProtectionScanResult
}

// Oracle Cloud Infrastructure (OCI) virtual network interface card (VNIC)
//
// Virtual network interface attached to a Compute instance, carrying its
// private and public IP addressing, MAC and hostname label, the subnet it
// lives in, and the network security groups applied to it. Use it to audit
// which interfaces hold public IPs and how instance traffic is filtered.
private oci.compute.vnic @defaults("name") {
  // VNIC OCID
  id string
  // VNIC display name
  name string
  // Compartment containing the VNIC
  compartment() oci.compartment
  // Whether this is the primary VNIC
  isPrimary bool
  // Private IP address
  privateIp string
  // Public IP address (empty if none assigned)
  publicIp string
  // IPv6 addresses assigned to the VNIC
  //
  // Global unicast addresses assigned from the subnet's IPv6 CIDR. OCI routes
  // these publicly once the VCN has an IPv6 CIDR and the route table forwards
  // ::/0 to an internet gateway, so a VNIC can be internet-reachable over IPv6
  // while `publicIp`, which only carries IPv4, is empty.
  ipv6Addresses []string
  // MAC address
  macAddress string
  // Hostname label for DNS
  hostnameLabel string
  // Subnet the VNIC is in
  subnet() oci.network.subnet
  // Network Security Groups attached to the VNIC
  securityGroups() []oci.network.networkSecurityGroup
  // Whether source/destination check is skipped
  skipSourceDestCheck bool
  // Security attributes applied to the VNIC, keyed by namespace
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to this VNIC's values. A VNIC can carry
  // attributes its instance does not, so Zero Trust Packet Routing may govern
  // one interface of a multi-homed instance and not another.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Lifecycle state (PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Compute image
//
// Operating-system or custom Compute image available for launching
// instances, keyed by its OCID. Reports the operating system and version,
// lifecycle state, size, and the base image it was derived from. Use it to
// track which OS images and versions are approved and in use.
private oci.compute.image @defaults("name") {
  // Image OCID
  id string
  // Image display name
  name string
  // Region where the image is available
  region oci.region
  // Image creation time
  created time
  // Image lifecycle state (e.g., PROVISIONING, IMPORTING, AVAILABLE, EXPORTING, DISABLED, DELETED)
  state string
  // Compartment containing the image
  compartment oci.compartment
  // Operating system name (e.g., Oracle Linux)
  operatingSystem string
  // Operating system version (e.g., 7.9)
  operatingSystemVersion string
  // Image size in megabytes
  sizeInMBs int
  // Image this image was derived from, when created from another image
  baseImage() oci.compute.image
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) block volume
//
// Managed block storage volume that can be attached to Compute instances,
// keyed by its OCID. Reports size and performance tier, lifecycle state, the
// KMS key protecting it, and the source volume or backup it was cloned or
// restored from. Use it to verify volume encryption and clone lineage.
private oci.compute.blockVolume @defaults("name") {
  // Block volume OCID
  id string
  // Block volume display name
  name string
  // Compartment containing the block volume
  compartment() oci.compartment
  // Availability domain where the block volume is located
  availabilityDomain string
  // Size of the block volume in GBs
  sizeInGBs int
  // Volume performance units per GB
  vpusPerGB int
  // Block volume lifecycle state (e.g., PROVISIONING, RESTORING, AVAILABLE, TERMINATING, TERMINATED, FAULTY)
  state string
  // Whether the cloned volume's data has finished copying from the source
  isHydrated bool
  // KMS key for volume encryption
  kmsKey() oci.kms.key
  // Whether auto-tune performance is enabled
  isAutoTuneEnabled bool
  // Source block volume this volume was cloned from, when cloned from another volume
  sourceVolume() oci.compute.blockVolume
  // OCID of the volume backup this volume was restored from, when restored from a backup
  sourceVolumeBackupId string
  // Block volume creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) boot volume
//
// Boot volume that holds an instance's operating-system disk, keyed by its
// OCID. Reports size and lifecycle state, the source image or boot volume,
// and the KMS key protecting it. Use it to verify boot-volume encryption
// and image provenance.
private oci.compute.bootVolume @defaults("name") {
  // Boot volume OCID
  id string
  // Boot volume display name
  name string
  // Compartment containing the boot volume
  compartment() oci.compartment
  // Availability domain where the boot volume is located
  availabilityDomain string
  // Size of the boot volume in GBs
  sizeInGBs int
  // Image used to create the boot volume
  image() oci.compute.image
  // Source boot volume this boot volume was cloned from, when cloned from another boot volume
  sourceBootVolume() oci.compute.bootVolume
  // OCID of the boot volume backup this boot volume was restored from, when restored from a backup
  sourceBootVolumeBackupId string
  // Boot volume lifecycle state (e.g., PROVISIONING, RESTORING, AVAILABLE, TERMINATING, TERMINATED, FAULTY)
  state string
  // KMS key for volume encryption
  kmsKey() oci.kms.key
  // Boot volume creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Networking
//
// Network topology for the tenancy: the Virtual Cloud Networks in `vcns`,
// the address ranges partitioned inside them in `subnets`, the layer-4
// access rules in `securityLists` and `networkSecurityGroups`, inbound and
// outbound internet connectivity through `internetGateways`, outbound-only
// address translation through `natGateways`, and the per-subnet forwarding
// decisions in `routeTables`. These expose the tenancy's ingress and egress
// paths for auditing internet exposure and lateral connectivity.
oci.network {
  // Virtual Cloud Networks (VCNs)
  vcns() []oci.network.vcn
  // Subnets within the VCNs of the tenancy
  subnets() []oci.network.subnet
  // VCN security lists
  securityLists() []oci.network.securityList
  // Network Security Groups (NSGs)
  networkSecurityGroups() []oci.network.networkSecurityGroup
  // Internet gateways
  internetGateways() []oci.network.internetGateway
  // NAT gateways
  natGateways() []oci.network.natGateway
  // Route tables
  routeTables() []oci.network.routeTable
  // Reserved and regional public IP addresses in the tenancy
  publicIps() []oci.network.publicIp
  // Dynamic routing gateways (DRGs)
  drgs() []oci.network.drg
  // Local peering gateways (LPGs)
  localPeeringGateways() []oci.network.localPeeringGateway
  // Service gateways
  serviceGateways() []oci.network.serviceGateway
  // Customer-premises equipment (CPE) devices
  cpes() []oci.network.cpe
  // Site-to-site VPN IPSec connections
  ipsecConnections() []oci.network.ipsecConnection
  // FastConnect virtual circuits
  virtualCircuits() []oci.network.virtualCircuit
  // FastConnect physical cross-connects
  crossConnects() []oci.network.crossConnect
}

// Oracle Cloud Infrastructure (OCI) public IP address
//
// Region-scoped public IP address in the tenancy: every `RESERVED` address
// (whether attached or not) plus the `EPHEMERAL` addresses held by regional
// entities such as NAT gateways. `lifetime` is `RESERVED` (you control its
// life; persists unassigned) or `EPHEMERAL` (tied to the entity it is attached
// to). `assignedEntityType` and `assignedEntityId` identify what the address
// is routed to, a `PRIVATE_IP` (a compute VNIC) or a `NAT_GATEWAY`; an empty
// `assignedEntityId` on a `RESERVED` address is an unattached, billable, and
// reusable public address. Inventory these to find the tenancy's
// internet-facing address surface and orphaned reservations.
//
// Availability-domain-scoped ephemeral public IPs (the addresses
// auto-assigned to compute instance VNICs) are not listed here; read
// `oci.compute.vnic.publicIp` for the per-instance view of those.
private oci.network.publicIp @defaults("ipAddress lifetime assignedEntityType") {
  // Public IP OCID
  id string
  // The public IP address
  ipAddress string
  // Display name
  name string
  // Compartment containing the public ip
  compartment() oci.compartment
  // Allocation lifetime (EPHEMERAL or RESERVED)
  lifetime string
  // Whether the address is region-wide or availability-domain scoped (REGION, AVAILABILITY_DOMAIN)
  scope string
  // Type of entity the address is assigned to (PRIVATE_IP, NAT_GATEWAY); empty when unassigned
  assignedEntityType string
  // OCID of the entity the address is assigned to; empty when unassigned
  assignedEntityId string
  // Availability domain, set only for availability-domain-scoped addresses
  availabilityDomain string
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, ASSIGNING, ASSIGNED, UNASSIGNING, UNASSIGNED, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Virtual Cloud Network (VCN)
//
// Regional private network in the tenancy that compute, load balancer, and
// gateway resources attach to. `cidrBlocks` defines its address space and
// `flowLogs` surfaces VCN flow logging when enabled. The VCN sits at the top
// of the network hierarchy that audits traverse to reach subnets, security
// lists, route tables, and gateways.
private oci.network.vcn @defaults("name") {
  // VCN OCID
  id string
  // Compartment containing the vcn
  compartment() oci.compartment
  // VCN display name
  name string
  // VCN creation time
  created time
  // VCN lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED, UPDATING)
  state string
  // IPv4 CIDR blocks for the VCN address space
  cidrBlocks []string
  // Oracle-allocated IPv6 CIDR blocks for the VCN address space
  //
  // Empty on an IPv4-only VCN. A VCN with an IPv6 range carries a second
  // address family whose reachability is governed by its own route rules and
  // security rules, so an audit written only against cidrBlocks does not
  // describe it.
  ipv6CidrBlocks []string
  // Customer-owned IPv6 CIDR blocks imported into the VCN (BYOIPv6)
  //
  // Empty when the VCN uses no imported address space.
  byoipv6CidrBlocks []string
  // ULA and private IPv6 CIDR blocks for the VCN address space
  //
  // Not routable to the internet. Empty when the VCN defines no private IPv6
  // range.
  ipv6PrivateCidrBlocks []string
  // Whether the VCN admits traffic only where a Zero Trust Packet Routing policy allows it
  //
  // When set, security lists and network security groups no longer decide on
  // their own what may reach resources in the VCN; a ZPR policy has to permit
  // the flow as well.
  isZprOnly bool
  // VCN domain name for DNS resolution
  vcnDomainName string
  // Security attributes applied to the VCN, keyed by namespace
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to this VCN's values. Zero Trust Packet
  // Routing statements can name a whole VCN this way rather than naming the
  // resources inside it.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Default DHCP options OCID for the VCN
  defaultDhcpOptionsId string
  // Default route table OCID for the VCN
  //
  // Deprecated in favor of defaultRouteTable.
  defaultRouteTableId @maturity("deprecated") string
  // Route table applied to subnets that specify none of their own
  defaultRouteTable() oci.network.routeTable
  // Default security list OCID for the VCN
  //
  // Deprecated in favor of defaultSecurityList.
  defaultSecurityListId @maturity("deprecated") string
  // Security list applied to subnets that specify none of their own
  defaultSecurityList() oci.network.securityList
  // DNS label for the VCN
  dnsLabel string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // Subnets carved out of the VCN address space
  subnets() []oci.network.subnet
  // Route tables defined in the VCN
  routeTables() []oci.network.routeTable
  // Security lists defined in the VCN
  //
  // The subnet-level firewall. OCI evaluates the union of a subnet's security
  // lists and the network security groups on a VNIC, so a rule in either layer
  // admits traffic on its own.
  securityLists() []oci.network.securityList
  // Network security groups defined in the VCN
  networkSecurityGroups() []oci.network.networkSecurityGroup
  // Internet gateways attached to the VCN
  //
  // A subnet reaches the internet only when its route table forwards a default
  // route to one of these and that gateway's isEnabled is true.
  internetGateways() []oci.network.internetGateway
  // NAT gateways attached to the VCN
  natGateways() []oci.network.natGateway
  // VCN flow logs (OCI logs with service=flowlogs whose resource is this VCN)
  flowLogs() []oci.logging.log
}

// Oracle Cloud Infrastructure (OCI) network subnet
//
// Address range partitioned from a VCN that VNICs attach to, carrying the
// controls that decide whether workloads on it are internet-reachable:
// `prohibitPublicIpOnVnic` and `prohibitInternetIngress` gate public
// addressing and ingress, `routeTable` forwards its traffic, and
// `securityLists` supplies the subnet-level firewall rules.
private oci.network.subnet @defaults("name") {
  // Subnet OCID
  id string
  // Subnet display name
  name string
  // Compartment containing the subnet
  compartment() oci.compartment
  // VCN the subnet belongs to
  vcn() oci.network.vcn
  // Availability domain (regional subnets have no AD)
  availabilityDomain string
  // IPv4 CIDR block for the subnet
  cidrBlock string
  // IPv4 CIDR blocks for the subnet
  //
  // The full list, for a subnet carrying more than the single range in
  // cidrBlock.
  ipv4CidrBlocks []string
  // Primary IPv6 CIDR block for the subnet
  //
  // Empty on an IPv4-only subnet.
  ipv6CidrBlock string
  // IPv6 CIDR blocks for the subnet
  //
  // Empty on an IPv4-only subnet. A dual-stack subnet reaches the internet
  // over a path that `prohibitPublicIpOnVnic` does not constrain, since that
  // flag governs IPv4 public addressing only.
  ipv6CidrBlocks []string
  // IPv6 address of the subnet's virtual router
  //
  // The default gateway VNICs on the subnet use for IPv6. Empty on an
  // IPv4-only subnet.
  ipv6VirtualRouterIp string
  // IPv4 address of the subnet's virtual router
  //
  // The default gateway VNICs on the subnet use for IPv4.
  virtualRouterIp string
  // MAC address of the subnet's virtual router
  virtualRouterMac string
  // Subnet lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED, UPDATING)
  state string
  // DNS label for the subnet
  dnsLabel string
  // Subnet domain name for DNS resolution
  subnetDomainName string
  // Whether public IP assignment on VNICs is prohibited
  prohibitPublicIpOnVnic bool
  // Whether internet ingress is prohibited
  prohibitInternetIngress bool
  // Route table used by this subnet
  routeTable() oci.network.routeTable
  // Security lists applied to the subnet
  securityLists() []oci.network.securityList
  // Subnet creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // Subnet flow logs (OCI logs with service=flowlogs whose resource is this subnet)
  flowLogs() []oci.logging.log
}

// Oracle Cloud Infrastructure (OCI) VCN security list (virtual firewall rules)
//
// Subnet-level firewall applied to every VNIC in the subnets that reference
// it. `ingressSecurityRules` and `egressSecurityRules` hold the allowed
// traffic; audits check these for rules that admit `0.0.0.0/0` or expose
// sensitive ports, and `hasStatelessRules` flags stateless rules that bypass
// connection tracking.
private oci.network.securityList @defaults("name") {
  // Security list OCID
  id string
  // Compartment containing the security list
  compartment() oci.compartment
  // Security list display name
  name string
  // Security list creation time
  created time
  // Security list lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Egress security rule dicts
  //
  // Deprecated in favor of egressRules, which resolves the port ranges nested
  // inside tcpOptions and udpOptions into queryable fields. Each entry:
  // {description, stateless, protocol (IANA number, e.g. "6"=TCP, "17"=UDP, "1"=ICMP), destination, destinationType, tcpOptions{sourcePortRange{min,max}, destinationPortRange{min,max}}, udpOptions{...}, icmpOptions{type,code}}.
  egressSecurityRules @maturity("deprecated") []dict
  // Ingress security rule dicts
  //
  // Deprecated in favor of ingressRules. Same shape as egressSecurityRules but
  // with source/sourceType instead of destination/destinationType.
  ingressSecurityRules @maturity("deprecated") []dict
  // Inbound rules the security list permits
  ingressRules []oci.network.securityRule
  // Outbound rules the security list permits
  egressRules []oci.network.securityRule
  // Whether any rule (ingress or egress) is stateless
  hasStatelessRules() bool
  // VCN that this security list belongs to
  vcn() oci.network.vcn
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Network Security Group (virtual firewall rules for VNICs)
//
// VNIC-level firewall whose rules apply only to the VNICs added to the group,
// giving finer-grained control than a subnet security list.
// `ingressSecurityRules` and `egressSecurityRules` hold the allowed traffic
// and `attachedVnics` lists what the group protects; audits check for rules
// admitting `0.0.0.0/0` or exposing sensitive ports.
private oci.network.networkSecurityGroup @defaults("name") {
  // NSG OCID
  id string
  // NSG display name
  name string
  // Compartment containing the NSG
  compartment() oci.compartment
  // VCN the NSG belongs to
  vcn() oci.network.vcn
  // NSG lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // NSG creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // Ingress security rule dicts
  //
  // Deprecated in favor of ingressRules, which resolves the port ranges nested
  // inside tcpOptions and udpOptions into queryable fields. Each entry:
  // {direction, protocol (IANA number, e.g. "6"=TCP), description, source, sourceType, isStateless, tcpOptions{sourcePortRange{min,max}, destinationPortRange{min,max}}, udpOptions{...}, icmpOptions{type,code}}.
  ingressSecurityRules() @maturity("deprecated") []dict
  // Egress security rule dicts
  //
  // Deprecated in favor of egressRules. Same shape as ingressSecurityRules but
  // with destination/destinationType instead of source/sourceType.
  egressSecurityRules() @maturity("deprecated") []dict
  // Inbound rules the group permits
  ingressRules() []oci.network.securityRule
  // Outbound rules the group permits
  egressRules() []oci.network.securityRule
  // Whether any rule (ingress or egress) is stateless
  hasStatelessRules() bool
  // VNICs this NSG is attached to (from ListNetworkSecurityGroupVnics + GetVnic)
  attachedVnics() []oci.compute.vnic
}

// Security rule from a VCN security list or network security group
//
// A single inbound or outbound rule, with the port ranges that OCI nests
// inside tcpOptions and udpOptions lifted into `sourcePortMin`,
// `sourcePortMax`, `destinationPortMin`, and `destinationPortMax` so traffic
// can be selected by port. `protocol` is the IANA protocol number as a string
// ("6" for TCP, "17" for UDP, "1" for ICMP, "58" for ICMPv6) or "all" when the
// rule covers every protocol. Rules from a security list apply to every VNIC
// in the subnets referencing it, while rules from a network security group
// apply only to the VNICs added to that group; OCI evaluates the union of
// both, so a rule from either layer can admit traffic on its own.
private oci.network.securityRule @defaults("direction protocol source destination destinationPortMin destinationPortMax") {
  // Traffic direction the rule covers: INGRESS or EGRESS
  direction string
  // Transport protocol
  //
  // The IANA protocol number as a string ("6" for TCP, "17" for UDP, "1" for
  // ICMP, "58" for ICMPv6), or "all" when the rule matches every protocol.
  protocol string
  // Rule description
  description string
  // Source the rule permits traffic from
  //
  // A CIDR block, the OCID of a network security group, or the name of a
  // service CIDR label, depending on sourceType. Empty on egress rules.
  source string
  // How to read the source value
  //
  // One of CIDR_BLOCK, SERVICE_CIDR_BLOCK, or NETWORK_SECURITY_GROUP. Empty on
  // egress rules.
  sourceType string
  // Destination the rule permits traffic to
  //
  // A CIDR block, the OCID of a network security group, or the name of a
  // service CIDR label, depending on destinationType. Empty on ingress rules.
  destination string
  // How to read the destination value
  //
  // One of CIDR_BLOCK, SERVICE_CIDR_BLOCK, or NETWORK_SECURITY_GROUP. Empty on
  // ingress rules.
  destinationType string
  // Whether the rule is stateless, so return traffic needs its own matching rule
  stateless bool
  // Lowest source port the rule covers
  //
  // Taken from the source port range of tcpOptions or udpOptions, whichever
  // the protocol selects. Null when the rule sets no source port range, which
  // means every source port, and on rules for protocols that carry no ports.
  sourcePortMin int
  // Highest source port the rule covers
  //
  // Null under the same conditions as sourcePortMin.
  sourcePortMax int
  // Lowest destination port the rule covers
  //
  // Taken from the destination port range of tcpOptions or udpOptions,
  // whichever the protocol selects. Null when the rule sets no destination
  // port range, which means every destination port, and on rules for protocols
  // that carry no ports. A null therefore admits more traffic than any explicit
  // range, so treat it as matching when selecting rules by port.
  destinationPortMin int
  // Highest destination port the rule covers
  //
  // Null under the same conditions as destinationPortMin.
  destinationPortMax int
  // ICMP message type the rule covers
  //
  // Null on rules for protocols other than ICMP and ICMPv6.
  icmpType int
  // ICMP message code the rule covers
  //
  // Null when the rule covers every code of its icmpType, and on rules for
  // protocols other than ICMP and ICMPv6.
  icmpCode int
}

// Oracle Cloud Infrastructure (OCI) internet gateway (connects VCN to the internet)
//
// Gateway that provides bidirectional internet connectivity for a VCN. A
// subnet is internet-reachable only when its route table forwards a default
// route to an internet gateway whose `isEnabled` is true, making this a key
// hop when auditing internet exposure.
private oci.network.internetGateway @defaults("name") {
  // Internet gateway OCID
  id string
  // Display name
  name string
  // Compartment containing the internet gateway
  compartment() oci.compartment
  // VCN the internet gateway belongs to
  vcn() oci.network.vcn
  // Whether the gateway is enabled
  isEnabled bool
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) NAT gateway (enables outbound internet access without inbound exposure)
//
// Gateway that lets a private subnet reach the internet for outbound traffic
// while blocking unsolicited inbound connections. `natIp` is the public
// address traffic egresses from and `blockTraffic` disables egress entirely.
// Unlike an internet gateway, a default route to a NAT gateway does not make a
// resource internet-reachable.
private oci.network.natGateway @defaults("name") {
  // NAT gateway OCID
  id string
  // Display name
  name string
  // Compartment containing the nat gateway
  compartment() oci.compartment
  // VCN the NAT gateway belongs to
  vcn() oci.network.vcn
  // Whether traffic is blocked through the gateway
  blockTraffic bool
  // The public IP address associated with the NAT gateway
  natIp string
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) route table (routing rules for VCN traffic)
//
// Set of forwarding rules that decide where a subnet's traffic goes. `routes`
// resolves each rule to its destination and target network entity (internet
// gateway, NAT gateway, service gateway, DRG, or local peering gateway),
// determining whether a subnet routes to the internet, stays private, or
// reaches peered and on-premises networks.
private oci.network.routeTable @defaults("name") {
  // Route table OCID
  id string
  // Display name
  name string
  // Compartment containing the route table
  compartment() oci.compartment
  // VCN the route table belongs to
  vcn() oci.network.vcn
  // Raw route rules defining traffic destinations and targets
  //
  // Each entry: {networkEntityId, destination, destinationType (CIDR_BLOCK,
  // SERVICE_CIDR_BLOCK), description, routeType (STATIC, LOCAL)}. See `routes`
  // for the same rules resolved to their destination and typed target entity.
  routeRules []dict
  // Route rules resolved to their destination and target network entity
  routes() []oci.network.routeTable.route
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) route rule
private oci.network.routeTable.route @defaults("destination targetType") {
  // Destination CIDR block or Oracle service CIDR label
  destination string
  // Type of destination (CIDR_BLOCK, SERVICE_CIDR_BLOCK)
  destinationType string
  // Route type (STATIC, LOCAL)
  routeType string
  // Description of the route rule
  description string
  // Kind of network entity matching traffic is forwarded to
  //
  // One of INTERNET_GATEWAY, NAT_GATEWAY, SERVICE_GATEWAY, DRG,
  // LOCAL_PEERING_GATEWAY, PRIVATE_IP, or the raw OCID resource type when the
  // entity is a kind not resolved to a dedicated accessor.
  targetType string
  // OCID of the network entity matching traffic is forwarded to
  targetId string
  // Internet gateway matching traffic is forwarded to, when targetType is INTERNET_GATEWAY
  internetGateway() oci.network.internetGateway
  // NAT gateway matching traffic is forwarded to, when targetType is NAT_GATEWAY
  natGateway() oci.network.natGateway
  // Service gateway matching traffic is forwarded to, when targetType is SERVICE_GATEWAY
  serviceGateway() oci.network.serviceGateway
  // Dynamic routing gateway matching traffic is forwarded to, when targetType is DRG
  drg() oci.network.drg
  // Local peering gateway matching traffic is forwarded to, when targetType is LOCAL_PEERING_GATEWAY
  localPeeringGateway() oci.network.localPeeringGateway
}

// Oracle Cloud Infrastructure (OCI) dynamic routing gateway (DRG)
//
// Virtual router connecting a VCN to networks outside it: other VCNs,
// on-premises sites over IPSec VPN or FastConnect, and remotely peered
// regions. `attachments` lists what is connected and `remotePeeringConnections`
// the cross-region peerings, making the DRG the hub audits follow to map
// hybrid and cross-network reachability.
private oci.network.drg @defaults("name state") {
  // DRG OCID
  id string
  // Display name
  name string
  // Compartment containing the DRG
  compartment() oci.compartment
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Attachments connecting VCNs, IPSec tunnels, virtual circuits, and remote peering connections to the DRG
  attachments() []oci.network.drgAttachment
  // Remote peering connections hosted on the DRG
  remotePeeringConnections() []oci.network.remotePeeringConnection
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) DRG attachment
//
// Link connecting one network to a dynamic routing gateway. `networkType`
// selects which reference resolves (VCN, IPSEC_TUNNEL, VIRTUAL_CIRCUIT,
// REMOTE_PEERING_CONNECTION, or LOOPBACK), and `isCrossTenancy` flags an
// attachment that reaches into another tenancy, an important boundary when
// auditing what a DRG bridges together.
private oci.network.drgAttachment @defaults("name networkType state") {
  // DRG attachment OCID
  id string
  // Display name
  name string
  // Compartment containing the DRG attachment
  compartment() oci.compartment
  // DRG the attachment belongs to
  drg() oci.network.drg
  // Kind of network attached to the DRG (VCN, IPSEC_TUNNEL, VIRTUAL_CIRCUIT, REMOTE_PEERING_CONNECTION, LOOPBACK)
  networkType string
  // OCID of the attached network entity
  networkId string
  // VCN attached to the DRG, when networkType is VCN
  vcn() oci.network.vcn
  // IPSec connection attached to the DRG, when networkType is IPSEC_TUNNEL
  ipsecConnection() oci.network.ipsecConnection
  // Virtual circuit attached to the DRG, when networkType is VIRTUAL_CIRCUIT
  virtualCircuit() oci.network.virtualCircuit
  // Whether the attachment and attached network live in a different tenancy than the DRG
  isCrossTenancy bool
  // Lifecycle state (e.g., ATTACHING, ATTACHED, DETACHING, DETACHED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) local peering gateway (LPG)
//
// Endpoint that privately connects two VCNs in the same region without routing
// over the internet. `peeringStatus` reports whether the peering is live,
// `peerAdvertisedCidr` the address range the far VCN advertises, and
// `isCrossTenancyPeering` whether the peer belongs to another tenancy.
private oci.network.localPeeringGateway @defaults("name peeringStatus") {
  // LPG OCID
  id string
  // Display name
  name string
  // Compartment containing the LPG
  compartment() oci.compartment
  // VCN that uses the LPG
  vcn() oci.network.vcn
  // Peer LPG at the other end of the peering
  peer() oci.network.localPeeringGateway
  // Whether the VCN at the other end of the peering is in a different tenancy
  isCrossTenancyPeering bool
  // Peering status (NEW, PENDING, PEERED, REVOKED, INVALID)
  peeringStatus string
  // Smallest aggregate CIDR containing all routes advertised by the peered VCN
  peerAdvertisedCidr string
  // Route table used by the LPG for traffic destined to the peered VCN
  routeTable() oci.network.routeTable
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) remote peering connection (RPC)
//
// Endpoint on a DRG that privately connects a VCN to a VCN in another region.
// `peeringStatus` reports whether the peering is live, `peerRegionName` the
// region at the far end, and `isCrossTenancyPeering` whether the peer belongs
// to another tenancy, all relevant when auditing cross-region reachability.
private oci.network.remotePeeringConnection @defaults("name peeringStatus") {
  // RPC OCID
  id string
  // Display name
  name string
  // Compartment containing the RPC
  compartment() oci.compartment
  // DRG that hosts the RPC
  drg() oci.network.drg
  // Whether the VCN at the other end of the peering is in a different tenancy
  isCrossTenancyPeering bool
  // Peering status (NEW, PENDING, PEERED, REVOKED, INVALID)
  peeringStatus string
  // OCID of the peer RPC at the other end of the peering
  peerId string
  // Region containing the peer RPC
  peerRegionName string
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) service gateway
//
// Gateway that gives a private subnet access to Oracle Services Network
// endpoints (such as Object Storage) without traversing the internet.
// `services` lists the service CIDR labels it opens egress to and
// `blockTraffic` disables the gateway regardless of route rules.
private oci.network.serviceGateway @defaults("name state") {
  // Service gateway OCID
  id string
  // Display name
  name string
  // Compartment containing the service gateway
  compartment() oci.compartment
  // VCN the service gateway belongs to
  vcn() oci.network.vcn
  // Route table used by the service gateway
  routeTable() oci.network.routeTable
  // Whether the gateway blocks all traffic regardless of route rules
  blockTraffic bool
  // Oracle Services Network services the gateway opens private egress to
  services() []oci.network.service
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Oracle Services Network service
//
// A range of Oracle service public IP addresses reachable privately through a
// service gateway. The `cidrBlock` is the service CIDR label (for example
// `oci-phx-objectstorage`) used as the destination in the route and security
// rules that direct traffic to the gateway, and `name` and `description`
// identify the covered service, such as regional Object Storage or the
// all-services label.
private oci.network.service @defaults("name cidrBlock") {
  // Service OCID
  id string
  // Service name (e.g., OCI PHX Object Storage)
  name string
  // Service CIDR label representing the covered Oracle service IP ranges (e.g., oci-phx-objectstorage)
  cidrBlock string
  // Description of the covered Oracle service or services
  description string
}

// Oracle Cloud Infrastructure (OCI) customer-premises equipment (CPE)
//
// On-premises router that terminates a site-to-site IPSec VPN. `ipAddress` is
// the router's public address at the customer end and `isPrivate` marks a
// device reachable only over a private network rather than the public
// internet.
private oci.network.cpe @defaults("name ipAddress") {
  // CPE OCID
  id string
  // Display name
  name string
  // Compartment containing the CPE
  compartment() oci.compartment
  // Public IP address of the on-premises router terminating the VPN
  ipAddress string
  // OCID of the CPE device shape describing the on-premises router model
  cpeDeviceShapeId string
  // Whether the CPE is reachable only over a private network rather than the public internet
  isPrivate bool
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) site-to-site VPN IPSec connection
//
// Encrypted VPN linking an on-premises network to a VCN, terminating on a CPE
// at the customer end and a DRG at the OCI end. `transportType` records
// whether it runs over the public internet or FastConnect, and `tunnels`
// exposes the individual encrypted tunnels whose negotiated cryptography
// audits inspect.
private oci.network.ipsecConnection @defaults("name state") {
  // IPSec connection OCID
  id string
  // Display name
  name string
  // Compartment containing the IPSec connection
  compartment() oci.compartment
  // Customer-premises equipment terminating the on-premises end of the connection
  cpe() oci.network.cpe
  // Dynamic routing gateway terminating the OCI end of the connection
  drg() oci.network.drg
  // Static routes advertised to the CPE, as CIDR blocks
  staticRoutes []string
  // Identifier the CPE presents during IKE negotiation
  cpeLocalIdentifier string
  // Type of the CPE local identifier (IP_ADDRESS, HOSTNAME)
  cpeLocalIdentifierType string
  // Transport the tunnels traverse (INTERNET routes over the public internet; PRIVATE routes over FastConnect)
  transportType string
  // Tunnels carrying the connection's encrypted traffic
  tunnels() []oci.network.ipsecConnectionTunnel
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) IPSec tunnel
//
// One encrypted tunnel within a site-to-site VPN, carrying the negotiated
// cryptographic parameters that security audits check: the IKE version, the
// key-exchange encryption, authentication, and Diffie-Hellman group, and the
// data-channel encryption, authentication, and perfect-forward-secrecy
// setting. `status` and `bgpState` report whether the tunnel is up.
private oci.network.ipsecConnectionTunnel @defaults("name status ikeVersion") {
  // Tunnel OCID
  id string
  // Display name
  name string
  // Compartment containing the tunnel
  compartment() oci.compartment
  // Tunnel connection status (UP, DOWN, DOWN_FOR_MAINTENANCE, PARTIAL_UP)
  status string
  // IKE protocol version negotiated (V1, V2)
  //
  // IKEv1 is weaker than IKEv2; audits that require modern key exchange expect
  // V2.
  ikeVersion string
  // Routing type (BGP, STATIC, POLICY)
  routing string
  // Whether Oracle can initiate the tunnel (INITIATOR_OR_RESPONDER, RESPONDER_ONLY)
  oracleCanInitiate string
  // Whether NAT-T is enabled (ENABLED, DISABLED, AUTO)
  natTranslationEnabled string
  // Dead peer detection mode (INITIATE_AND_RESPOND, RESPOND_ONLY)
  dpdMode string
  // Oracle VPN headend IP address for the tunnel
  vpnIp string
  // CPE IP address for the tunnel
  cpeIp string
  // BGP session state, when routing is BGP (UP, DOWN)
  bgpState string
  // Negotiated IKE (ISAKMP) key-exchange encryption algorithm
  //
  // Null until the tunnel negotiates its security association, and while the
  // tunnel is down; the other negotiated crypto fields behave the same way.
  // Audit the configured counterparts below when the tunnel may not be up,
  // since those carry a value regardless.
  phase1EncryptionAlgorithm() string
  // Negotiated IKE (ISAKMP) key-exchange authentication algorithm
  phase1AuthenticationAlgorithm() string
  // Negotiated IKE (ISAKMP) key-exchange Diffie-Hellman group
  //
  // Larger groups resist attack better. Groups below 14 (2048-bit MODP)
  // are considered weak for modern key exchange.
  phase1DhGroup() string
  // Negotiated IPSec (ESP) data-channel encryption algorithm
  phase2EncryptionAlgorithm() string
  // Negotiated IPSec (ESP) data-channel authentication algorithm
  phase2AuthenticationAlgorithm() string
  // Whether perfect forward secrecy is enabled for the IPSec (ESP) data channel
  //
  // False also means the tunnel's negotiated crypto is unavailable (the
  // tunnel is down), not only that perfect forward secrecy is disabled.
  // Combine with `status` when auditing so an unknown tunnel is not
  // mistaken for one with it turned off.
  phase2PfsEnabled() bool
  // Diffie-Hellman group for IPSec (ESP) perfect forward secrecy
  phase2DhGroup() string
  // Configured IKE (ISAKMP) key-exchange encryption algorithm
  //
  // The custom proposal set on the tunnel, present whether or not the tunnel
  // has come up, so a tunnel that never negotiated is still auditable. Null
  // when the tunnel uses Oracle's default proposal set rather than a custom
  // one; `isCustomPhase1Config` distinguishes the two.
  phase1ConfiguredEncryptionAlgorithm() string
  // Configured IKE (ISAKMP) key-exchange authentication algorithm
  phase1ConfiguredAuthenticationAlgorithm() string
  // Configured IKE (ISAKMP) key-exchange Diffie-Hellman group
  phase1ConfiguredDhGroup() string
  // Configured IPSec (ESP) data-channel encryption algorithm
  phase2ConfiguredEncryptionAlgorithm() string
  // Configured IPSec (ESP) data-channel authentication algorithm
  phase2ConfiguredAuthenticationAlgorithm() string
  // Configured IPSec (ESP) data-channel Diffie-Hellman group
  phase2ConfiguredDhGroup() string
  // Whether the tunnel uses a custom IKE (ISAKMP) proposal set
  isCustomPhase1Config() bool
  // Whether the tunnel uses a custom IPSec (ESP) proposal set
  isCustomPhase2Config() bool
  // Whether the IKE (ISAKMP) security association is established
  //
  // False when the key exchange has not completed, which is also why the
  // negotiated phase 1 fields read null.
  isIkeEstablished() bool
  // Whether the IPSec (ESP) security association is established
  isEspEstablished() bool
  // IKE (ISAKMP) security association lifetime in seconds
  //
  // Null when OCI reports no lifetime. Shorter lifetimes force more frequent
  // rekeying.
  phase1Lifetime() int
  // IPSec (ESP) security association lifetime in seconds
  phase2Lifetime() int
  // Lifecycle state (e.g., PROVISIONING, AVAILABLE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) FastConnect virtual circuit
//
// Dedicated FastConnect connection between the tenancy and an on-premises
// network or Oracle public services. `type` distinguishes a PUBLIC circuit
// reaching Oracle public services from a PRIVATE circuit reaching a VCN
// through a DRG, and the BGP session and administrative state report whether
// the routing session is up.
private oci.network.virtualCircuit @defaults("name type state") {
  // Virtual circuit OCID
  id string
  // Display name
  name string
  // Compartment containing the virtual circuit
  compartment() oci.compartment
  // Circuit type (PUBLIC exposes Oracle public services; PRIVATE reaches a VCN through a DRG)
  type string
  // Provisioning model (COLOCATED for a direct cross-connect; LAYER2 or LAYER3 for a provider-mediated circuit)
  serviceType string
  // Bandwidth shape name (e.g., 10 Gbps)
  bandwidthShapeName string
  // Whether Oracle or the customer manages BGP (CUSTOMER_MANAGED, ORACLE_MANAGED, PROVIDER_MANAGED)
  bgpManagement string
  // BGP session state (UP, DOWN)
  bgpSessionState string
  // BGP administrative state (ENABLED, DISABLED)
  bgpAdminState string
  // Dynamic routing gateway the circuit reaches, for a private virtual circuit
  drg() oci.network.drg
  // FastConnect partner name, for a provider-mediated circuit
  providerName string
  // FastConnect partner service name, for a provider-mediated circuit
  providerServiceName string
  // Public IP prefixes advertised over a public virtual circuit
  publicPrefixes []string
  // BGP peering mappings to the underlying cross-connects
  //
  // Each entry: {crossConnectOrCrossConnectGroupId, vlan, customerBgpPeeringIp,
  // oracleBgpPeeringIp, customerBgpPeeringIpv6, oracleBgpPeeringIpv6}.
  crossConnectMappings []dict
  // Lifecycle state (e.g., PENDING_PROVIDER, PROVISIONING, PROVISIONED, FAILED, INACTIVE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) FastConnect cross-connect
//
// Physical port at a FastConnect location that carries the tenancy's dedicated
// connectivity. `locationName` and `portName` identify the port,
// `portSpeedShapeName` its bandwidth, and `crossConnectGroupId` the link
// aggregation group when multiple cross-connects are bonded together.
private oci.network.crossConnect @defaults("name state") {
  // Cross-connect OCID
  id string
  // Display name
  name string
  // Compartment containing the cross-connect
  compartment() oci.compartment
  // Name of the FastConnect location housing the physical port
  locationName string
  // Name of the physical port at the FastConnect location
  portName string
  // Port speed shape name (e.g., 10 Gbps)
  portSpeedShapeName string
  // OCID of the cross-connect group this cross-connect belongs to, when grouped for link aggregation
  crossConnectGroupId string
  // Lifecycle state (e.g., PENDING_CUSTOMER, PROVISIONING, PROVISIONED, INACTIVE, TERMINATING, TERMINATED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Logging
//
// Logging service for the tenancy, covering both service-emitted and
// custom logs across every compartment. The `logGroups` field returns
// the log groups, each holding its enabled `logs`, retention duration,
// source service and resource, and the log category (read, write, or
// all) that describes which events are captured. Query it to confirm
// audit and diagnostic logging is enabled where required.
oci.logging {
  // Log groups in the tenancy
  logGroups() []oci.logging.logGroup
}

// Oracle Cloud Infrastructure (OCI) log group
private oci.logging.logGroup @defaults("name") {
  // Log group OCID
  id string
  // Display name
  name string
  // Log group description
  description string
  // Compartment containing the log group
  compartment() oci.compartment
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, INACTIVE, DELETING, FAILED)
  state string
  // Logs in the group
  logs() []oci.logging.log
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) log
private oci.logging.log @defaults("name") {
  // Log OCID
  id string
  // Display name
  name string
  // Log type (SERVICE, CUSTOM)
  logType string
  // Parent log group
  logGroup() oci.logging.logGroup
  // Whether the log is enabled
  isEnabled bool
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, INACTIVE, DELETING, FAILED)
  state string
  // Retention duration in days
  retentionDuration int
  // Log configuration
  //
  // Keyed by `compartmentId` (OCID of the compartment owning the log),
  // `source`, a nested map describing the emitting service, and
  // `archiving` when object-storage archiving is configured. For a
  // service log the `source` map carries `sourceType`, `service`,
  // `resource`, and `category`. The `category`, `sourceService`, and
  // `sourceResource` fields surface those source values directly. The
  // `archiving` map carries `isEnabled`.
  configuration dict
  // Log category (e.g., all, write, read — depends on service)
  category string
  // Emitting OCI service (e.g., flowlogs, objectstorage, vcn)
  sourceService string
  // OCID of the resource emitting the log (e.g., a subnet OCID for flow logs)
  sourceResource string
  // Creation time
  created time
  // Last modification time
  timeLastModified time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Key Management Service
//
// Encryption vaults and customer-managed keys across the tenancy. Each
// vault (DEFAULT, VIRTUAL_PRIVATE, or EXTERNAL type) exposes its
// management endpoint and the keys it holds, and each key reports its
// algorithm, length, lifecycle state, rotation history, and whether it
// is software-protected or HSM-protected. Audit key rotation policy,
// protection mode, and vault type to confirm keys meet cryptographic
// and isolation requirements.
oci.kms {
  // Key vaults in the tenancy
  vaults() []oci.kms.vault
}

// Oracle Cloud Infrastructure (OCI) KMS vault
private oci.kms.vault @defaults("name") {
  // Vault OCID
  id string
  // Display name
  name string
  // Compartment containing the vault
  compartment() oci.compartment
  // Vault type (DEFAULT, VIRTUAL_PRIVATE, EXTERNAL)
  vaultType string
  // Lifecycle state
  //
  // For example: CREATING, ACTIVE, DELETING, DELETED, PENDING_DELETION, SCHEDULING_DELETION, CANCELLING_DELETION, UPDATING, BACKUP_IN_PROGRESS, RESTORING.
  state string
  // Vault management endpoint
  managementEndpoint string
  // Keys in the vault
  keys() []oci.kms.key
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) KMS key
private oci.kms.key @defaults("name") {
  // Key OCID
  id string
  // Display name
  name string
  // Compartment containing the key
  compartment() oci.compartment
  // Parent vault containing the key
  vault() oci.kms.vault
  // Key algorithm (AES, RSA, ECDSA)
  algorithm string
  // Key length in bits
  //
  // For example 2048 or 4096 for RSA keys and 256 for AES keys. For
  // ECDSA keys this is the key's byte length expressed in bits; use
  // `curveId` for the exact curve.
  length() int
  // Elliptic curve for ECDSA keys
  //
  // One of NIST_P256, NIST_P384, or NIST_P521. Empty for non-ECDSA
  // (AES, RSA) keys.
  curveId() string
  // Protection mode (HSM, SOFTWARE, EXTERNAL)
  protectionMode string
  // Lifecycle state
  //
  // For example: CREATING, ENABLING, ENABLED, DISABLING, DISABLED, DELETING, DELETED, PENDING_DELETION, SCHEDULING_DELETION, CANCELLING_DELETION, UPDATING, BACKUP_IN_PROGRESS, RESTORING.
  state string
  // Whether auto key rotation is enabled
  isAutoRotationEnabled bool
  // Key versions (rotation history)
  keyVersions() []oci.kms.keyVersion
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) KMS key version (represents a specific version of a master encryption key)
private oci.kms.keyVersion @defaults("id") {
  // Key version OCID
  id string
  // Master encryption key OCID
  keyId string
  // Parent vault containing the key version
  vault() oci.kms.vault
  // Compartment containing the key version
  compartment() oci.compartment
  // Origin of the key material (INTERNAL, EXTERNAL)
  origin string
  // Lifecycle state
  //
  // For example: CREATING, ENABLING, ENABLED, DISABLING, DISABLED, DELETING, DELETED, PENDING_DELETION, SCHEDULING_DELETION, CANCELLING_DELETION.
  state string
  // Whether this version was created by auto-rotation
  isAutoRotated bool
  // Creation time
  created time
  // Scheduled deletion time
  timeOfDeletion time
}

// Oracle Cloud Infrastructure (OCI) Object Storage
//
// S3-compatible object storage for the tenancy. The `namespace` field
// holds the unique per-tenancy Object Storage namespace, and `buckets`
// enumerates every bucket. Each bucket surfaces its public access type,
// storage tier, auto-tiering and versioning configuration, KMS
// encryption key, retention rules for WORM/immutability, replication
// enablement, and the approximate object count and total size.
oci.objectStorage {
  // Object Storage namespace (unique per tenancy)
  namespace() string
  // Object Storage buckets
  buckets() []oci.objectStorage.bucket
}

// Oracle Cloud Infrastructure (OCI) Object Storage bucket
private oci.objectStorage.bucket {
  // Object Storage namespace
  namespace string
  // Bucket name
  name string
  // Compartment containing the bucket
  compartment() oci.compartment
  // Bucket creation time
  created time
  // Region where the bucket is located
  region oci.region
  // OCID of the principal that created the bucket
  createdBy() string
  // User that created the bucket, when the creating principal is an IAM user
  createdByUser() oci.identity.user
  // Public access type (NoPublicAccess, ObjectRead, ObjectReadWithoutList)
  publicAccessType() string
  // Storage tier (Standard or Archive)
  storageTier() string
  // Auto-tiering status (Disabled or InfrequentAccess)
  autoTiering() string
  // Object versioning status (Enabled, Suspended, Disabled)
  versioning() string
  // Whether object state change events are emitted
  objectEventsEnabled() bool
  // Whether the bucket is configured as a replication source
  replicationEnabled() bool
  // Bucket OCID
  //
  // Resolved from the bucket detail call, so it reports the same value whether
  // the bucket came from `oci.objectStorage.buckets` or from
  // `oci.objectStorage.bucket(namespace:, name:)`.
  ocid() string
  // Whether the bucket is read-only (true when configured as replication destination)
  isReadOnly() bool
  // Entity tag (ETag) for change detection
  etag() string
  // KMS key OCID for encryption
  kmsKeyId() string
  // Whether the bucket derives a per-bucket data key from its KMS key
  //
  // A bucket key lets Object Storage encrypt objects under a key derived once
  // for the bucket rather than calling the vault per object. Without it every
  // object is wrapped directly by the KMS key.
  isBucketKeyEnabled() bool
  // Whether the bucket name is unique within its namespace or only within its compartment
  //
  // One of GLOBAL or COMPARTMENT. A compartment-scoped bucket name can be
  // reused in another compartment of the same namespace.
  bucketScope() string
  // User-defined metadata stored on the bucket
  //
  // Free-form keys set at creation. Object Storage returns them unchanged and
  // attaches no meaning to them.
  metadata() map[string]string
  // Approximate number of objects in the bucket
  approximateCount() int
  // Approximate total size of objects in bytes
  approximateSize() int
  // Object lifecycle policy ETag
  objectLifecyclePolicyEtag() string
  // Free-form tags for resource management
  freeformTags() map[string]string
  // Defined tags for governance and cost tracking
  definedTags() map[string]map[string]string
  // Retention rules for immutability/WORM compliance
  retentionRules() []oci.objectStorage.retentionRule
  // Pre-authenticated requests granting credential-free access to the bucket or its objects
  preauthenticatedRequests() []oci.objectStorage.preauthenticatedRequest
}

// Oracle Cloud Infrastructure (OCI) Object Storage pre-authenticated request
//
// Pre-authenticated request (PAR), a URL that grants access to the
// bucket or a specific object without any IAM credential or audit
// trail for the duration of its validity. `accessType` is one of
// `ObjectRead`, `ObjectWrite`, `ObjectReadWrite`, `AnyObjectRead`,
// `AnyObjectWrite`, or `AnyObjectReadWrite`; the `AnyObject*` forms apply to
// the whole bucket. `objectName` names the single object a PAR is scoped to,
// and is null for bucket-level PARs. `bucketListingAction` (`Deny` or
// `ListObjects`) decides whether the holder can enumerate bucket contents.
// `timeExpires` is when the URL stops working. A far-future value is an
// effectively permanent anonymous grant.
private oci.objectStorage.preauthenticatedRequest @defaults("name accessType timeExpires") {
  // Pre-authenticated request identifier
  id string
  // Display name
  name string
  // Operation the request grants (ObjectRead, ObjectWrite, ObjectReadWrite, AnyObjectRead, AnyObjectWrite, AnyObjectReadWrite)
  accessType string
  // Object the request is scoped to; null when the request grants access to the entire bucket
  objectName string
  // Whether the holder may list bucket contents (Deny or ListObjects)
  bucketListingAction string
  // Expiration time after which the request is no longer valid
  timeExpires time
  // Creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) Object Storage retention rule
private oci.objectStorage.retentionRule @defaults("name") {
  // Retention rule OCID
  id string
  // Retention rule display name
  name string
  // Duration amount (in timeUnit units)
  durationAmount int
  // Duration time unit (DAYS, YEARS)
  durationTimeUnit string
  // Time after which the rule becomes locked and cannot be deleted
  timeRuleLocked time
  // Creation time
  created time
  // Last modification time
  timeModified time
}

// Oracle Cloud Infrastructure (OCI) File Storage
//
// Managed NFS file systems across the tenancy's availability domains,
// exposed through fileSystems, together with the machinery that decides
// who may mount them. `exports` carries the NFS export options that
// authorize clients by source CIDR, `mountTargets` carries the network
// placement and Kerberos or LDAP identity mapping each export is served
// through, and `exportSets` groups the exports served by one mount
// target. `replications`, `snapshotPolicies` and `outboundConnectors`
// cover cross-region copies, snapshot retention, and the LDAP servers
// mount targets bind to.
oci.fileStorage {
  // File systems
  fileSystems() []oci.fileStorage.fileSystem
  // Mount targets serving NFS exports
  mountTargets() []oci.fileStorage.mountTarget
  // Export sets grouping the exports on a mount target
  exportSets() []oci.fileStorage.exportSet
  // NFS exports and the client options authorizing them
  exports() []oci.fileStorage.export
  // Snapshot policies and their schedules
  snapshotPolicies() []oci.fileStorage.snapshotPolicy
  // Cross-region file system replications
  replications() []oci.fileStorage.replication
  // Outbound connectors mount targets bind to for LDAP identity mapping
  outboundConnectors() []oci.fileStorage.outboundConnector
}

// Oracle Cloud Infrastructure (OCI) file system
//
// A single managed NFS file system. Reports the availability domain
// it is anchored to, the KMS key encrypting its data at rest, metered
// byte consumption including snapshots, and clone lineage through
// parentFileSystem and isCloneParent, supporting encryption-coverage
// and storage-capacity audits.
private oci.fileStorage.fileSystem @defaults("name") {
  // File system OCID
  id string
  // File system display name
  name string
  // Compartment containing the file system
  compartment() oci.compartment
  // Availability domain where the file system is located
  availabilityDomain string
  // File system lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // KMS key for file system encryption
  kmsKey() oci.kms.key
  // Number of bytes consumed by the file system including snapshots
  meteredBytes int
  // File system this one was cloned from, when this is a clone
  parentFileSystem() oci.fileStorage.fileSystem
  // Whether this file system is the parent of at least one clone
  isCloneParent bool
  // NFS exports publishing this file system
  //
  // Each export publishes the file system at a path on one mount target and
  // carries the client options deciding which source addresses may mount it
  // and with what access. A file system with no exports is unreachable over
  // NFS whatever its mount targets allow.
  exports() []oci.fileStorage.export
  // Snapshots of this file system
  snapshots() []oci.fileStorage.snapshot
  // Snapshot policy scheduling this file system's snapshots
  snapshotPolicy() oci.fileStorage.snapshotPolicy
  // Storage quota rules applied to this file system
  //
  // Rules are reported for every principal scope the service supports:
  // whole-file-system, default user and group, and individual users and
  // groups.
  quotaRules() []oci.fileStorage.quotaRule
  // Quota enforcement state
  //
  // One of ENABLING, ENABLED, DISABLING, DISABLED, SYNCING, or FAILED. Only
  // ENABLED means the rules in `quotaRules` are being applied.
  quotaEnforcementState string
  // File system creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) mount target
//
// NFS endpoint a file system is served through. The mount target owns the
// network placement of the share: `subnet` and `securityGroups` decide which
// clients can reach it at all, and `privateIpIds` are the addresses it answers
// on. Identity mapping is set here too, through `idmapType` with the `ldap`
// fields, or through Kerberos with `kerberosEnabled` and `kerberosRealm`. The
// exports served through this target hang off `exportSet`.
private oci.fileStorage.mountTarget @defaults("name state availabilityDomain") {
  // Mount target OCID
  id string
  // Mount target display name
  name string
  // Compartment containing the mount target
  compartment() oci.compartment
  // Availability domain the mount target is in
  availabilityDomain string
  // Mount target lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Subnet the mount target's addresses are allocated from
  subnet() oci.network.subnet
  // Network security groups applied to the mount target
  //
  // The NSGs are the mount target's own packet filter, evaluated in addition
  // to the subnet's security lists. A mount target with no NSGs is
  // constrained only by its subnet.
  securityGroups() []oci.network.networkSecurityGroup
  // Export set holding the exports this mount target serves
  exportSet() oci.fileStorage.exportSet
  // OCIDs of the private IP addresses the mount target answers on
  privateIpIds []string
  // OCIDs of the IPv6 addresses the mount target answers on
  ipv6Ids []string
  // Identity mapping mode for UNIX credentials
  //
  // LDAP when the mount target maps incoming UIDs and GIDs through the
  // directory named by the `ldap` fields, NONE when it trusts the credentials
  // the client presents.
  idmapType() string
  // LDAP schema the identity mapping reads
  //
  // RFC2307 or RFC2307BIS.
  ldapSchemaType() string
  // Base distinguished name user lookups start from
  ldapUserSearchBase() string
  // Base distinguished name group lookups start from
  ldapGroupSearchBase() string
  // Seconds between refreshes of the identity mapping cache
  ldapCacheRefreshIntervalSeconds() int
  // Seconds a cached identity mapping stays valid
  ldapCacheLifetimeSeconds() int
  // Seconds a failed identity lookup is remembered
  ldapNegativeCacheLifetimeSeconds() int
  // Outbound connectors the identity mapping binds to
  ldapOutboundConnectors() []oci.fileStorage.outboundConnector
  // Whether Kerberos authentication is enabled on the mount target
  //
  // With Kerberos enabled the export options may demand KRB5, KRB5I or KRB5P
  // rather than AUTH_SYS, which is what moves the trust decision off the
  // client's self-asserted UID.
  kerberosEnabled() bool
  // Kerberos realm the mount target authenticates against
  kerberosRealm() string
  // Vault secret holding the mount target's Kerberos keytab
  kerberosKeyTabSecret() oci.vault.secret
  // Keytab secret version currently in use
  kerberosKeyTabSecretVersion() int
  // Keytab secret version kept as the fallback
  kerberosBackupKeyTabSecretVersion() int
  // Throughput requested for the mount target, in bytes per second
  requestedThroughput int
  // Throughput currently observed on the mount target, in bytes per second
  observedThroughput int
  // Storage capacity reserved for the mount target, in bytes
  reservedStorageCapacity int
  // Mount target creation time
  created time
  // Security attributes applied to the mount target, keyed by namespace
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to this mount target's values. Zero Trust
  // Packet Routing authorizes traffic from these rather than from the mount
  // target's address.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) export set
//
// Group of NFS exports served through one mount target. The export set is
// bound to a VCN, so it also records which virtual network the file systems
// under it are published into. `maxFsStatBytes` and `maxFsStatFiles` are the
// capacity figures the exports report back to NFS clients.
private oci.fileStorage.exportSet @defaults("name state availabilityDomain") {
  // Export set OCID
  id string
  // Export set display name
  name string
  // Compartment containing the export set
  compartment() oci.compartment
  // Availability domain the export set is in
  availabilityDomain string
  // Export set lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED)
  state string
  // Virtual cloud network the export set publishes into
  vcn() oci.network.vcn
  // Exports in this export set
  exports() []oci.fileStorage.export
  // Capacity reported to NFS clients, in bytes
  maxFsStatBytes() int
  // File count reported to NFS clients
  maxFsStatFiles() int
  // Export set creation time
  created time
}

// Oracle Cloud Infrastructure (OCI) NFS export
//
// Publication of one file system at a path on one mount target, and the point
// where NFS access is authorized. The `options` list is the access control:
// each entry admits a source CIDR with read-write or read-only access, decides
// whether root is squashed, and names the authentication flavors accepted. An
// export whose options admit `0.0.0.0/0` with `identitySquash` set to NONE is
// an open share with root access.
private oci.fileStorage.export @defaults("path state") {
  // Export OCID
  id string
  // Path the file system is exported at
  path string
  // Export lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED)
  state string
  // File system published by this export
  fileSystem() oci.fileStorage.fileSystem
  // Export set this export belongs to
  exportSet() oci.fileStorage.exportSet
  // Client options authorizing access to the export
  options() []oci.fileStorage.export.option
  // Whether supplementary groups are looked up through identity mapping for AUTH_SYS clients
  //
  // With this off, an AUTH_SYS client's own list of supplementary groups is
  // trusted as sent, so a client can claim membership of any group.
  isIdmapGroupsForSysAuth bool
  // Export creation time
  created time
}

// Client options on an NFS export
//
// One access rule on an export: which client addresses it covers and what they
// are allowed to do. `source` is the CIDR or address the rule applies to,
// `access` is READ_WRITE or READ_ONLY, and `identitySquash` decides whether
// incoming root or all users are remapped to the anonymous UID and GID.
// `allowedAuth` names the authentication flavors accepted, which is where a
// Kerberos requirement is expressed.
private oci.fileStorage.export.option @defaults("source access identitySquash") {
  // Source address or CIDR block the rule applies to
  source string
  // Access granted to matching clients
  //
  // READ_WRITE or READ_ONLY.
  access string
  // Which incoming identities are remapped to the anonymous UID and GID
  //
  // NONE leaves client credentials as sent, ROOT remaps only UID 0, and ALL
  // remaps every user. NONE on a wide source is what lets a client mount the
  // share as root.
  identitySquash string
  // Whether clients must connect from a privileged source port below 1024
  requirePrivilegedSourcePort bool
  // UID that squashed identities are remapped to
  anonymousUid int
  // GID that squashed identities are remapped to
  anonymousGid int
  // Whether unauthenticated access is allowed
  isAnonymousAccessAllowed bool
  // Authentication flavors accepted from matching clients
  //
  // Any of SYS, KRB5, KRB5I, or KRB5P. SYS is unauthenticated in the sense
  // that the client asserts its own UID and GID.
  allowedAuth []string
}

// Oracle Cloud Infrastructure (OCI) file system snapshot
//
// Point-in-time copy of a file system. `snapshotType` separates snapshots a
// user took from ones a snapshot policy or a replication created, and
// `expirationTime` is when a policy-created snapshot is due to be removed.
private oci.fileStorage.snapshot @defaults("name snapshotType state") {
  // Snapshot OCID
  id string
  // Snapshot name
  name string
  // Snapshot lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED)
  state string
  // File system this snapshot was taken from
  fileSystem() oci.fileStorage.fileSystem
  // How the snapshot was created
  //
  // USER for a manually taken snapshot, POLICY_BASED for one a snapshot
  // policy scheduled, and REPLICATION for one taken to support replication.
  snapshotType string
  // Time the snapshot represents
  snapshotTime time
  // Time the snapshot is due to be removed
  expirationTime time
  // Whether a file system has been cloned from this snapshot
  isCloneSource bool
  // Bytes held only by this snapshot and no other
  exclusiveBytes int
  // Snapshot creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) file system snapshot policy
//
// Schedule governing when snapshots of the file systems attached to it are
// taken and how long they are kept. The `schedules` carry the period and the
// retention duration, so this is where a retention requirement is proved or
// disproved. A policy in the INACTIVE state takes no snapshots at all while
// still being attached.
private oci.fileStorage.snapshotPolicy @defaults("name state availabilityDomain") {
  // Snapshot policy OCID
  id string
  // Snapshot policy display name
  name string
  // Compartment containing the snapshot policy
  compartment() oci.compartment
  // Availability domain the snapshot policy is in
  availabilityDomain string
  // Snapshot policy lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, INACTIVE, FAILED)
  state string
  // Prefix prepended to the names of snapshots this policy takes
  policyPrefix string
  // Schedules on which snapshots are taken
  schedules() []oci.fileStorage.snapshotPolicy.schedule
  // Snapshot policy creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Schedule on a file system snapshot policy
//
// One recurrence in a snapshot policy: how often a snapshot is taken and how
// long it is kept. `retentionDurationInSeconds` is the retention control, and
// it is absent when snapshots taken on this schedule are kept indefinitely.
private oci.fileStorage.snapshotPolicy.schedule @defaults("period retentionDurationInSeconds") {
  // How often the schedule runs
  //
  // One of HOURLY, DAILY, WEEKLY, MONTHLY, or YEARLY.
  period string
  // Time zone the schedule's clock fields are interpreted in
  //
  // UTC or REGIONAL_DATA_CENTER_TIME.
  timeZone string
  // Prefix prepended to the names of snapshots this schedule takes
  schedulePrefix string
  // Time the schedule starts taking snapshots
  timeScheduleStart time
  // Seconds a snapshot taken on this schedule is kept
  //
  // Null when snapshots on this schedule are kept indefinitely.
  retentionDurationInSeconds int
  // Hour of the day the schedule runs
  hourOfDay int
  // Day of the week the schedule runs
  //
  // One of MONDAY through SUNDAY.
  dayOfWeek string
  // Day of the month the schedule runs
  dayOfMonth int
  // Month the schedule runs
  //
  // One of JANUARY through DECEMBER.
  month string
}

// Oracle Cloud Infrastructure (OCI) file system replication
//
// Scheduled copy of a file system to a target file system, usually in another
// region. Replication moves the data out of the source region on the interval
// in `replicationInterval`, so this is the resource that answers where a file
// system's contents are allowed to land. `recoveryPointTime` is how current
// the target copy is.
private oci.fileStorage.replication @defaults("name state replicationInterval") {
  // Replication OCID
  id string
  // Replication display name
  name string
  // Compartment containing the replication
  compartment() oci.compartment
  // Availability domain the replication is in
  availabilityDomain string
  // Replication lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // File system being copied
  sourceFileSystem() oci.fileStorage.fileSystem
  // File system being copied to
  //
  // Null when the target is in a region this scan does not cover, which is
  // the common case for a cross-region replication.
  targetFileSystem() oci.fileStorage.fileSystem
  // Seconds between replication cycles
  replicationInterval int
  // Time the target copy is current as of
  recoveryPointTime time
  // Progress of the current replication cycle
  //
  // One of IDLE, CAPTURING, APPLYING, TRANSFERRING, SERVICE_ERROR,
  // USER_ERROR, or FAILED.
  deltaStatus() string
  // Detail behind the current lifecycle state
  lifecycleDetails string
  // Replication creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) outbound connector
//
// Directory server a mount target binds to when it maps UNIX identities
// through LDAP. The connector is credential-bearing: `bindDistinguishedName`
// is the account it binds as, `passwordSecret` holds that account's password,
// and `trustedCertificateSecret` holds the certificate the LDAP connection is
// validated against. `endpoints` are the hosts and ports it connects to.
private oci.fileStorage.outboundConnector @defaults("name connectorType state") {
  // Outbound connector OCID
  id string
  // Outbound connector display name
  name string
  // Compartment containing the outbound connector
  compartment() oci.compartment
  // Availability domain the outbound connector is in
  availabilityDomain string
  // Outbound connector lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED)
  state string
  // Kind of directory the connector reaches
  //
  // LDAPBIND is the only kind the service offers today.
  connectorType string
  // Distinguished name the connector binds to the directory as
  bindDistinguishedName string
  // Directory hosts and ports the connector reaches, each with `hostname` and `port`
  endpoints []dict
  // Vault secret holding the bind account's password
  passwordSecret() oci.vault.secret
  // Vault secret version holding the bind account's password
  passwordSecretVersion() int
  // Vault secret holding the certificate the LDAP connection is validated against
  //
  // Null when the connector does not validate the directory's certificate.
  trustedCertificateSecret() oci.vault.secret
  // Vault secret version holding the trusted certificate
  trustedCertificateSecretVersion() int
  // Outbound connector creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) file system quota rule
//
// Storage limit applied to a file system as a whole or to one user or group
// within it. `isHardQuota` separates a limit that blocks writes from one that
// only reports, and `usageInBytes` against `quotaLimitInGigabytes` is how far
// the principal is from that limit.
private oci.fileStorage.quotaRule @defaults("name principalType quotaLimitInGigabytes") {
  // Quota rule OCID
  id string
  // Quota rule display name
  name string
  // File system the quota rule applies to
  fileSystem() oci.fileStorage.fileSystem
  // Scope the quota rule applies to
  //
  // One of FILE_SYSTEM_LEVEL, DEFAULT_USER, DEFAULT_GROUP, INDIVIDUAL_USER,
  // or INDIVIDUAL_GROUP.
  principalType string
  // UID or GID the rule applies to, for the individual scopes
  principalId int
  // Whether exceeding the limit blocks writes rather than only being reported
  isHardQuota bool
  // Storage limit in gigabytes
  quotaLimitInGigabytes int
  // Storage currently consumed under this rule, in bytes
  usageInBytes int
  // Quota rule creation time
  created time
  // Last modification time
  timeUpdated time
}

// Oracle Cloud Infrastructure (OCI) Events
//
// Event-rule-based automation configured in the tenancy. Each rule in
// `rules` surfaces the matching `condition` (a JSON event filter), the
// `actions` to perform (function, notification topic, or stream
// targets), the rule's enabled state, and its lifecycle state. Audit
// these to see which events trigger automated responses and where
// those responses are routed.
oci.events {
  // Event rules
  rules() []oci.events.rule
}

// Oracle Cloud Infrastructure (OCI) event rule
//
// A single Events service rule, evaluated against emitted events. The
// `condition` holds the JSON filter that selects matching events,
// `actions` lists the automated responses to invoke, and `isEnabled`
// with `state` report whether the rule is active. Audit these to
// confirm which events trigger automation and that no rule routes
// events to unexpected functions, topics, or streams.
private oci.events.rule @defaults("name") {
  // Rule OCID
  id string
  // Rule display name
  name string
  // Rule description
  description string
  // Compartment containing the rule
  compartment() oci.compartment
  // Event matching condition (JSON)
  condition string
  // Whether the rule is enabled
  isEnabled bool
  // Rule lifecycle state (e.g., CREATING, ACTIVE, INACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Actions to perform when the rule matches an event
  //
  // Each entry has `id`, `isEnabled`, `state` (the action's lifecycle
  // state), a `description`, and `actionType` set to ONS (notification
  // topic), OSS (stream), or FAAS (function). Depending on that type,
  // the target OCID is in `topicId`, `streamId`, or `functionId`.
  actions() []dict
  // Rule creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard
//
// Tenancy-wide threat-detection and security-monitoring posture. The
// `status` and `reportingRegion` fields report whether Cloud Guard is
// enabled and which region collects findings, and `selfManageResources`
// reports whether Cloud Guard's resources are customer-managed. The
// `targets` field lists the compartments and resources Cloud Guard
// monitors, `detectorRecipes` the curated detector rule sets,
// `securityZones` and `securityZoneRecipes` the security-zone
// enforcement scopes, and `securityPolicies` the individual policies
// that compose those recipes. `problems` carries the findings Cloud
// Guard has actually raised, each with its risk level, affected
// resource, and triage state.
oci.cloudGuard {
  // Whether Cloud Guard is enabled
  status() bool
  // Region where Cloud Guard reports findings
  reportingRegion() string
  // Whether Cloud Guard resources are self-managed
  selfManageResources() bool
  // Cloud Guard targets (monitored compartments/resources)
  targets() []oci.cloudGuard.target
  // Problems (security findings) Cloud Guard has raised across the tenancy
  problems() []oci.cloudGuard.problem
  // Cloud Guard detector recipes
  detectorRecipes() []oci.cloudGuard.detectorRecipe
  // Cloud Guard security zones
  securityZones() []oci.cloudGuard.securityZone
  // Cloud Guard security zone recipes
  securityZoneRecipes() []oci.cloudGuard.securityZoneRecipe
  // Cloud Guard security policies (used in security zone recipes)
  securityPolicies() []oci.cloudGuard.securityPolicy
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard target
private oci.cloudGuard.target @defaults("name") {
  // Target OCID
  id string
  // Target display name
  name string
  // Compartment containing the target
  compartment() oci.compartment
  // OCID of the resource being monitored
  targetResourceId string
  // Type of resource being monitored (e.g., COMPARTMENT, ERPCLOUD, HCMCLOUD, SECURITY_ZONE)
  targetResourceType string
  // Target lifecycle state (e.g., CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Number of detector recipes attached to the target
  recipeCount int
  // Target creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard detector recipe
private oci.cloudGuard.detectorRecipe @defaults("name") {
  // Detector recipe OCID
  id string
  // Detector recipe display name
  name string
  // Detector recipe description
  description string
  // Compartment containing the detector recipe
  compartment() oci.compartment
  // Owner of the detector recipe (CUSTOMER or ORACLE)
  owner string
  // Type of detector
  //
  // For example: IAAS_ACTIVITY_DETECTOR, IAAS_CONFIGURATION_DETECTOR, IAAS_THREAT_DETECTOR, IAAS_LOG_INSIGHT_DETECTOR, IAAS_INSTANCE_SECURITY_DETECTOR, IAAS_CONTAINER_SECURITY_DETECTOR.
  detectorType string
  // Detector recipe lifecycle state (e.g., CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detector recipe creation time
  created time
  // Detector rules in the recipe, each with its enabled state and risk level
  rules() []oci.cloudGuard.detectorRule
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard detector rule
//
// Single detection rule inside a detector recipe, and the switch that
// decides whether Cloud Guard looks for that condition at all. `isEnabled`
// reports whether the rule is active in the recipe and `riskLevel` the
// severity it assigns to matches, so a rule that is present but disabled,
// or downgraded to a lower risk level, is visible rather than silently
// absent from findings. `configurations` carries the rule's tunable
// parameters, `labels` its classification tags, and `problemThreshold` the
// count that must be reached before a problem is raised. `recommendation`
// is the remediation guidance Cloud Guard shows for matches.
private oci.cloudGuard.detectorRule @defaults("name isEnabled riskLevel") {
  // Detector rule identifier
  id string
  // Detector this rule belongs to
  //
  // For example: IAAS_ACTIVITY_DETECTOR, IAAS_CONFIGURATION_DETECTOR,
  // IAAS_THREAT_DETECTOR, IAAS_LOG_INSIGHT_DETECTOR,
  // IAAS_INSTANCE_SECURITY_DETECTOR, or IAAS_CONTAINER_SECURITY_DETECTOR.
  detector string
  // Detector rule display name
  name string
  // Detector rule description
  description string
  // Remediation guidance shown for problems this rule raises
  recommendation string
  // OCI service the rule inspects
  serviceType string
  // Resource type the rule inspects
  resourceType string
  // Whether the rule is enabled in the recipe
  isEnabled bool
  // Risk level assigned to problems this rule raises (CRITICAL, HIGH, MEDIUM, LOW, MINOR)
  riskLevel string
  // Number of matches required before a problem is raised
  problemThreshold int
  // Classification labels attached to the rule
  labels []string
  // Tunable parameters for the rule
  //
  // Each entry has `configKey`, `name`, `value`, `dataType`, and for
  // multi-valued settings a `values` list whose entries carry `name`,
  // `value`, and `listType`.
  configurations []dict
  // Whether the rule's configuration may be changed
  isConfigurationAllowed bool
  // Whether the rule can be cloned into a customer-managed recipe
  isCloneable bool
  // Managed list types the rule accepts (e.g., CIDR_BLOCK, USERS, GROUPS, RESOURCE_OCID)
  managedListTypes []string
  // Detector rule lifecycle state (e.g., CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detector rule creation time
  created time
  // Detector rule last-update time
  updated time
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard problem
//
// Security finding Cloud Guard has raised against a monitored resource.
// `riskLevel` and `riskScore` grade the finding, `resourceId`,
// `resourceName`, and `resourceType` identify what it was raised against,
// and `detectorRuleId` names the rule that fired. `lifecycleDetail` is the
// triage state (OPEN, RESOLVED, DISMISSED, or DELETED), which distinguishes
// a finding still awaiting action from one an operator has already closed
// or waived. `firstDetected` and `lastDetected` bound how long the
// condition has persisted, and `labels` carries the rule's classification
// tags. Problems are reported from the Cloud Guard reporting region and
// cover every compartment in the tenancy subtree.
private oci.cloudGuard.problem @defaults("resourceName riskLevel lifecycleDetail") {
  // Problem OCID
  id string
  // Compartment containing the resource the problem was raised against
  compartment() oci.compartment
  // Risk level assigned to the problem (CRITICAL, HIGH, MEDIUM, LOW, MINOR)
  riskLevel string
  // Numeric risk score for the problem
  riskScore float
  // Identifier of the detector rule that raised the problem
  detectorRuleId string
  // Detector that raised the problem
  //
  // For example: IAAS_ACTIVITY_DETECTOR, IAAS_CONFIGURATION_DETECTOR,
  // IAAS_THREAT_DETECTOR, IAAS_LOG_INSIGHT_DETECTOR,
  // IAAS_INSTANCE_SECURITY_DETECTOR, or IAAS_CONTAINER_SECURITY_DETECTOR.
  detector string
  // OCID of the resource the problem was raised against
  resourceId string
  // Name of the resource the problem was raised against
  resourceName string
  // Type of the resource the problem was raised against
  resourceType string
  // Cloud Guard target that reported the problem
  target() oci.cloudGuard.target
  // Classification labels attached to the problem
  labels []string
  // Problem record lifecycle state (ACTIVE, INACTIVE)
  state string
  // Triage state of the problem (OPEN, RESOLVED, DISMISSED, DELETED)
  lifecycleDetail string
  // Region the problem was detected in
  region string
  // Every region the problem applies to, for problems spanning more than one
  regions []string
  // When the problem was first detected
  firstDetected time
  // When the problem was most recently detected
  lastDetected time
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard security zone
private oci.cloudGuard.securityZone @defaults("name") {
  // Security zone OCID
  id string
  // Security zone display name
  name string
  // Security zone description
  description string
  // Compartment containing the security zone
  compartment() oci.compartment
  // Security zone recipe applied to the zone
  securityZoneRecipe() oci.cloudGuard.securityZoneRecipe
  // Whether the compartment inherits a parent security zone after the zone is deleted
  isInheritanceAfterDeleteEnabled bool
  // Security zone lifecycle state (e.g., CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Security zone creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard security zone recipe
private oci.cloudGuard.securityZoneRecipe @defaults("name") {
  // Security zone recipe OCID
  id string
  // Security zone recipe display name
  name string
  // Security zone recipe description
  description string
  // Compartment containing the security zone recipe
  compartment() oci.compartment
  // Owner of the recipe (CUSTOMER or ORACLE)
  owner string
  // Security policies included in the recipe
  securityPolicies() []oci.cloudGuard.securityPolicy
  // Recipe lifecycle state (e.g., CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Recipe creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Cloud Guard security policy (a requirement enforced inside a security zone)
private oci.cloudGuard.securityPolicy @defaults("name category") {
  // Security policy OCID
  id string
  // Security policy display name
  name string
  // Shorter version of the policy name
  friendlyName string
  // Security policy description
  description string
  // Compartment containing the security policy
  compartment() oci.compartment
  // Owner of the policy (CUSTOMER or ORACLE)
  owner string
  // Policy category (e.g., the resource type or feature the policy applies to)
  category string
  // OCI services protected by the policy
  services []string
  // Policy lifecycle state (e.g., CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Policy creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Notifications
//
// OCI Notifications (ONS) pub/sub messaging service used for
// broadcasting alerts and delivering events to subscribers. The
// `topics` collection enumerates every notification topic, and
// each topic exposes its lifecycle state and the `subscriptions`
// bound to it (Email, HTTPS, Slack, PagerDuty, function, and other
// delivery protocols). Auditing topics and their subscriptions
// reveals where operational and security alerts are routed.
oci.ons {
  // Notification topics
  topics() []oci.ons.topic
}

// Oracle Cloud Infrastructure (OCI) notification topic
private oci.ons.topic @defaults("name") {
  // Topic OCID
  id string
  // Topic name
  name string
  // Topic description
  description string
  // Compartment containing the topic
  compartment() oci.compartment
  // Topic lifecycle state (e.g., ACTIVE, DELETING, CREATING)
  state string
  // Topic creation time
  created time
  // Topic subscriptions
  subscriptions() []oci.ons.subscription
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) notification subscription
private oci.ons.subscription @defaults("id") {
  // Subscription OCID
  id string
  // Topic the subscription belongs to
  topic() oci.ons.topic
  // Delivery protocol (EMAIL, HTTPS, SLACK, etc.)
  protocol string
  // Delivery endpoint
  endpoint string
  // Subscription lifecycle state (e.g., PENDING, ACTIVE, DELETED)
  state string
  // Subscription creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Audit
//
// Tenancy-wide audit log configuration and the recorded events
// themselves. The `retentionPeriodDays` field reports how many days
// audit events are retained before being purged, letting you verify
// that the retention window meets your compliance requirements.
// `events` returns the control-plane API calls recorded across the
// tenancy in the last 24 hours, each carrying the calling principal,
// source IP, and outcome.
oci.audit {
  // Audit log retention period in days
  retentionPeriodDays() int
  // Audit events recorded across the tenancy in the last 24 hours
  //
  // Every control-plane API call OCI recorded, across every compartment
  // and every subscribed region. The window is fixed at the last 24
  // hours: the Audit API requires an explicit time range, and an
  // unbounded one would pull the full retention period on every query.
  events() []oci.audit.event
}

// Oracle Cloud Infrastructure (OCI) audit event
//
// Single control-plane API call recorded by the Audit service.
// `eventName` is the operation invoked (for example
// `CreateUser`, `DeletePolicy`, or `Login`) and `eventTime` when it
// happened. The calling principal is described by `principalName`,
// `principalId`, and `authType` (`natv` for a native OCI user,
// `federated` for one signing in through an identity provider, or
// `instance` for an instance principal), with `ipAddress` and
// `userAgent` recording where the call came from and
// `consoleSessionId` linking console actions to a session.
// `responseStatus` is the HTTP status the call returned, which
// separates a successful change from a rejected attempt, and
// `resourceName` and `resourceId` identify what was acted on. Together
// these support detection-style checks such as finding privilege
// changes, console logins from unexpected addresses, or repeated
// authorization failures.
private oci.audit.event @defaults("eventName principalName eventTime responseStatus") {
  // Audit event OCID
  id string
  // Operation that was invoked (e.g., CreateUser, DeletePolicy, Login)
  eventName string
  // Type of the event, in reverse-DNS form (e.g., com.oraclecloud.identityControlPlane.CreateUser)
  eventType string
  // When the event occurred
  eventTime time
  // Service that emitted the event (e.g., identityControlPlane)
  source string
  // Identifier grouping the request and response halves of one operation
  eventGroupingId string
  // Compartment the event was recorded in
  compartment() oci.compartment
  // Name of the compartment the event was recorded in
  compartmentName string
  // Name of the resource the operation acted on
  resourceName string
  // OCID of the resource the operation acted on
  resourceId string
  // Availability domain the event originated from
  availabilityDomain string
  // Display name of the principal that made the call
  principalName string
  // OCID of the principal that made the call
  //
  // Kept alongside `principal` because the two do not always agree: an
  // instance principal, a service principal, or a user deleted since the
  // call was recorded all leave an OCID here that resolves to no user.
  principalId string
  // IAM user that made the call
  //
  // Null when the caller was not a user, which is the case for instance
  // and service principals, or when the user has since been deleted.
  // `principalId` records the raw OCID either way.
  principal() oci.identity.user
  // How the caller authenticated (e.g., natv, federated, instance)
  authType string
  // Name of the user on whose behalf a delegated call was made
  callerName string
  // OCID of the user on whose behalf a delegated call was made
  callerId string
  // IAM user on whose behalf a delegated call was made
  //
  // Set for calls a service made on a user's behalf, and null for direct
  // calls or when the user has since been deleted.
  caller() oci.identity.user
  // Source IP address the call originated from
  ipAddress string
  // User agent string of the calling client
  userAgent string
  // Console session the call belongs to, for actions taken in the web console
  consoleSessionId string
  // HTTP method of the request (e.g., GET, POST, DELETE)
  requestAction string
  // Full path of the API request
  //
  // For example `/20160918/instances/ocid1.instance.oc1.phx.<unique_ID>`.
  requestPath string
  // Request identifier assigned by the service
  requestId string
  // Query and body parameters supplied with the request
  //
  // Keyed by parameter name, each mapping to the list of values supplied
  // for it.
  requestParameters map[string][]string
  // HTTP status code the operation returned
  //
  // A 2xx status means the change was applied; 401 and 403 record a
  // rejected attempt, which is what distinguishes a successful privilege
  // change from a failed one.
  responseStatus string
  // Message the service returned with the response
  responseMessage string
  // When the response was issued
  responseTime time
  // State transition the operation caused
  //
  // Has `previous` and `current` maps for operations that changed a
  // resource's lifecycle state, and is empty for read-only calls.
  stateChange dict
  // Service-specific details recorded alongside the event
  additionalDetails dict
}

// Oracle Cloud Infrastructure (OCI) Connector Hub
//
// Connectors that move data between OCI services, and the mechanism by
// which audit events and logs leave the tenancy for an external SIEM or
// long-term storage. Each connector reads from one source and writes to
// one target, so enumerating `connectors` answers whether audit logging
// is exported at all, where it goes, and whether the pipeline is
// running. A tenancy with audit retention configured but no connector
// forwarding it keeps its logs only inside OCI, and only for the
// retention window.
oci.serviceConnectorHub {
  // Connectors defined across the tenancy
  connectors() []oci.serviceConnectorHub.connector
}

// Oracle Cloud Infrastructure (OCI) connector
//
// Single data-movement pipeline, keyed by its OCID. `sourceKind` and
// `targetKind` name the two ends: a source of `logging` (which includes
// the tenancy audit log), `monitoring`, `streaming`, or `plugin`, and a
// target of `objectStorage`, `streaming`, `functions`,
// `loggingAnalytics`, `monitoring`, or `notifications`. The `source` and
// `target` fields carry the full configuration of each end, including
// the bucket, stream, or function being written to and any private
// endpoint the connector routes through, and `tasks` holds the optional
// filtering or transformation applied in between. `state` reports
// whether the pipeline is ACTIVE and actually moving data, since an
// INACTIVE or FAILED connector exports nothing while still appearing to
// be configured.
private oci.serviceConnectorHub.connector @defaults("name sourceKind targetKind state") {
  // Connector OCID
  id string
  // Connector display name
  name string
  // Connector description
  description string
  // Compartment containing the connector
  compartment() oci.compartment
  // Connector lifecycle state (CREATING, UPDATING, ACTIVE, INACTIVE, NEEDS_ATTENTION, DELETING, DELETED, FAILED)
  state string
  // Detail explaining the current lifecycle state, for FAILED connectors
  stateDetails string
  // Region the connector runs in
  region string
  // Kind of source the connector reads from (logging, monitoring, streaming, plugin)
  sourceKind() string
  // Configuration of the source the connector reads from
  //
  // Shape varies by `sourceKind`. A `logging` source has `logSources`,
  // each with `compartmentId` and optionally `logGroupId` and `logId`,
  // where the tenancy audit log is named by the reserved value `_Audit`. A
  // `monitoring` source has `namespaceDetails`, a `streaming` source has
  // `streamId` and `cursor`, and a `plugin` source has `pluginName` and
  // `configMap`.
  source() dict
  // Kind of target the connector writes to (objectStorage, streaming, functions, loggingAnalytics, monitoring, notifications)
  targetKind() string
  // Object Storage bucket the connector writes to
  //
  // Set when `targetKind` is objectStorage, and null otherwise. Resolving
  // it makes the receiving bucket's own posture, such as its public
  // access type and retention rules, reachable from the connector.
  targetBucket() oci.objectStorage.bucket
  // Notification topic the connector writes to
  //
  // Set when `targetKind` is notifications, and null otherwise.
  targetTopic() oci.ons.topic
  // Log groups the connector reads from
  //
  // The log groups named by a `logging` source. Empty for every other
  // source kind. The tenancy audit log is not a customer log group and so
  // does not appear here; `exportsAuditLog` reports it instead.
  sourceLogGroups() []oci.logging.logGroup
  // Whether the connector exports the tenancy audit log
  //
  // True when a `logging` source names the reserved `_Audit` log group,
  // which is the audit stream itself rather than a log group in a
  // compartment. This is the check for whether control-plane audit events
  // leave OCI for archival or a SIEM.
  exportsAuditLog() bool
  // Configuration of the target the connector writes to
  //
  // Shape varies by `targetKind`. An `objectStorage` target has
  // `bucketName`, `namespace`, `objectNamePrefix`, and batch rollover
  // settings; `streaming` has `streamId`; `functions` has `functionId`;
  // `notifications` has `topicId`; `loggingAnalytics` has `logGroupId`;
  // and `monitoring` has `namespace` and `metric`.
  target() dict
  // Filtering and transformation applied between source and target
  //
  // Each entry has a `kind` of `logRule` (a filter expression in
  // `condition`) or `function` (a transform in `functionId`), plus
  // `batchSizeInKbs` and `batchTimeInSec` where the task batches.
  tasks() []dict
  // Connector creation time
  created time
  // Connector last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Bastion
//
// Bastion instances in the tenancy, managed jump hosts that broker SSH
// access into private subnets without exposing them to the public
// internet. Iterate `bastions` for every bastion deployment, each
// exposing its target VCN and subnet, DNS proxy status, bastion type,
// and lifecycle state.
oci.bastion {
  // Bastion instances
  bastions() []oci.bastion.instance
}

// Oracle Cloud Infrastructure (OCI) bastion instance
private oci.bastion.instance @defaults("name") {
  // Bastion OCID
  id string
  // Bastion display name
  name string
  // Compartment containing the bastion
  compartment() oci.compartment
  // Bastion type (e.g., standard)
  bastionType string
  // Target VCN
  targetVcn() oci.network.vcn
  // Target subnet
  targetSubnet() oci.network.subnet
  // CIDR blocks allowed to connect to the bastion
  //
  // The source addresses permitted to open a session. A bastion allowing
  // 0.0.0.0/0 accepts session attempts from anywhere on the internet. Empty
  // when no allow list is configured.
  clientCidrBlockAllowList() []string
  // Maximum session lifetime in seconds
  //
  // The longest a session may stay open before it must be recreated. Null
  // when the bastion reports no limit.
  maxSessionTtlInSeconds() int
  // Maximum number of concurrent sessions
  maxSessionsAllowed() int
  // Fixed private IP addresses the bastion connects from
  //
  // The jump-host addresses that appear as the source of bastion traffic, for
  // writing narrower target security rules.
  staticJumpHostIpAddresses() []string
  // Private endpoint IP address of the bastion in the target subnet
  privateEndpointIpAddress() string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // DNS proxy status (DISABLED, ENABLED)
  dnsProxyStatus string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Monitoring
//
// Alarm definitions configured in the tenancy's Monitoring service.
// Each alarm evaluates a Monitoring Query Language expression against a
// metric namespace and publishes notifications to Oracle Notifications
// Service (ONS) topics when its condition and severity threshold are
// met. Auditing the alarms confirms that critical infrastructure and
// security signals are actually monitored and routed to a live
// notification destination.
oci.monitoring {
  // Monitoring alarm definitions
  alarms() []oci.monitoring.alarm
}

// Oracle Cloud Infrastructure (OCI) monitoring alarm
//
// Single alarm definition in the Monitoring service. It evaluates a
// Monitoring Query Language expression over a metric namespace and
// raises a notification when the condition is met. Each alarm records
// its severity, whether it is enabled, its lifecycle state, and the
// Oracle Notifications Service topics that receive its messages, so an
// audit can confirm that important metrics are watched and that the
// resulting alerts reach a valid destination.
private oci.monitoring.alarm @defaults("name") {
  // Alarm OCID
  id string
  // Alarm display name
  name string
  // Compartment containing the alarm
  compartment() oci.compartment
  // Metric compartment OCID being monitored
  metricCompartmentId string
  // Metric namespace (e.g., oci_computeagent, oci_vcn)
  namespace string
  // Monitoring Query Language (MQL) expression
  query string
  // Alarm severity (CRITICAL, ERROR, WARNING, INFO)
  severity string
  // Notification topics the alarm publishes to
  topics() []oci.ons.topic
  // Whether the alarm is enabled
  isEnabled bool
  // Lifecycle state (e.g., ACTIVE, DELETING, DELETED)
  state string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Vault
//
// Secrets stored across the tenancy's vaults, exposed through the
// secrets collection. Auditing these surfaces rotation posture,
// upcoming or overdue rotations, and secrets approaching expiry, so
// you can flag credentials that are stale, unrotated, or
// auto-generated outside of policy.
oci.vault {
  // Secrets stored in vaults
  secrets() []oci.vault.secret
}

// Oracle Cloud Infrastructure (OCI) vault secret
//
// A single secret managed in a KMS vault, holding a protected value
// such as a password, API key, or certificate. Auditing a secret
// reveals its rotation status and schedule, whether rotation is
// automated, and when the current version expires, so you can catch
// credentials that are stale, unrotated, or nearing expiry.
private oci.vault.secret @defaults("name") {
  // Secret OCID
  id string
  // Secret name
  name string
  // Compartment containing the secret
  compartment() oci.compartment
  // Vault containing this secret
  kmsVault() oci.kms.vault
  // KMS key used for encryption
  kmsKey() oci.kms.key
  // Secret description
  description string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, SCHEDULING_DELETION, PENDING_DELETION, CANCELLING_DELETION, FAILED)
  state string
  // Rotation status (NOT_ENABLED, SCHEDULED, IN_PROGRESS, CANCELLING)
  rotationStatus string
  // Last rotation time
  lastRotationTime time
  // Next scheduled rotation time
  nextRotationTime time
  // Whether auto generation is enabled
  isAutoGenerationEnabled bool
  // Rotation interval in ISO 8601 duration format (e.g., P30D for 30 days)
  rotationInterval() string
  // Whether scheduled rotation is enabled
  isScheduledRotationEnabled() bool
  // Expiry time of the current secret version
  timeOfCurrentVersionExpiry() time
  // Secret versions
  secretVersions() []oci.vault.secretVersion
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) vault secret version
private oci.vault.secretVersion @defaults("versionNumber") {
  // Secret OCID this version belongs to
  secretId string
  // Secret version number
  versionNumber int
  // Version name
  name string
  // Content type (BASE64)
  contentType string
  // Version lifecycle stages (CURRENT, PENDING, LATEST, PREVIOUS, DEPRECATED)
  stages []string
  // Whether the content was auto-generated
  isContentAutoGenerated bool
  // Creation time
  created time
  // Version expiry time
  timeOfExpiry time
  // Scheduled deletion time
  timeOfDeletion time
}

// Oracle Cloud Infrastructure (OCI) Load Balancer
//
// OCI Load Balancer service for the tenancy, exposing the layer 7 and
// layer 4 load balancers through `loadBalancers`. Each load balancer
// surfaces its shape (e.g., flexible, 100Mbps, 400Mbps), private vs.
// public placement, delete protection, listeners (with their TLS
// protocols, cipher suites, and peer-certificate verification), backend
// sets and policies, and lifecycle state, plus an internet-exposure
// assessment gated on the attached network security groups.
oci.loadBalancer {
  // Load balancers
  loadBalancers() []oci.loadBalancer.loadBalancer
}

// Oracle Cloud Infrastructure (OCI) load balancer
private oci.loadBalancer.loadBalancer @defaults("name") {
  // Load balancer OCID
  id string
  // Display name
  name string
  // Compartment containing the load balancer
  compartment() oci.compartment
  // Shape name (e.g., flexible, 100Mbps, 400Mbps)
  shape string
  // Guaranteed pre-provisioned bandwidth in Mbps
  //
  // Ingress plus egress. Set only on a flexible shape; null on a fixed
  // shape, where the shape name itself carries the bandwidth.
  minimumBandwidthInMbps int
  // Maximum burst bandwidth in Mbps
  //
  // Ingress plus egress, not guaranteed. Set only on a flexible shape;
  // null on a fixed shape.
  maximumBandwidthInMbps int
  // Whether the load balancer is private (no public IP)
  isPrivate bool
  // IP address family the load balancer serves
  //
  // One of IPV4 or IPV6. An IPV6 load balancer reaches the internet over a
  // path that IPv4-only egress rules do not describe.
  ipMode string
  // Whether the load balancer stamps a request identifier on forwarded requests
  isRequestIdEnabled bool
  // HTTP header carrying the generated request identifier
  //
  // Defaults to X-Request-Id when isRequestIdEnabled is set and no header
  // name was chosen. Empty when request identifiers are disabled.
  requestIdHeader string
  // Virtual hostnames defined on the load balancer, keyed by hostname resource name
  //
  // Listeners reference these by name to route by Host header. Each value is
  // the hostname served, for example `app.example.com`.
  hostnames map[string]string
  // Assigned IP addresses
  //
  // Each entry is a dict with an `ipAddress`, an `isPublic` flag indicating
  // internet reachability, and a `reservedIpId` (the oci.network.publicIp
  // OCID) when the address is backed by a reserved public IP.
  ipAddresses []dict
  // Whether delete protection is enabled
  isDeleteProtectionEnabled bool
  // Subnets the load balancer is attached to
  subnets() []oci.network.subnet
  // Lifecycle state (e.g., CREATING, ACTIVE, FAILED, DELETING, DELETED)
  state string
  // Security attributes applied to the load balancer, keyed by namespace
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to this load balancer's values. Zero Trust
  // Packet Routing decides what the load balancer may talk to from these
  // rather than from its address.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Load balancer listeners
  listeners() []oci.loadBalancer.listener
  // Network Security Groups attached to the load balancer
  securityGroups() []oci.network.networkSecurityGroup
  // Internet-exposure breakdown (a public IP and a listener, gated on the network security group ingress rules)
  exposure() oci.network.exposure
  // Backend sets
  backendSets() []oci.loadBalancer.backendSet
  // Custom SSL cipher suites defined on the load balancer
  //
  // Only user-defined suites appear here. A listener or backend set that
  // names one of Oracle's predefined suites reports that name in
  // sslCipherSuiteName without a matching entry in this list.
  sslCipherSuites() []oci.loadBalancer.sslCipherSuite
  // Certificate bundles uploaded directly to the load balancer
  //
  // The inline alternative to the OCI Certificates service. A listener or
  // backend set selects one by name through certificateName, and the
  // bundle's parsed certificates carry the validity window an expiry audit
  // needs.
  certificateBundles() []oci.loadBalancer.certificateBundle
  // Rule sets defined on the load balancer
  //
  // Named collections of request and response rules that listeners attach
  // by name, covering header manipulation, HTTP method allow lists,
  // redirects, and source IP restrictions.
  ruleSets() []oci.loadBalancer.ruleSet
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) load balancer listener
private oci.loadBalancer.listener @defaults("name") {
  // Listener name
  name string
  // Listener port
  port int
  // Protocol (HTTP, HTTPS, HTTP2, TCP, GRPC)
  protocol string
  // Default backend set name
  defaultBackendSetName string
  // SSL/TLS protocols enabled (e.g., TLSv1.2, TLSv1.3)
  sslProtocols []string
  // SSL cipher suite name
  //
  // Deprecated in favor of sslCipherSuite, which resolves the same name to
  // the suite itself. Kept because it also carries the OCI-predefined suite
  // names, which are not part of the load balancer's own collection and so
  // do not resolve.
  sslCipherSuiteName @maturity("deprecated") string
  // Cipher suite the listener negotiates with
  //
  // Resolves the load balancer's named cipher suite, whose ciphers list is
  // what a weak-algorithm audit reads. Null when the listener terminates no
  // TLS, and when it names an OCI-predefined suite rather than a custom one
  // defined on this load balancer; sslCipherSuiteName carries the name in
  // that case.
  sslCipherSuite() oci.loadBalancer.sslCipherSuite
  // Whether peer certificate verification is enabled
  sslVerifyPeerCertificate bool
  // Maximum depth for peer certificate chain verification
  //
  // Only meaningful when sslVerifyPeerCertificate is enabled.
  sslVerifyDepth int
  // Whether the listener prefers its own cipher order over the client's
  //
  // One of ENABLED or DISABLED. Server order preference stops a client from
  // steering the negotiation toward the weakest mutually supported cipher.
  // Empty when the listener terminates no TLS.
  sslServerOrderPreference string
  // Whether TLS session resumption is enabled
  //
  // Session resumption reuses the cryptographic parameters of a previous TLS
  // session to skip a full handshake. It improves performance but lowers
  // security, so audits that require forward secrecy expect it disabled.
  hasSessionResumption bool
  // Name of the inline certificate bundle configured on the listener
  //
  // Deprecated in favor of certificateBundle, which resolves the same name to
  // the bundle itself.
  certificateName @maturity("deprecated") string
  // Inline certificate bundle the listener serves
  //
  // Resolves the bundle managed directly on the load balancer, carrying the
  // leaf certificate and its CA chain. Null when the listener uses OCI
  // Certificates service certificates, exposed through certificates,
  // instead.
  certificateBundle() oci.loadBalancer.certificateBundle
  // Certificates served by the listener from the OCI Certificates service
  //
  // The managed certificates exposing the validity window, key algorithm, and
  // signature algorithm needed for expiry and weak-algorithm audits. Empty
  // when the listener serves an inline certificate bundle (certificateName).
  certificates() []oci.certificates.certificate
  // Certificate authorities the listener trusts to verify peer certificates
  //
  // The OCI Certificates service CAs trusted when sslVerifyPeerCertificate is
  // enabled. Trusted CA bundles are exposed separately as trustedCaBundles.
  trustedCertificateAuthorities() []oci.certificates.certificateAuthority
  // CA bundles the listener trusts to verify peer certificates
  //
  // The OCI Certificates service CA bundles trusted when
  // sslVerifyPeerCertificate is enabled.
  trustedCaBundles() []oci.certificates.caBundle
  // Idle timeout in seconds between successive client and backend operations
  //
  // A send does not reset the receive timer and a receive does not reset the
  // send timer. Null when the listener leaves the service default in place.
  idleTimeoutInSeconds int
  // Backend TCP Proxy Protocol version negotiated toward the backends
  //
  // Null when the listener does not use the proxy protocol, in which case
  // backends see the load balancer address rather than the client address.
  backendTcpProxyProtocolVersion int
  // Names of the virtual hostnames the listener answers for
  //
  // Each entry selects an entry of the load balancer's hostnames map. Empty
  // when the listener answers for every hostname.
  hostnameNames []string
  // Name of the path route set that maps request paths to backend sets
  //
  // Empty when every request goes to defaultBackendSetName.
  pathRouteSetName string
  // Name of the routing policy applied to the listener
  //
  // Empty when the listener applies no routing policy.
  routingPolicyName string
  // Rule sets attached to the listener
  //
  // Resolves the load balancer's rule sets, which carry the header
  // manipulation, HTTP method restriction, and source IP rules applied to
  // traffic through this listener. Empty when no rule set is attached.
  ruleSets() []oci.loadBalancer.ruleSet
}

// Custom SSL cipher suite on an Oracle Cloud Infrastructure (OCI) load balancer
//
// A user-defined named set of ciphers that listeners and backend sets select
// through sslCipherSuiteName. The ciphers list is what a weak-algorithm audit
// reads, since a suite naming CBC, RC4, 3DES, or MD5 ciphers weakens every
// listener that references it.
private oci.loadBalancer.sslCipherSuite @defaults("name") {
  // Cipher suite name
  name string
  // Ciphers the suite offers, in the order the load balancer presents them
  ciphers []string
}

// Certificate bundle on an Oracle Cloud Infrastructure (OCI) load balancer
//
// A certificate uploaded directly to the load balancer rather than managed by
// the OCI Certificates service. Listeners and backend sets select one by name
// through certificateName. The bundle carries the leaf certificate and the
// issuing chain in PEM form, and certificates parses them into the validity
// window, key algorithm, and signature algorithm an expiry or weak-algorithm
// audit needs. The private key is never returned by the API.
private oci.loadBalancer.certificateBundle @defaults("name") {
  // Certificate bundle name
  name string
  // Leaf certificate in PEM format
  publicCertificate string
  // Issuing certificate authority chain in PEM format
  //
  // Empty when the bundle was uploaded without its chain.
  caCertificate string
  // Certificates in the bundle, parsed
  //
  // The leaf followed by the chain, exposing subject, issuer, validity
  // window, and signature algorithm. A certificate that fails to parse is
  // skipped rather than blinding the whole bundle.
  certificates() []network.certificate
}

// Rule set on an Oracle Cloud Infrastructure (OCI) load balancer
//
// A named collection of rules that listeners attach through ruleSetNames.
// Rules act on requests and responses as they pass through the load balancer.
private oci.loadBalancer.ruleSet @defaults("name") {
  // Rule set name
  name string
  // Rules composing the set
  //
  // Each entry carries an `action` naming the rule kind, for example
  // ADD_HTTP_REQUEST_HEADER, REMOVE_HTTP_RESPONSE_HEADER,
  // CONTROL_ACCESS_USING_HTTP_METHODS, REDIRECT, ALLOW, or
  // HTTP_HEADER. The remaining keys vary by action: header rules carry
  // `header` and `value`, method control carries `allowedMethods` and
  // `statusCode`, redirects carry `conditions` and `redirectUri`, and ALLOW
  // rules carry `conditions` describing the permitted sources.
  rules []dict
}

// Oracle Cloud Infrastructure (OCI) load balancer backend set
private oci.loadBalancer.backendSet @defaults("name") {
  // Backend set name
  name string
  // Load balancing policy (ROUND_ROBIN, LEAST_CONNECTIONS, IP_HASH)
  policy string
  // Health checker configuration
  //
  // A dict describing the backend health probe. Keys: `protocol` (the
  // probe protocol, e.g., HTTP, HTTPS, or TCP), `port`, `urlPath`,
  // `returnCode` (expected status code), `responseBodyRegex`, `retries`,
  // `timeoutInMillis`, `intervalInMillis`, and `isForcePlainText`.
  healthChecker dict
  // Number of backends
  backendCount int
  // Backend servers in the set
  backends() []oci.loadBalancer.backend
  // Maximum simultaneous connections the load balancer opens to each backend
  //
  // Null when the backend set sets no limit, in which case the number of
  // simultaneous connections is unbounded.
  backendMaxConnections int
  // TLS protocols offered on connections to the backends
  //
  // Empty when the backend set carries no SSL configuration, which means
  // traffic between the load balancer and its backends is not encrypted.
  sslProtocols []string
  // Cipher suite used on connections to the backends
  //
  // Resolves the load balancer's named cipher suite. Null when the backend
  // set carries no SSL configuration, and when it uses an OCI-predefined
  // suite rather than a custom one defined on this load balancer.
  sslCipherSuite() oci.loadBalancer.sslCipherSuite
  // Whether the load balancer verifies the backend server certificates
  //
  // False when the backend set carries no SSL configuration, and also when
  // it terminates TLS without checking who is on the other end.
  sslVerifyPeerCertificate bool
  // Maximum depth for backend certificate chain verification
  //
  // Only meaningful when sslVerifyPeerCertificate is enabled.
  sslVerifyDepth int
  // Inline certificate bundle presented to the backends
  //
  // Resolves the bundle managed directly on the load balancer. Null when the
  // backend set uses OCI Certificates service certificates, exposed through
  // certificates, or carries no SSL configuration at all.
  certificateBundle() oci.loadBalancer.certificateBundle
  // Certificates presented to the backends from the OCI Certificates service
  certificates() []oci.certificates.certificate
  // Certificate authorities trusted to verify the backend certificates
  trustedCertificateAuthorities() []oci.certificates.certificateAuthority
  // CA bundles trusted to verify the backend certificates
  trustedCaBundles() []oci.certificates.caBundle
  // Name of the application cookie the backends set to pin a session
  //
  // Application cookie stickiness, where the load balancer keys persistence
  // off a cookie the backend already sets. A value of `*` pins on any
  // backend cookie. Empty when this form of persistence is off.
  sessionPersistenceCookieName string
  // Whether application cookie stickiness refuses to fall back to another backend
  //
  // When set, a client pinned to a backend that has failed its health check
  // is not redirected to a healthy one.
  sessionPersistenceDisableFallback bool
  // Name of the cookie the load balancer itself inserts to pin a session
  //
  // Load balancer cookie stickiness. Empty when this form of persistence is
  // off; defaults to X-Oracle-BMC-LBS-Route when enabled without a chosen
  // name.
  lbCookieName string
  // Whether load balancer cookie stickiness refuses to fall back to another backend
  lbCookieDisableFallback bool
  // Domain attribute set on the load balancer session cookie
  //
  // Empty when the load balancer omits the attribute, in which case the
  // client returns the cookie only to the host that issued it.
  lbCookieDomain string
  // Path attribute set on the load balancer session cookie
  //
  // Defaults to `/`.
  lbCookiePath string
  // Lifetime in seconds of the load balancer session cookie
  //
  // Null when the load balancer omits the Max-Age attribute, in which case
  // the cookie lasts as long as the client session.
  lbCookieMaxAgeInSeconds int
  // Whether the load balancer session cookie carries the Secure attribute
  //
  // Without it the cookie that pins a session can travel over plain HTTP.
  lbCookieIsSecure bool
  // Whether the load balancer session cookie carries the HttpOnly attribute
  //
  // Without it the cookie that pins a session is readable from JavaScript.
  lbCookieIsHttpOnly bool
}

// Backend server in an Oracle Cloud Infrastructure (OCI) load balancer backend set
//
// One server the load balancer forwards to, with the weighting and drain,
// backup, and offline flags that decide whether it currently receives traffic.
private oci.loadBalancer.backend @defaults("name") {
  // Backend name, the address and port that identify it in the set (e.g., 10.0.0.3:8080)
  name string
  // Backend server IP address
  ipAddress string
  // Backend server port
  port int
  // Load balancing weight relative to the other backends in the set
  weight int
  // Whether the backend is draining and receives no new connections
  drain bool
  // Whether the backend only receives traffic once every non-backup backend fails its health check
  backup bool
  // Whether the backend is marked offline and receives no traffic
  offline bool
  // Maximum simultaneous connections the load balancer opens to this backend
  //
  // Null or zero means unlimited.
  maxConnections int
}

// Oracle Cloud Infrastructure (OCI) Network Load Balancer
//
// Layer 4 load balancers in the tenancy, a service distinct from the
// Load Balancer exposed by `oci.loadBalancer`. Network load balancers
// operate on TCP, UDP, and ICMP rather than HTTP, are frequently
// internet-facing, and are governed by their own listeners and backend
// sets. `loadBalancers` enumerates every deployment with its public or
// private placement, assigned addresses, attached network security
// groups, and a derived internet-exposure breakdown matching the one on
// compute instances and load balancers.
oci.networkLoadBalancer {
  // Network load balancers
  loadBalancers() []oci.networkLoadBalancer.loadBalancer
}

// Oracle Cloud Infrastructure (OCI) network load balancer
//
// Single layer 4 load balancer, keyed by its OCID. `isPrivate` and
// `ipAddresses` decide whether it fronts the public internet, and
// `exposure` combines that with the subnet's routing and the ingress
// rules of the attached network security groups and security lists into
// a single reachability verdict. `isPreserveSourceDestination` reports
// whether the original client address survives to the backend, which
// decides whether backend-side address filtering sees the real source.
// `securityAttributes` carries the Zero Trust Packet Routing attributes
// applied to the load balancer.
private oci.networkLoadBalancer.loadBalancer @defaults("name isPrivate state") {
  // Network load balancer OCID
  id string
  // Display name
  name string
  // Compartment containing the network load balancer
  compartment() oci.compartment
  // Whether the network load balancer is private (no public IP)
  isPrivate bool
  // Assigned IP addresses
  //
  // Each entry is a dict with an `ipAddress`, an `isPublic` flag indicating
  // internet reachability, an `ipVersion` of IPV4 or IPV6, and a
  // `reservedIp` block carrying the OCID when the address is backed by a
  // reserved public IP.
  ipAddresses []dict
  // IP version the network load balancer serves (IPV4, IPV4_AND_IPV6, IPV6)
  ipVersion string
  // Subnet the network load balancer is attached to
  subnet() oci.network.subnet
  // Network security groups attached to the network load balancer
  securityGroups() []oci.network.networkSecurityGroup
  // Internet-exposure breakdown (a public IP and a listener, gated on subnet routing and ingress rules)
  exposure() oci.network.exposure
  // Listeners accepting traffic
  listeners []oci.networkLoadBalancer.listener
  // Backend sets traffic is distributed to
  backendSets []oci.networkLoadBalancer.backendSet
  // Whether the source and destination addresses are preserved to the backend
  //
  // When enabled the backend observes the original client address rather
  // than the load balancer's, so any address-based filtering on the
  // backend evaluates the real source.
  isPreserveSourceDestination bool
  // Whether symmetric hashing is enabled for flow distribution
  isSymmetricHashEnabled bool
  // Zero Trust Packet Routing security attributes applied to the load balancer
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Lifecycle state (CREATING, UPDATING, ACTIVE, FAILED, DELETING, DELETED)
  state string
  // Detail explaining the current lifecycle state, for FAILED deployments
  stateDetails string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) network load balancer listener
//
// Port and protocol a network load balancer accepts traffic on, and the
// backend set it forwards to. Unlike a Load Balancer listener there is
// no TLS termination here: the network load balancer operates at layer
// 4, so `protocol` is TCP, UDP, TCP_AND_UDP, ICMP, or ANY, and a `port`
// of 0 means the listener accepts every port.
private oci.networkLoadBalancer.listener @defaults("name port protocol") {
  // Listener name
  name string
  // Listener port, or 0 when the listener accepts all ports
  port int
  // Protocol (TCP, UDP, TCP_AND_UDP, ICMP, ANY)
  protocol string
  // Name of the backend set the listener forwards to
  defaultBackendSetName string
  // IP version the listener serves (IPV4, IPV6)
  ipVersion string
  // Idle timeout for TCP connections, in seconds
  tcpIdleTimeout int
  // Idle timeout for UDP flows, in seconds
  udpIdleTimeout int
}

// Oracle Cloud Infrastructure (OCI) network load balancer backend set
private oci.networkLoadBalancer.backendSet @defaults("name policy") {
  // Backend set name
  name string
  // Load balancing policy (FIVE_TUPLE, THREE_TUPLE, TWO_TUPLE)
  policy string
  // IP version the backend set serves (IPV4, IPV6)
  ipVersion string
  // Whether the original source address is preserved to the backend
  isPreserveSource bool
  // Whether traffic is still forwarded when every backend fails its health check
  //
  // When enabled the load balancer keeps sending traffic to backends that
  // are failing health checks rather than dropping it, so an
  // all-unhealthy backend set continues to receive requests.
  isFailOpen bool
  // Whether instant failover is enabled
  isInstantFailoverEnabled bool
  // Health checker configuration
  //
  // A dict describing the backend health probe. Keys: `protocol` (HTTP,
  // HTTPS, TCP, UDP, or DNS), `port`, `urlPath`, `returnCode` (expected
  // status code), `responseBodyRegex`, `retries`, `timeoutInMillis`,
  // `intervalInMillis`, and a `dns` block for DNS probes.
  healthChecker dict
  // Backends traffic is distributed to
  backends []oci.networkLoadBalancer.backend
  // Number of backends
  backendCount int
}

// Oracle Cloud Infrastructure (OCI) network load balancer backend
//
// Single target behind a backend set. A backend is addressed either by
// `ipAddress` or by the OCID of the compute instance in `instance`,
// which is what makes a network load balancer traceable to the workload
// it fronts when assessing an internet-facing path. `isOffline`,
// `isDrain`, and `isBackup` report whether the backend is actually
// taking traffic.
private oci.networkLoadBalancer.backend @defaults("name port") {
  // Backend name, usually `<address>:<port>`
  name string
  // IP address of the backend, when addressed by address rather than OCID
  ipAddress string
  // Compute instance the backend targets, when addressed by OCID
  instance() oci.compute.instance
  // Port the backend receives traffic on
  port int
  // Relative weight for traffic distribution
  weight int
  // Whether the backend is draining existing connections and taking no new ones
  isDrain bool
  // Whether the backend only receives traffic when the primary backends fail
  isBackup bool
  // Whether the backend is marked offline and receives no traffic
  isOffline bool
}

// Oracle Cloud Infrastructure (OCI) Resource Manager
//
// Terraform stacks managed by Resource Manager, and the jobs that have
// run against them. A stack's `driftStatus` reports whether the deployed
// infrastructure still matches the configuration that declared it, which
// is the signal that something was changed outside the pipeline that is
// supposed to own it. `configSource` records where the Terraform
// configuration comes from, so a stack fed from an external Git
// repository is distinguishable from one uploaded as a zip.
oci.resourceManager {
  // Terraform stacks
  stacks() []oci.resourceManager.stack
}

// Oracle Cloud Infrastructure (OCI) Resource Manager stack
//
// Single Terraform stack, keyed by its OCID. `driftStatus` is IN_SYNC,
// DRIFTED, or NOT_CHECKED, and `driftLastChecked` records when that
// verdict was produced. NOT_CHECKED matters as much as DRIFTED: a stack
// whose drift has never been evaluated offers no assurance that the
// running infrastructure matches its declaration, and a `driftLastChecked`
// far in the past means the same thing. `jobs` lists the plan, apply,
// destroy and drift-detection runs against the stack, which is the
// change history for everything the stack manages.
private oci.resourceManager.stack @defaults("name state terraformVersion") {
  // Stack OCID
  id string
  // Stack display name
  name string
  // Stack description
  description string
  // Compartment containing the stack
  compartment() oci.compartment
  // Terraform version the stack runs
  terraformVersion string
  // Whether the deployed infrastructure matches the configuration (IN_SYNC, DRIFTED, NOT_CHECKED)
  driftStatus() string
  // When drift was last evaluated
  //
  // Null when `driftStatus` is NOT_CHECKED. A timestamp far in the past
  // means the IN_SYNC or DRIFTED verdict describes the stack as it was
  // then, not as it is now.
  driftLastChecked() time
  // Where the Terraform configuration is loaded from
  //
  // Has a `configSourceType` of ZIP_UPLOAD, GIT_CONFIG_SOURCE,
  // COMPARTMENT_CONFIG_SOURCE, OBJECT_STORAGE_CONFIG_SOURCE,
  // DEVOPS_CONFIG_SOURCE, BITBUCKET_CLOUD_CONFIG_SOURCE, or
  // BITBUCKET_SERVER_CONFIG_SOURCE, plus a `workingDirectory`. Git-backed
  // sources also carry the `configurationSourceProviderId`,
  // `repositoryUrl`, and `branchName` naming the external repository the
  // configuration comes from.
  configSource() dict
  // Names of the Terraform variables set on the stack
  //
  // Names only. Resource Manager stores variable values as plain strings
  // with no sensitivity marking, so they routinely hold passwords, tokens,
  // and keys; returning them would copy those secrets into every scan
  // result. The names are enough to find a stack carrying credentials it
  // should be sourcing from Vault.
  variableNames() []string
  // Whether the stack may use third-party Terraform providers
  isThirdPartyProviderExperienceEnabled() bool
  // Jobs run against the stack
  jobs() []oci.resourceManager.job
  // Stack lifecycle state (CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Stack creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Resource Manager job
//
// Single run against a stack, keyed by its OCID. `operation` is PLAN,
// APPLY, DESTROY, IMPORT_TF_STATE, or PLAN_ROLLBACK and APPLY_ROLLBACK,
// so the job list is the record of who changed the managed
// infrastructure and when. `state` reports whether the run succeeded,
// and `finished` when it ended.
private oci.resourceManager.job @defaults("name operation state") {
  // Job OCID
  id string
  // Job display name
  name string
  // Compartment containing the job
  compartment() oci.compartment
  // Stack the job ran against
  //
  // Every job belongs to a stack, so this is the route from a failed or
  // rolled-back run to the configuration that produced it and to the
  // Terraform state it manages.
  stack() oci.resourceManager.stack
  // Operation the job performed (PLAN, APPLY, DESTROY, IMPORT_TF_STATE, PLAN_ROLLBACK, APPLY_ROLLBACK)
  operation string
  // Job lifecycle state (ACCEPTED, IN_PROGRESS, FAILED, SUCCEEDED, CANCELING, CANCELED)
  state string
  // Job start time
  created time
  // Job completion time
  finished time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Streaming
//
// Kafka-compatible streams in the tenancy and the pools that host them.
// Encryption and network placement are properties of the pool rather
// than the stream: `streamPools` reports whether a pool is reachable
// only through a private endpoint, which subnet and network security
// groups that endpoint sits behind, and whether messages at rest are
// protected by a customer-managed key. `streams` enumerates the
// individual streams and resolves back to the pool that governs them.
oci.streaming {
  // Streams
  streams() []oci.streaming.stream
  // Stream pools hosting the streams
  streamPools() []oci.streaming.streamPool
}

// Oracle Cloud Infrastructure (OCI) stream
//
// Single stream, keyed by its OCID. A stream inherits its encryption and
// network reachability from the pool it belongs to, so `streamPool` is
// the field that answers whether the data it carries is protected by a
// customer-managed key and whether producers and consumers can reach it
// from outside the VCN.
private oci.streaming.stream @defaults("name state") {
  // Stream OCID
  id string
  // Stream name
  name string
  // Compartment containing the stream
  compartment() oci.compartment
  // Stream pool hosting the stream
  streamPool() oci.streaming.streamPool
  // Number of partitions
  partitions int
  // Endpoint messages are produced to and consumed from
  messagesEndpoint string
  // Lifecycle state (CREATING, ACTIVE, DELETING, DELETED, FAILED, UPDATING)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) stream pool
//
// Pool hosting one or more streams, keyed by its OCID. `isPrivate`
// decides whether the pool is reachable only through a private endpoint
// in `subnet`, gated by `securityGroups`, or published on a public
// endpoint. `encryptionKey` resolves the customer-managed key protecting
// messages at rest and is null when Oracle-managed encryption is in use.
// `kafkaSettings` carries the Kafka compatibility configuration,
// including whether producers may create topics implicitly.
private oci.streaming.streamPool @defaults("name isPrivate state") {
  // Stream pool OCID
  id string
  // Stream pool name
  name string
  // Compartment containing the stream pool
  compartment() oci.compartment
  // Whether the pool is reachable only through a private endpoint
  isPrivate bool
  // Fully-qualified domain name of the pool endpoint
  endpointFqdn() string
  // Subnet the private endpoint is placed in
  subnet() oci.network.subnet
  // Network security groups attached to the private endpoint
  securityGroups() []oci.network.networkSecurityGroup
  // Private IP address of the pool endpoint
  privateEndpointIp() string
  // Customer-managed KMS key protecting messages at rest
  //
  // Null when the pool uses Oracle-managed encryption.
  encryptionKey() oci.kms.key
  // State of the customer-managed encryption key (ACTIVE, CREATING, DELETING, NONE, FAILED, UPDATING)
  //
  // NONE means no customer-managed key is configured and the pool is
  // encrypted with an Oracle-managed key.
  encryptionKeyState() string
  // Kafka compatibility settings
  //
  // Has `bootstrapServers`, `autoCreateTopicsEnable` (whether producers
  // may create topics implicitly), `logRetentionHours`, and
  // `numPartitions`.
  kafkaSettings() dict
  // Zero Trust Packet Routing security attributes applied to the pool
  securityAttributes map[string]dict
  // Lifecycle state (CREATING, ACTIVE, DELETING, DELETED, FAILED, UPDATING)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Queue
//
// Managed message queues in the tenancy. Each queue reports the endpoint
// its messages are produced to and consumed from, the retention and
// visibility timers that govern how long undelivered messages persist,
// and whether messages at rest are protected by a customer-managed key.
oci.queue {
  // Message queues
  queues() []oci.queue.queue
}

// Oracle Cloud Infrastructure (OCI) queue
//
// Single message queue, keyed by its OCID. `encryptionKey` resolves the
// customer-managed key protecting messages at rest and is null when the
// queue uses Oracle-managed encryption. `retentionInSeconds` bounds how
// long an undelivered message persists, and
// `deadLetterQueueDeliveryCount` how many delivery attempts precede a
// move to the dead-letter queue.
private oci.queue.queue @defaults("name state") {
  // Queue OCID
  id string
  // Queue display name
  name string
  // Compartment containing the queue
  compartment() oci.compartment
  // Endpoint messages are produced to and consumed from
  messagesEndpoint string
  // Customer-managed KMS key protecting messages at rest
  //
  // Null when the queue uses Oracle-managed encryption.
  encryptionKey() oci.kms.key
  // How long an undelivered message is retained, in seconds
  retentionInSeconds() int
  // How long a consumed message stays hidden from other consumers, in seconds
  visibilityInSeconds() int
  // How long a request waits for a message before returning empty, in seconds
  timeoutInSeconds() int
  // Delivery attempts before a message moves to the dead-letter queue
  deadLetterQueueDeliveryCount() int
  // Capabilities enabled on the queue (CONSUMER_GROUPS, LARGE_MESSAGES)
  capabilities []string
  // Lifecycle state (CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail explaining the current lifecycle state, for FAILED queues
  stateDetails string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Managed Kafka
//
// Managed Apache Kafka clusters in the tenancy. Each cluster is reached
// through the subnets in `subnets`, and `bootstrapUrls` reports the
// broker endpoints along with the authentication mechanism each one
// requires, which is what decides whether clients present SASL
// credentials or a mutual-TLS certificate. `clusterType` separates
// DEVELOPMENT clusters, which run without the redundancy of a
// PRODUCTION deployment.
oci.kafka {
  // Managed Kafka clusters
  clusters() []oci.kafka.cluster
}

// Oracle Cloud Infrastructure (OCI) Kafka cluster
//
// Single managed Kafka cluster, keyed by its OCID. `subnets` places the
// cluster on the network and is the primary reachability control, since
// a managed Kafka cluster has no public endpoint. `superuserSecret`
// resolves the vault secret holding the cluster's superuser password
// when one is configured. `coordinationType` reports whether the cluster
// still uses ZooKeeper or has moved to KRaft.
private oci.kafka.cluster @defaults("name kafkaVersion clusterType state") {
  // Cluster OCID
  id string
  // Cluster display name
  name string
  // Compartment containing the cluster
  compartment() oci.compartment
  // Kafka version
  kafkaVersion string
  // Cluster type (DEVELOPMENT, PRODUCTION)
  clusterType string
  // Cluster coordination mechanism (ZOOKEEPER, KRAFT)
  coordinationType string
  // Subnets the cluster is reachable through
  subnets() []oci.network.subnet
  // Broker bootstrap endpoints
  //
  // Each entry has a `name` and a `url`. The name identifies which
  // authentication mechanism the endpoint expects, SASL or MTLS.
  bootstrapUrls() []dict
  // Vault secret holding the cluster superuser password
  superuserSecret() oci.vault.secret
  // Broker shape and sizing
  //
  // Has `nodeCount`, `ocpuCount`, `storageSizeInGBs`, and the
  // `shapeName`.
  brokerShape dict
  // Cluster configuration applied to the cluster
  clusterConfigId string
  // Version of the cluster configuration in use
  clusterConfigVersion int
  // Lifecycle state (CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail explaining the current lifecycle state, for FAILED clusters
  stateDetails string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) MySQL HeatWave
//
// Managed MySQL database systems in the tenancy. Each system reports
// where it sits on the network, how it encrypts data at rest, whether
// TLS is served from a customer certificate or a service-generated one,
// and its backup retention, which together cover the standard
// data-at-rest and data-in-transit questions for a managed database.
// `accessMode` and `databaseMode` report whether the system is accepting
// unrestricted traffic and whether it is writable.
oci.mysql {
  // MySQL database systems
  dbSystems() []oci.mysql.dbSystem
}

// Oracle Cloud Infrastructure (OCI) MySQL database system
//
// Single managed MySQL system, keyed by its OCID. `encryptionKey`
// resolves the customer-managed KMS key protecting data at rest, and is
// null when the system uses Oracle-managed encryption:
// `encryptionKeyGenerationType` distinguishes the two. `tlsCertificateType`
// reports whether TLS is served from a BYOC certificate or a
// service-generated one. `subnet` and `securityGroups` place the system
// on the network, and `endpoints` lists the addresses it answers on with
// the read/write modes each accepts.
private oci.mysql.dbSystem @defaults("name version state") {
  // Database system OCID
  id string
  // Display name
  name string
  // Description
  description string
  // Compartment containing the database system
  compartment() oci.compartment
  // MySQL server version
  version string
  // Shape name
  shape string
  // Whether the database accepts writes (READ_WRITE) or is read-only (READ_ONLY)
  databaseMode string
  // Whether the database accepts unrestricted traffic (UNRESTRICTED) or is restricted to administrators (RESTRICTED)
  accessMode string
  // Whether the system is deployed across availability domains for high availability
  isHighlyAvailable bool
  // Whether a HeatWave analytics cluster is attached
  isHeatWaveClusterAttached bool
  // Whether crash recovery is enabled (ENABLED, DISABLED)
  crashRecovery string
  // Whether Database Management is enabled for the system (ENABLED, DISABLED)
  databaseManagement string
  // Subnet the database system is attached to
  subnet() oci.network.subnet
  // Network security groups attached to the database system
  securityGroups() []oci.network.networkSecurityGroup
  // Addresses the database system answers on
  //
  // Each entry has an `ipAddress`, `hostname`, `port` and `portX`, an
  // `ipAddressVersion`, a `status`, the `modes` the endpoint accepts
  // (READ, WRITE), and a `resourceType` of DBSYSTEM, READ_REPLICA,
  // LOAD_BALANCER, or READ_ENDPOINT.
  endpoints []dict
  // How the data-at-rest encryption key is managed (SYSTEM, BYOK)
  encryptionKeyGenerationType() string
  // Customer-managed KMS key protecting data at rest
  //
  // Null when `encryptionKeyGenerationType` is SYSTEM, meaning the system
  // is encrypted with an Oracle-managed key.
  encryptionKey() oci.kms.key
  // How the TLS certificate is provided (SYSTEM, BYOC)
  tlsCertificateType() string
  // Certificate served for TLS connections, when a customer certificate is configured
  tlsCertificate() oci.certificates.certificate
  // Automatic backup policy
  //
  // Has `isEnabled`, `retentionInDays`, `windowStartTime`, a `softDelete`
  // flag, a `pitrPolicy` block for point-in-time recovery, and
  // `copyPolicies` for cross-region backup copies.
  backupPolicy dict
  // Policy applied to backups and automatic backups when the system is deleted
  //
  // Has `automaticBackupRetention`, `finalBackup`, and
  // `isDeleteProtected`, which together decide whether deleting the
  // system also destroys its recovery points.
  deletionPolicy dict
  // Availability domain the system is placed in
  availabilityDomain string
  // Fault domain the system is placed in
  faultDomain string
  // Lifecycle state (CREATING, ACTIVE, INACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Database with PostgreSQL
//
// Managed PostgreSQL database systems in the tenancy. PostgreSQL systems
// are always deployed into a customer subnet with no public endpoint, so
// the exposure question is which subnet and which network security
// groups rather than public versus private. Each system reports its
// backup policy, its admin username, and whether a reader endpoint is
// published alongside the primary.
oci.postgresql {
  // PostgreSQL database systems
  dbSystems() []oci.postgresql.dbSystem
}

// Oracle Cloud Infrastructure (OCI) PostgreSQL database system
//
// Single managed PostgreSQL system, keyed by its OCID. `subnet` and
// `securityGroups` place it on the network and are the only controls on
// who can reach it. `managementPolicy` carries the backup retention and
// maintenance window, `storageDetails` the storage configuration
// including whether the volume is encrypted with a customer-managed key,
// and `systemRole` distinguishes a standalone system from a primary or
// one of its standbys.
private oci.postgresql.dbSystem @defaults("name dbVersion state") {
  // Database system OCID
  id string
  // Display name
  name string
  // Description
  description() string
  // Compartment containing the database system
  compartment() oci.compartment
  // PostgreSQL server version
  dbVersion string
  // Shape name
  shape string
  // Deployment type (OCI_OPTIMIZED_STORAGE)
  systemType string
  // Role in a disaster-recovery topology (STANDALONE_DB_SYSTEM, PRIMARY_DB_SYSTEM, WARM_STANDBY_DB_SYSTEM, PILOT_LIGHT_DB_SYSTEM)
  systemRole() string
  // Number of database instances in the system
  instanceCount int
  // OCPUs allocated per instance
  instanceOcpuCount int
  // Memory allocated per instance, in GB
  instanceMemorySizeInGBs int
  // Administrator user name created with the system
  adminUsername() string
  // Subnet the database system is attached to
  subnet() oci.network.subnet
  // Network security groups attached to the database system
  securityGroups() []oci.network.networkSecurityGroup
  // Private IP address of the primary database endpoint
  primaryEndpointPrivateIp() string
  // Whether a separate read-only endpoint is published
  isReaderEndpointEnabled() bool
  // Storage configuration
  //
  // Has `systemType`, `isRegionallyDurable`, `availabilityDomain`, and
  // `iops`. For an encrypted volume it also carries the KMS `kmsKeyId`.
  storageDetails() dict
  // Backup and maintenance policy
  //
  // Has a `backupPolicy` block with the retention schedule (`kind` of
  // DAILY, WEEKLY, MONTHLY, or NONE, plus `retentionDays` and the
  // scheduled time) and a `maintenanceWindowStart`.
  managementPolicy() dict
  // Database configuration applied to the system
  configId string
  // Lifecycle state (CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED, NEEDS_ATTENTION)
  state string
  // Detail explaining the current lifecycle state, for FAILED systems
  stateDetails string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) NoSQL Database
//
// NoSQL tables in the tenancy. Tables report their lifecycle state,
// throughput and storage limits, and whether they are replicated across
// regions, which is what determines where the data physically resides.
// `isAutoReclaimable` marks always-free tables that OCI deletes
// automatically after a period of inactivity, and `timeOfExpiration`
// when that will happen.
oci.nosql {
  // NoSQL tables
  tables() []oci.nosql.table
}

// Oracle Cloud Infrastructure (OCI) NoSQL table
private oci.nosql.table @defaults("name state") {
  // Table OCID
  id string
  // Table name
  name string
  // Compartment containing the table
  compartment() oci.compartment
  // Throughput and storage limits
  //
  // Has `maxReadUnits`, `maxWriteUnits`, `maxStorageInGBs`, and a
  // `capacityMode` of PROVISIONED or ON_DEMAND.
  tableLimits dict
  // Whether the table is replicated across regions
  isMultiRegion bool
  // Whether the table is reclaimed automatically after a period of inactivity
  //
  // True for always-free tables, which OCI deletes on the
  // `timeOfExpiration` date unless they are used again.
  isAutoReclaimable bool
  // When an auto-reclaimable table will be deleted
  timeOfExpiration time
  // Whether the table schema can still be changed (MUTABLE, FROZEN)
  //
  // A frozen schema is required before a table can be replicated across
  // regions.
  schemaState string
  // Lifecycle state (CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Search with OpenSearch
//
// OpenSearch clusters in the tenancy. `securityMode` is the field that
// decides whether the cluster authenticates its callers at all: a
// cluster in DISABLED mode serves every request unauthenticated to
// anything that can reach it on the network, PERMISSIVE accepts both
// authenticated and unauthenticated requests, and only ENFORCING
// requires credentials. Combined with the subnet placement in `subnet`,
// that determines who can read the indexed data.
oci.opensearch {
  // OpenSearch clusters
  clusters() []oci.opensearch.cluster
}

// Oracle Cloud Infrastructure (OCI) OpenSearch cluster
//
// Single OpenSearch cluster, keyed by its OCID. Beyond `securityMode`,
// `securityMasterUserName` names the built-in administrator when
// security is on, and `outboundClusterConfig` reports whether the
// cluster is configured to reach out to other clusters through
// cross-cluster search. `securityAttributes` carries the Zero Trust
// Packet Routing attributes applied to the cluster.
private oci.opensearch.cluster @defaults("name softwareVersion securityMode state") {
  // Cluster OCID
  id string
  // Display name
  name string
  // Compartment containing the cluster
  compartment() oci.compartment
  // OpenSearch version
  softwareVersion string
  // Whether the cluster requires authentication (DISABLED, PERMISSIVE, ENFORCING)
  //
  // DISABLED serves every request unauthenticated. PERMISSIVE accepts
  // both authenticated and unauthenticated requests, so it does not
  // enforce access control either. Only ENFORCING requires credentials.
  securityMode string
  // Built-in administrator user name, when security is enabled
  securityMasterUserName() string
  // Whether SAML single sign-on is configured on the cluster
  //
  // When enabled, an external identity provider issues the assertions the
  // cluster accepts, so access is no longer bounded by the built-in master
  // user and internal roles alone.
  isSamlEnabled() bool
  // Entity identifier of the SAML identity provider
  //
  // Empty when SAML is not configured.
  samlIdpEntityId() string
  // Backend role mapped to cluster administrator for SAML users
  //
  // Every federated user carrying this role becomes a cluster administrator.
  // Empty when SAML is not configured.
  samlAdminBackendRole() string
  // Assertion attribute the cluster reads the user name from
  samlSubjectKey() string
  // Assertion attribute the cluster reads role membership from
  //
  // Role membership drives authorization, so the attribute named here decides
  // which cluster roles a federated user receives.
  samlRolesKey() string
  // OpenSearch Dashboards URL registered with the identity provider
  samlOpendashboardUrl() string
  // How the OpenSearch API endpoint certificate is managed (OCI_MANAGED, BYOC)
  //
  // BYOC means the certificate comes from the tenancy's own OCI Certificates
  // service, exposed through apiCertificate.
  clusterCertificateMode() string
  // How the OpenSearch Dashboards endpoint certificate is managed (OCI_MANAGED, BYOC)
  dashboardCertificateMode() string
  // Certificate served on the OpenSearch API endpoint
  //
  // Null when the endpoint uses an Oracle-managed certificate.
  apiCertificate() oci.certificates.certificate
  // Certificate served on the OpenSearch Dashboards endpoint
  //
  // Null when the endpoint uses an Oracle-managed certificate.
  dashboardCertificate() oci.certificates.certificate
  // VCN the cluster is deployed into
  vcn() oci.network.vcn
  // Subnet the cluster is deployed into
  subnet() oci.network.subnet
  // Network security groups attached to the cluster
  //
  // Empty when the cluster is governed only by its subnet's security lists.
  securityGroups() []oci.network.networkSecurityGroup
  // Fully-qualified domain name of the cluster
  fqdn() string
  // Load balancer service fronting the cluster (OCI_LOAD_BALANCER, OCI_NETWORK_LOAD_BALANCER)
  //
  // Empty when the cluster is reached directly rather than through a load
  // balancer.
  loadBalancerServiceType() string
  // Reverse connection endpoints the cluster reaches customer networks through
  //
  // Each entry has a `customerIp`, the address in the customer VCN, and a
  // `natIp`, the address the cluster presents. These are the paths by which
  // the managed cluster reaches back into tenant networks.
  reverseConnectionEndpoints() []dict
  // Clusters permitted to run cross-cluster searches against this one
  //
  // The inbound half of cross-cluster search, where outboundClusterConfig is
  // the outbound half. Empty when no other cluster may search this one.
  inboundClusters() []oci.opensearch.cluster
  // Fully-qualified domain name of the OpenSearch API endpoint
  opensearchFqdn() string
  // Private IP address of the OpenSearch API endpoint
  opensearchPrivateIp() string
  // Fully-qualified domain name of the OpenSearch Dashboards endpoint
  opendashboardFqdn() string
  // Private IP address of the OpenSearch Dashboards endpoint
  opendashboardPrivateIp() string
  // Cross-cluster search configuration
  //
  // Has `isEnabled` and a `outboundClusters` list, each entry carrying
  // the remote cluster's `displayName`, `seedClusterId`, `mode`, and
  // whether it `isSkipUnavailable`.
  outboundClusterConfig dict
  // Backup policy
  //
  // Has `isEnabled`, a `retentionInDays`, and the cron `scheduleTime`
  // the snapshot runs on.
  backupPolicy dict
  // Total storage allocated to the cluster, in GB
  totalStorageGB int
  // Availability domains the cluster is distributed across
  availabilityDomains []string
  // Zero Trust Packet Routing security attributes applied to the cluster
  securityAttributes map[string]dict
  // Lifecycle state (CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail explaining the current lifecycle state, for FAILED clusters
  stateDetails string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) GoldenGate
//
// GoldenGate data-replication deployments in the tenancy. A deployment
// runs a web console and REST API that hold the credentials for every
// source and target database it replicates between, so `isPublic` - and
// the `publicIpAddress` that accompanies it - is the field that decides
// whether that console is reachable from the internet. `subnet` and
// `securityGroups` govern reachability for private deployments.
oci.goldenGate {
  // GoldenGate deployments
  deployments() []oci.goldenGate.deployment
}

// Oracle Cloud Infrastructure (OCI) GoldenGate deployment
//
// Single GoldenGate deployment, keyed by its OCID. `deploymentUrl` is
// the console and REST endpoint, published on `publicIpAddress` when
// `isPublic` is set and on `privateIpAddress` otherwise.
// `isLatestVersion` reports whether the deployment is running the
// current GoldenGate release, which is the patch-currency signal for a
// component that brokers database credentials. `securityAttributes`
// carries the Zero Trust Packet Routing attributes applied to the
// deployment.
private oci.goldenGate.deployment @defaults("name isPublic state") {
  // Deployment OCID
  id string
  // Display name
  name string
  // Description
  description string
  // Compartment containing the deployment
  compartment() oci.compartment
  // Whether the deployment console and REST API are reachable from the internet
  isPublic bool
  // Public IP address of the deployment, when it is public
  publicIpAddress string
  // Private IP address of the deployment
  privateIpAddress string
  // Fully-qualified domain name of the deployment
  fqdn string
  // URL of the deployment console and REST API
  deploymentUrl string
  // Subnet the deployment is attached to
  subnet() oci.network.subnet
  // Network security groups attached to the deployment
  securityGroups() []oci.network.networkSecurityGroup
  // Load balancer fronting the deployment
  loadBalancer() oci.loadBalancer.loadBalancer
  // Deployment type
  //
  // One of OGG, DATABASE_ORACLE, BIGDATA, DATABASE_MICROSOFT_SQLSERVER,
  // DATABASE_MYSQL, DATABASE_POSTGRESQL, DATABASE_DB2ZOS, GGSA, or
  // DATA_TRANSFORMS.
  deploymentType string
  // Deployment category (DATA_REPLICATION, STREAM_ANALYTICS, DATA_TRANSFORMS)
  category string
  // Environment type (PRODUCTION, DEVELOPMENT_OR_TESTING)
  environmentType string
  // License model (LICENSE_INCLUDED, BRING_YOUR_OWN_LICENSE)
  licenseModel string
  // Whether the deployment runs the latest available GoldenGate version
  isLatestVersion bool
  // Whether CPU auto-scaling is enabled
  isAutoScalingEnabled bool
  // OCPUs allocated to the deployment
  cpuCoreCount int
  // Zero Trust Packet Routing security attributes applied to the deployment
  securityAttributes map[string]dict
  // Lifecycle state
  //
  // One of CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED,
  // FAILED, NEEDS_ATTENTION, IN_PROGRESS, CANCELING, CANCELED,
  // SUCCEEDED, or WAITING.
  state string
  // Detailed lifecycle sub-state
  //
  // One of RECOVERING, STARTING, STOPPING, MOVING, UPGRADING, RESTORING,
  // BACKUP_IN_PROGRESS, or ROLLBACK_IN_PROGRESS.
  lifecycleSubState string
  // Detail explaining the current lifecycle state, for FAILED deployments
  stateDetails string
  // Creation time
  created time
  // Last-update time
  updated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) DNS
//
// DNS zones hosted in the tenancy and the traffic-management policies
// applied to them. `zones` covers both the internet-facing GLOBAL scope
// and the VCN-internal PRIVATE scope, each zone reporting its DNSSEC
// state and, through `records`, the resource records it publishes.
// `steeringPolicies` lists the geo, failover, and load-balancing rules
// that decide which answer a resolver receives.
oci.dns {
  // DNS zones hosted in the tenancy
  zones() []oci.dns.zone
  // Steering policies applied to DNS answers
  steeringPolicies() []oci.dns.steeringPolicy
}

// Oracle Cloud Infrastructure (OCI) DNS zone
//
// Single DNS zone, keyed by its OCID. `scope` separates a GLOBAL zone,
// which answers queries from the public internet, from a PRIVATE zone,
// which answers only inside the VCNs attached to its view.
// `dnssecState` reports whether DNSSEC signing is ENABLED or DISABLED,
// and `dnssecKeyVersions` carries the key-signing and zone-signing key
// material behind it, including each key's algorithm and its scheduled
// rollover times. `records` returns the resource records the zone
// publishes, which is what makes it possible to find answers still
// pointing at addresses or hostnames the tenancy no longer owns.
// `isProtected` marks zones OCI manages and blocks from modification.
private oci.dns.zone @defaults("name zoneType scope dnssecState") {
  // Zone OCID
  id string
  // Zone name (the DNS domain, e.g., example.com)
  name string
  // Compartment containing the zone
  compartment() oci.compartment
  // Zone type (PRIMARY, SECONDARY)
  zoneType string
  // Whether the zone answers publicly (GLOBAL) or only inside attached VCNs (PRIVATE)
  scope string
  // How the zone resolves queries (IMPORTED, IMPORTED_LIMITED_SUPPORT)
  resolutionMode string
  // Whether DNSSEC signing is enabled for the zone (ENABLED, DISABLED)
  dnssecState string
  // DNSSEC key-signing and zone-signing key versions
  //
  // Has `kskDnssecKeyVersions` and `zskDnssecKeyVersions` lists. Each
  // entry carries the key's `algorithm`, `lengthInBytes`, `uuid`, the
  // `timeCreated`, `timeActivated`, `timeExpired` and `timeInactivated`
  // marking its lifecycle, and the scheduled `timePromoted` and
  // `predecessorId` or `successorId` linking a rollover. Empty when
  // `dnssecState` is DISABLED.
  dnssecKeyVersions dict
  // Resource records published by the zone
  records() []oci.dns.record
  // Whether the zone is managed by OCI and cannot be modified
  isProtected bool
  // Zone serial number
  serial int
  // Zone version
  version string
  // View the zone belongs to, for PRIVATE zones
  viewId string
  // Zone lifecycle state (ACTIVE, CREATING, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Zone creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) DNS record
//
// Single resource record in a zone. `domain` is the fully-qualified name
// the record answers for, `rtype` its type (A, AAAA, CNAME, MX, TXT, NS,
// and so on), and `rdata` the answer itself. A CNAME or A record whose
// `rdata` points at a hostname or address the tenancy has released is
// the shape of a subdomain-takeover exposure, which is why the record
// data is queryable rather than summarized.
private oci.dns.record @defaults("domain rtype rdata") {
  // Fully-qualified domain name the record answers for
  domain string
  // Record type (A, AAAA, CNAME, MX, TXT, NS, SRV, CAA, and others)
  rtype string
  // Record data, in the presentation format for its type
  rdata string
  // Time-to-live in seconds
  ttl int
  // Whether the record is managed by OCI and cannot be modified
  isProtected bool
  // Version of the record set this record belongs to
  rrsetVersion string
}

// Oracle Cloud Infrastructure (OCI) DNS steering policy
//
// Rule set that decides which answer a resolver receives for a name,
// keyed by its OCID. `template` names the behavior: FAILOVER,
// LOAD_BALANCE, ROUTE_BY_GEO, ROUTE_BY_ASN, ROUTE_BY_IP, or CUSTOM.
// Geo and IP routing decide answers from client location, so the policy
// is part of understanding what a given client actually resolves.
private oci.dns.steeringPolicy @defaults("name template state") {
  // Steering policy OCID
  id string
  // Steering policy display name
  name string
  // Compartment containing the steering policy
  compartment() oci.compartment
  // Policy behavior (FAILOVER, LOAD_BALANCE, ROUTE_BY_GEO, ROUTE_BY_ASN, ROUTE_BY_IP, CUSTOM)
  template string
  // Time-to-live applied to answers this policy produces, in seconds
  ttl int
  // Health check monitor driving failover decisions
  healthCheckMonitorId string
  // Policy lifecycle state (ACTIVE, CREATING, UPDATING, DELETING, DELETED)
  state string
  // Policy creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Network Firewall
//
// Next-generation firewall deployments in the tenancy. The
// `firewalls` field lists the deployed firewall instances, each with
// its subnet placement, IPv4/IPv6 addresses, shape, and attached
// `policy`. The `policies` field lists the reusable firewall policies
// that drive the rule sets each firewall enforces.
oci.networkFirewall {
  // Network firewalls
  firewalls() []oci.networkFirewall.firewall
  // Network firewall policies
  policies() []oci.networkFirewall.policy
}

// Oracle Cloud Infrastructure (OCI) network firewall
private oci.networkFirewall.firewall @defaults("name") {
  // Firewall OCID
  id string
  // Display name
  name string
  // Compartment containing the firewall
  compartment() oci.compartment
  // Subnet the firewall is deployed in
  subnet() oci.network.subnet
  // Firewall policy
  policy() oci.networkFirewall.policy
  // IPv4 address
  ipv4Address string
  // IPv6 address
  ipv6Address string
  // Firewall shape
  shape string
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Zero Trust Packet Routing security attributes enforced on the firewall
  //
  // Keyed by security-attribute namespace. Each value is a dict mapping
  // the attribute names within that namespace to their assigned values.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Overall firewall health status
  //
  // One of OK, WARNING, CRITICAL, or UNKNOWN.
  healthStatus() string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) network firewall policy
private oci.networkFirewall.policy @defaults("name") {
  // Policy OCID
  id string
  // Display name
  name string
  // Compartment containing the policy
  compartment() oci.compartment
  // Policy description
  description() string
  // Number of firewalls attached to this policy
  attachedFirewallCount() int
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // SSL/TLS decryption profiles
  //
  // Decryption profiles bound to the policy's decryption rules, which
  // control the SSL/TLS sessions the firewall blocks during inspection
  // (unsupported protocol versions, weak cipher suites, expired or
  // untrusted certificates). Audit these to confirm that legacy TLS and
  // non-compliant ciphers are refused in transit.
  decryptionProfiles() []oci.networkFirewall.policy.decryptionProfile
  // Security rules the policy enforces
  //
  // The ordered rule set deciding which traffic the attached firewalls allow,
  // drop, reject or inspect. An empty list means the policy enforces nothing,
  // which is not the same as a policy that denies.
  securityRules() []oci.networkFirewall.policy.securityRule
  // Decryption rules the policy enforces
  //
  // The rules deciding which TLS sessions are decrypted for inspection, and
  // under which decryption profile.
  decryptionRules() []oci.networkFirewall.policy.decryptionRule
  // Address lists the policy's rules match on
  //
  // Named groups of IP addresses or FQDNs. Security rules name these rather
  // than carrying addresses inline, so a rule cannot be read as permissive or
  // restrictive without them.
  addressLists() []oci.networkFirewall.policy.addressList
  // URL pattern lists the policy's rules match on
  //
  // Named groups of URL patterns evaluated against the HTTP(S) request target
  // of decrypted traffic.
  urlLists() []oci.networkFirewall.policy.urlList
  // Services the policy's rules match on
  //
  // Named TCP or UDP port ranges. Together with applications these define the
  // protocol side of what a security rule matches.
  services() []oci.networkFirewall.policy.service
  // Service lists the policy's rules match on
  //
  // Named groups of services, referenced by security rules in place of listing
  // each service individually.
  serviceLists() []oci.networkFirewall.policy.serviceList
  // Applications the policy's rules match on
  //
  // Named ICMP and ICMPv6 message types and codes.
  applications() []oci.networkFirewall.policy.application
  // Application groups the policy's rules match on
  //
  // Named groups of applications, referenced by security rules in place of
  // listing each application individually.
  applicationGroups() []oci.networkFirewall.policy.applicationGroup
  // Secrets mapped into the policy for TLS inspection
  //
  // The certificates and private keys, held in Vault, that the firewall uses to
  // decrypt traffic. Each entry names the vault secret it reads, so these are
  // the key material behind every decryption rule.
  mappedSecrets() []oci.networkFirewall.policy.mappedSecret
  // NAT rules the policy applies
  //
  // Source network address translation applied to matching traffic. A NAT rule
  // changes the address a destination observes, so it also changes which
  // address-based controls downstream apply.
  natRules() []oci.networkFirewall.policy.natRule
  // Tunnel inspection rules the policy applies
  //
  // Rules deciding which encapsulated traffic (VXLAN) is unwrapped and
  // inspected. Traffic inside a tunnel that no rule matches passes through
  // uninspected.
  tunnelInspectionRules() []oci.networkFirewall.policy.tunnelInspectionRule
}

// Oracle Cloud Infrastructure (OCI) network firewall decryption profile
//
// SSL/TLS decryption profile bound to a network firewall policy. The
// `name` field selects the profile within its policy. `type`
// distinguishes SSL_FORWARD_PROXY (outbound inspection, which validates
// the server certificate) from SSL_INBOUND_INSPECTION. The boolean
// fields report which sessions the firewall blocks: unsupported TLS
// versions, unsupported cipher suites, and out-of-capacity conditions
// apply to both types, while the certificate-validation controls apply
// only to forward-proxy profiles.
// Network firewall policy security rule
//
// Single rule in a network firewall policy's ordered rule set, deciding what
// the attached firewalls do with matching traffic. `action` is the verdict and
// `priorityOrder` fixes the position in the evaluation order, so a permissive
// rule early in the set overrides stricter rules after it.
private oci.networkFirewall.policy.securityRule @defaults("name action inspection priorityOrder") {
  // Unique name of the rule within its policy
  name string
  // Verdict applied to matching traffic
  //
  // One of ALLOW, DROP, REJECT or INSPECT. DROP discards silently while
  // REJECT answers the sender.
  action string
  // Inspection applied to matching traffic
  //
  // INTRUSION_DETECTION logs a match, INTRUSION_PREVENTION blocks it. Empty
  // when the rule performs no inspection.
  inspection string
  // Position in the policy's evaluation order
  priorityOrder int
  // Rule description
  description string
  // Traffic the rule matches, by list name
  //
  // Keys are `sourceAddress`, `destinationAddress`, `application`, `service`
  // and `url`, each holding the names of the lists the rule evaluates against.
  // An absent or empty key matches anything, so a rule with no `sourceAddress`
  // accepts every source. The names resolve through the accessors below.
  condition() dict
  // Address lists evaluated against the traffic source
  sourceAddressLists() []oci.networkFirewall.policy.addressList
  // Address lists evaluated against the traffic destination
  destinationAddressLists() []oci.networkFirewall.policy.addressList
  // Applications the rule matches
  applications() []oci.networkFirewall.policy.application
  // Services the rule matches
  services() []oci.networkFirewall.policy.service
  // URL pattern lists the rule matches
  urlLists() []oci.networkFirewall.policy.urlList
}

// Named group of addresses in a network firewall policy
//
// A set of IP addresses or FQDNs that security and NAT rules match traffic
// against by name. The `name` field selects the list as a rule references it,
// for example `addressLists.where(name == "corp-ranges")`. An address list
// admitting broad public ranges makes every rule that names it permissive.
private oci.networkFirewall.policy.addressList @defaults("name type totalAddresses") {
  // Name of the address list, unique within its policy
  name string
  // Kind of entries the list holds
  //
  // One of IP or FQDN.
  type string
  // Description of the address list
  description string
  // Number of addresses in the list
  totalAddresses int
  // The addresses themselves
  //
  // IP addresses in CIDR notation, or fully qualified domain names, depending
  // on the list's type.
  addresses() []string
}

// Named group of URL patterns in a network firewall policy
//
// A set of URL patterns evaluated against the request target of decrypted
// HTTP(S) traffic. The `name` field selects the list as a security rule
// references it.
private oci.networkFirewall.policy.urlList @defaults("name totalUrls") {
  // Name of the URL list, unique within its policy
  name string
  // Description of the URL list
  description string
  // Number of URL patterns in the list
  totalUrls int
  // The URL patterns themselves
  //
  // Each entry: {type, pattern}, where a `SIMPLE` type pattern may use `*` as
  // a leading wildcard for subdomains and a trailing wildcard for paths.
  urls() []dict
}

// TCP or UDP service in a network firewall policy
//
// A named set of port ranges that security rules match traffic against. The
// `name` field selects the service as a rule references it.
private oci.networkFirewall.policy.service @defaults("name type") {
  // Name of the service, unique within its policy
  name string
  // Transport protocol
  //
  // One of TCP_SERVICE or UDP_SERVICE.
  type string
  // Description of the service
  description string
  // Port ranges the service covers
  //
  // Each entry: {minimumPort, maximumPort}. `maximumPort` is absent for a
  // single-port range.
  portRanges() []dict
}

// Named group of services in a network firewall policy
//
// A set of services referenced by security rules in place of listing each
// service individually.
private oci.networkFirewall.policy.serviceList @defaults("name totalServices") {
  // Name of the service list, unique within its policy
  name string
  // Description of the service list
  description string
  // Number of services in the list
  totalServices int
  // Names of the services the list contains
  services() []string
}

// ICMP application in a network firewall policy
//
// A named ICMP or ICMPv6 message type, and optionally a code, that security
// rules match traffic against. The `name` field selects the application as a
// rule references it.
private oci.networkFirewall.policy.application @defaults("name type icmpType") {
  // Name of the application, unique within its policy
  name string
  // Protocol the application describes
  //
  // One of ICMP or ICMP6.
  type string
  // Description of the application
  description string
  // ICMP message type
  icmpType int
  // ICMP message code (subtype)
  //
  // Null when the application matches every code of its type.
  icmpCode int
}

// Named group of applications in a network firewall policy
//
// A set of applications referenced by security rules in place of listing each
// application individually.
private oci.networkFirewall.policy.applicationGroup @defaults("name totalApps") {
  // Name of the application group, unique within its policy
  name string
  // Description of the application group
  description string
  // Number of applications in the group
  totalApps int
  // Names of the applications the group contains
  applications() []string
}

// Secret mapped into a network firewall policy for TLS inspection
//
// The certificate and private key material the firewall uses to decrypt
// traffic, held in Vault and referenced here by name. Every decryption rule
// resolves to one of these, so this is where the trust placed in the firewall
// becomes concrete.
private oci.networkFirewall.policy.mappedSecret @defaults("name type source") {
  // Name of the mapped secret, unique within its policy
  name string
  // What the secret is used for
  //
  // One of SSL_INBOUND_INSPECTION (inbound inspection) or SSL_FORWARD_PROXY
  // (outbound forward-proxy inspection).
  type string
  // Where the secret is stored
  //
  // OCI_VAULT for secrets held in the Vault service.
  source string
  // Vault secret holding the certificate and key
  vaultSecret() oci.vault.secret
  // Version of the vault secret the policy reads
  vaultSecretVersionNumber() int
}

// NAT rule in a network firewall policy
//
// Source network address translation applied to matching traffic. Because a
// NAT rule changes the address a destination observes, it also changes which
// address-based controls downstream see, so it belongs in any reading of what
// a firewall policy actually does.
private oci.networkFirewall.policy.natRule @defaults("name action priorityOrder") {
  // Name of the NAT rule, unique within its policy
  name string
  // Translation applied to matching traffic
  //
  // DIPP_SRC_NAT performs dynamic-IP-and-port source translation.
  action string
  // Position in the policy's evaluation order
  priorityOrder int
  // Rule description
  description string
  // Traffic the rule matches, by list name
  //
  // Keys are `sourceAddress`, `destinationAddress` and `service`, each holding
  // the names of the lists the rule evaluates against. An absent or empty key
  // matches anything.
  condition dict
}

// Tunnel inspection rule in a network firewall policy
//
// Decides which encapsulated traffic is unwrapped so the policy's other rules
// can see inside it. Traffic in a tunnel that no rule matches passes through
// uninspected, so the absence of a rule is itself the finding.
private oci.networkFirewall.policy.tunnelInspectionRule @defaults("name protocol action priorityOrder") {
  // Name of the tunnel inspection rule, unique within its policy
  name string
  // Encapsulation the rule inspects
  //
  // VXLAN is the only protocol currently supported.
  protocol string
  // Action taken on matching traffic
  //
  // INSPECT unwraps and inspects the traffic; INSPECT_AND_CAPTURE_LOG also
  // records it.
  action string
  // Position in the policy's evaluation order
  priorityOrder int
  // Rule description
  description string
  // Traffic the rule matches, by list name
  //
  // Keys are `sourceAddress` and `destinationAddress`, each holding the names
  // of the address lists the rule evaluates against.
  condition dict
  // Inspection profile applied to matching traffic
  //
  // Contains `mustReturnTrafficToSource`, which decides whether inspected
  // traffic is returned along the path it arrived on.
  profile dict
}

// Network firewall policy decryption rule
//
// Single rule deciding which TLS sessions a network firewall policy decrypts
// for inspection. `action` selects DECRYPT or NO_DECRYPT, and
// `decryptionProfile` names the profile whose controls apply to the decrypted
// session.
private oci.networkFirewall.policy.decryptionRule @defaults("name action decryptionProfile priorityOrder") {
  // Unique name of the rule within its policy
  name string
  // Whether matching sessions are decrypted (DECRYPT, NO_DECRYPT)
  action string
  // Name of the decryption profile applied to matching sessions
  decryptionProfile string
  // Name of the mapped secret holding the certificate used to decrypt
  secret string
  // Position in the policy's evaluation order
  priorityOrder int
  // Rule description
  description string
}

private oci.networkFirewall.policy.decryptionProfile @defaults("name type isUnsupportedVersionBlocked isUnsupportedCipherBlocked") {
  // Unique name of the decryption profile within its policy
  name string
  // Inspection type
  //
  // One of SSL_FORWARD_PROXY (outbound forward-proxy inspection) or
  // SSL_INBOUND_INSPECTION (inbound inspection).
  type string
  // Description of the decryption profile
  description string
  // Whether sessions are blocked when the SSL/TLS version is not supported
  //
  // Applies to both forward-proxy and inbound-inspection profiles.
  isUnsupportedVersionBlocked bool
  // Whether sessions are blocked when the SSL/TLS cipher suite is not supported
  //
  // Applies to both forward-proxy and inbound-inspection profiles.
  isUnsupportedCipherBlocked bool
  // Whether sessions are blocked when the firewall is temporarily unable to decrypt their traffic
  //
  // Applies to both forward-proxy and inbound-inspection profiles.
  isOutOfCapacityBlocked bool
  // Whether sessions are blocked when the server certificate is expired
  //
  // Forward-proxy profiles only; null for inbound-inspection profiles.
  isExpiredCertificateBlocked bool
  // Whether sessions are blocked when the server certificate is issued by an untrusted certificate authority
  //
  // Forward-proxy profiles only; null for inbound-inspection profiles.
  isUntrustedIssuerBlocked bool
  // Whether sessions are blocked when the certificate revocation status check times out
  //
  // Forward-proxy profiles only; null for inbound-inspection profiles.
  isRevocationStatusTimeoutBlocked bool
  // Whether sessions are blocked when the certificate revocation status is unknown
  //
  // Forward-proxy profiles only; null for inbound-inspection profiles.
  isUnknownRevocationStatusBlocked bool
  // Whether sessions are blocked when the server certificate uses restricted extensions
  //
  // Blocks certificates using extensions other than key usage and
  // extended key usage. Forward-proxy profiles only; null for
  // inbound-inspection profiles.
  areCertificateExtensionsRestricted bool
  // Whether a Subject Alternative Name is appended to the impersonating certificate when the server certificate is missing one
  //
  // Forward-proxy profiles only; null for inbound-inspection profiles.
  isAutoIncludeAltName bool
}

// Oracle Cloud Infrastructure (OCI) Container Engine for Kubernetes
//
// Managed Kubernetes deployments in the tenancy. The `clusters` field
// enumerates every OKE cluster, each surfacing the Kubernetes version,
// cluster type (BASIC_CLUSTER or ENHANCED_CLUSTER), the VCN it is
// deployed in, the KMS key used for secret encryption, the
// public/private API endpoint configuration, image-policy
// verification, the available upgrade versions, the legacy Pod
// Security Policy admission setting, and the node pools running
// workloads inside the cluster.
oci.oke {
  // OKE clusters
  clusters() []oci.oke.cluster
}

// Oracle Cloud Infrastructure (OCI) OKE cluster
//
// Managed Kubernetes cluster and its control-plane security posture:
// the Kubernetes version and cluster type, the VCN it runs in, the KMS
// key protecting Kubernetes secrets, whether the API server endpoint
// is publicly reachable, whether image signature verification is
// enforced, and whether the deprecated Pod Security Policy admission
// controller is still enabled. Worker nodes running inside the cluster
// are reached through nodePools.
private oci.oke.cluster @defaults("name") {
  // Cluster OCID
  id string
  // Cluster name
  name string
  // Compartment containing the cluster
  compartment() oci.compartment
  // Kubernetes version
  kubernetesVersion string
  // Cluster type (BASIC_CLUSTER, ENHANCED_CLUSTER)
  type string
  // VCN the cluster is deployed in
  vcn() oci.network.vcn
  // KMS key for secret encryption
  kmsKey() oci.kms.key
  // Whether the Kubernetes API endpoint is publicly accessible
  isPublicEndpointEnabled bool
  // Public API endpoint URL
  publicEndpoint string
  // Private API endpoint URL
  privateEndpoint string
  // Whether image signature verification policy is enabled
  isImagePolicyEnabled bool
  // Keys whose signatures the image policy accepts
  //
  // Empty when isImagePolicyEnabled is false, and also when the policy is on
  // but names no key, which admits every image.
  imagePolicyKmsKeys() []oci.kms.key
  // Available Kubernetes version upgrades
  availableKubernetesUpgrades []string
  // Whether Pod Security Policy admission controller is enabled
  isPodSecurityPolicyEnabled bool
  // Whether the Kubernetes Dashboard add-on is deployed
  //
  // The dashboard is a cluster-wide administrative console; where it is
  // reachable it becomes a route to the cluster that does not pass through
  // kubectl authentication.
  isKubernetesDashboardEnabled bool
  // Whether the Tiller add-on is deployed
  //
  // Tiller is the Helm 2 in-cluster component. It runs with broad cluster
  // permissions and is unsupported upstream.
  isTillerEnabled bool
  // CIDR block pods are addressed from
  //
  // Empty on a cluster using native VCN pod networking, where pods take
  // addresses from the subnets rather than from an overlay range.
  podsCidr string
  // CIDR block Kubernetes services are addressed from
  servicesCidr string
  // IP address families the cluster serves (IPv4, IPv6)
  ipFamilies []string
  // Pod networking types available on the cluster
  //
  // Each entry is OCI_VCN_IP_NATIVE, where pods take VCN addresses and are
  // governed by network security groups directly, or FLANNEL_OVERLAY, where
  // pod traffic is tunnelled and invisible to VCN rules.
  podNetworkTypes []string
  // Subnets the cluster provisions service load balancers into
  serviceLbSubnets() []oci.network.subnet
  // Subnet hosting the Kubernetes API endpoint
  //
  // Null on a cluster that predates native VCN endpoint configuration.
  endpointSubnet() oci.network.subnet
  // Network security groups applied to the Kubernetes API endpoint
  //
  // Empty when no group is attached, in which case the endpoint is governed
  // only by its subnet's security lists.
  endpointSecurityGroups() []oci.network.networkSecurityGroup
  // Whether the API server accepts OpenID Connect tokens
  //
  // When enabled, an external identity provider can mint tokens the cluster
  // accepts, so cluster access is no longer bounded by OCI IAM alone.
  isOpenIdConnectAuthEnabled bool
  // Issuer URL the API server accepts OpenID Connect tokens from
  openIdConnectIssuerUrl string
  // Client identifier the API server requires in OpenID Connect tokens
  openIdConnectClientId string
  // Token claim the API server reads the username from
  openIdConnectUsernameClaim string
  // Token claim the API server reads group membership from
  //
  // Group membership drives role binding, so the claim named here decides
  // which cluster roles a federated user receives.
  openIdConnectGroupsClaim string
  // Signing algorithms the API server accepts on OpenID Connect tokens
  openIdConnectSigningAlgorithms []string
  // Discovery endpoint serving the cluster's own OpenID Connect metadata
  //
  // Used by workloads federating to OCI with a service account token. Empty
  // when the cluster publishes no discovery document.
  openIdConnectDiscoveryEndpoint() string
  // Lifecycle state (e.g., CREATING, ACTIVE, FAILED, DELETING, DELETED, UPDATING)
  state string
  // Detail behind the lifecycle state
  //
  // Empty on a healthy cluster.
  lifecycleDetails string
  // User that created the cluster
  //
  // Null when the cluster was created by a principal that is not an IAM user,
  // for example an instance principal or a federated identity.
  createdByUser() oci.identity.user
  // Last time the cluster was updated
  timeUpdated time
  // Expiry of the cluster's own credentials
  //
  // The point at which the control plane certificates the cluster issued to
  // itself stop being valid. Null when the cluster reports no expiry.
  timeCredentialExpiration time
  // Node pools in the cluster
  nodePools() []oci.oke.nodePool
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // Zero Trust Packet Routing security attributes on the cluster API endpoint
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
}

// Oracle Cloud Infrastructure (OCI) OKE node pool
//
// Group of worker nodes within an OKE cluster, sharing a compute
// shape, node image, and Kubernetes version. Covers the network
// placement of the nodes (subnets and attached network security
// groups) and the SSH public key provisioned onto them, which should
// be empty so nodes are not directly reachable by operators.
private oci.oke.nodePool @defaults("name") {
  // Node pool OCID
  id string
  // Node pool name
  name string
  // Compartment containing the node pool
  compartment() oci.compartment
  // Kubernetes version on nodes
  kubernetesVersion string
  // Compute shape for nodes
  nodeShape string
  // Cluster this node pool belongs to
  cluster() oci.oke.cluster
  // Node shape configuration
  //
  // Compute sizing applied to each node when the shape is flexible.
  // The `ocpus` key holds the number of OCPUs and `memoryInGBs` the
  // memory in gigabytes.
  nodeShapeConfig dict
  // Image the nodes boot from
  //
  // Resolved from the node pool's node source, so it is populated on node
  // pools provisioned by current tooling. Null when the node pool boots from
  // a source that is not an image.
  nodeImage() oci.compute.image
  // Boot volume size in GB for each node
  //
  // Null when the node pool does not override the image's default size.
  bootVolumeSizeInGBs int
  // SSH public key on nodes (should be empty for security)
  sshPublicKey string
  // Subnets where Kubernetes nodes are placed (per-placement-config on newer node pools; node-pool-level fallback on older ones)
  subnets() []oci.network.subnet
  // Network Security Groups attached to the node VNICs (empty when nodes are governed only by subnet security lists)
  networkSecurityGroups() []oci.network.networkSecurityGroup
  // Number of nodes the pool maintains
  size int
  // Whether traffic between the nodes and their block volumes is encrypted
  //
  // Without it, volume traffic crosses the network in the clear even though
  // the volume itself is encrypted at rest.
  isPvEncryptionInTransitEnabled bool
  // Key encrypting the node boot and block volumes
  //
  // Null when the nodes use Oracle-managed encryption rather than a key from
  // the tenancy's vault.
  nodeKmsKey() oci.kms.key
  // Labels applied to every node as it joins the cluster
  //
  // Workloads select nodes by these, so a label here is what places a pod on
  // this pool.
  initialNodeLabels map[string]string
  // Grace period before a node being removed is force-drained
  //
  // An ISO 8601 duration, for example PT60M. Empty when the pool leaves the
  // service default in place.
  evictionGraceDuration string
  // Whether a node is deleted once its grace period expires even if pods remain
  isForceDeleteAfterGraceDuration bool
  // Whether the pool replaces its nodes in place when its configuration changes
  //
  // Node cycling is how a pool picks up a new Kubernetes version or a patched
  // image without being recreated. Where it is off, nodes keep running the
  // image they launched with.
  isNodeCyclingEnabled bool
  // Nodes the pool may take out of service at once while cycling
  //
  // A count or a percentage, for example `1` or `25%`.
  cyclingMaximumUnavailable string
  // Extra nodes the pool may add above its size while cycling
  //
  // A count or a percentage, for example `1` or `25%`.
  cyclingMaximumSurge string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED, NEEDS_ATTENTION)
  state string
  // Detail behind the lifecycle state
  //
  // Empty on a healthy node pool.
  lifecycleDetails string
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Web Application Firewall
//
// L7 web-application firewall deployments in the tenancy. The
// `firewalls` field lists every WAF instance with the load balancer
// it fronts and the attached `policy`, and `policies` lists the
// reusable rule sets that drive request-rate limiting, signature
// inspection, IP allow/deny lists, and bot management.
oci.waf {
  // Web Application Firewalls
  firewalls() []oci.waf.firewall
  // Web Application Firewall policies
  policies() []oci.waf.policy
}

// Oracle Cloud Infrastructure (OCI) Web Application Firewall
private oci.waf.firewall @defaults("name") {
  // WAF OCID
  id string
  // Display name
  name string
  // Compartment containing the firewall
  compartment() oci.compartment
  // WAF policy attached to this firewall
  policy() oci.waf.policy
  // Load balancer this WAF is attached to
  loadBalancer() oci.loadBalancer.loadBalancer
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Web Application Firewall policy
private oci.waf.policy @defaults("name") {
  // WAF policy OCID
  id string
  // Display name
  name string
  // Compartment containing the policy
  compartment() oci.compartment
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Functions
//
// Serverless functions across the tenancy, organized into function
// applications. Each application in `applications` sets the shape,
// subnet placement, network security groups, environment
// configuration, syslog destination, and image-signature
// verification, and holds the `functions` that run under it with
// their image, memory, timeout, invoke endpoint, and tracing
// settings.
oci.functions {
  // Function applications
  applications() []oci.functions.application
}

// Oracle Cloud Infrastructure (OCI) Functions application
//
// Function application grouping related serverless functions under a
// shared shape, subnet placement, network security groups, and
// environment configuration. The application is the security and
// networking boundary for OCI Functions: `imagePolicyConfig` controls
// whether function images must pass signature verification, `subnets`
// and `networkSecurityGroups` place function invocations on the
// network, and `syslogUrl` routes function logs to an external
// collector.
private oci.functions.application @defaults("name") {
  // Application OCID
  id string
  // Application display name
  name string
  // Compartment containing the application
  compartment() oci.compartment
  // Lifecycle state (CREATING, ACTIVE, INACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Application shape (GENERIC_X86, GENERIC_ARM, GENERIC_X86_ARM)
  shape string
  // Distributed tracing configuration
  //
  // Application Performance Monitoring (APM) trace settings, with keys
  // `isEnabled` (bool, whether tracing is enabled) and `domainId`
  // (OCID of the APM domain that traces are sent to).
  traceConfig dict
  // Image signature verification policy
  //
  // Signed-image enforcement for the application, with keys
  // `isPolicyEnabled` (bool, whether functions must use signed images)
  // and `keyDetails` (array of objects, each carrying `kmsKeyId`, the
  // OCID of a KMS key trusted to verify image signatures).
  imagePolicyConfig dict
  // Application creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // Subnets where functions run
  subnets() []oci.network.subnet
  // Network Security Groups applied to the application
  networkSecurityGroups() []oci.network.networkSecurityGroup
  // Application environment configuration variables
  config() map[string]string
  // Syslog URL for logging (tcp://host:port)
  syslogUrl() string
  // Functions in this application
  functions() []oci.functions.function
}

// Oracle Cloud Infrastructure (OCI) serverless function
//
// Single serverless function within an application, addressable by
// its `invokeEndpoint`. Each function pins the Docker `image` and
// `imageDigest` it runs, its `shape`, memory and timeout limits, and
// its tracing configuration, so you can audit which image a function
// executes and whether its resource limits and observability settings
// meet policy.
private oci.functions.function @defaults("name") {
  // Function OCID
  id string
  // Function display name
  name string
  // Compartment containing the function
  compartment() oci.compartment
  // Parent application OCID
  applicationId string
  // Lifecycle state (CREATING, ACTIVE, INACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Docker image URL for the function
  image string
  // Image digest (sha256)
  imageDigest string
  // Function shape (GENERIC_X86, GENERIC_ARM, GENERIC_X86_ARM)
  shape string
  // Maximum memory in MBs
  memoryInMBs int
  // Execution timeout in seconds
  timeoutInSeconds int
  // Base URL for function invocation
  invokeEndpoint string
  // Distributed tracing configuration
  //
  // Application Performance Monitoring (APM) trace settings, with a
  // single key `isEnabled` (bool, whether tracing is enabled for this
  // function).
  traceConfig dict
  // Function creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // Function environment configuration variables
  config() map[string]string
}

// Oracle Cloud Infrastructure (OCI) Container Instances
//
// Serverless container deployments across the tenancy. Each container
// instance in `instances` surfaces its compute shape, CPU and memory
// allocation, fault domain, container restart policy, graceful shutdown
// timeout, lifecycle state, and the containers running inside it.
oci.containerInstances {
  // Container instances
  instances() []oci.containerInstances.instance
}

// Oracle Cloud Infrastructure (OCI) container instance (serverless container group)
//
// Serverless group of one or more containers that share a compute shape
// and lifecycle. The instance records its shape and OCPU/memory
// allocation, fault domain, container restart policy, graceful shutdown
// timeout, attached volume count, and lifecycle state, and lists the
// `containers` running inside it. Useful for auditing which serverless
// workloads run in a compartment and how they are sized and restarted.
private oci.containerInstances.instance @defaults("name") {
  // Container instance OCID
  id string
  // Display name
  name string
  // Compartment containing the instance
  compartment() oci.compartment
  // Availability domain
  availabilityDomain string
  // Lifecycle state (CREATING, ACTIVE, INACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Compute shape
  shape string
  // Shape allocation
  //
  // OCPU and memory sizing for the instance, with keys `ocpus`,
  // `memoryInGBs`, `processorDescription`, and
  // `networkingBandwidthInGbps`.
  shapeConfig dict
  // Number of containers
  containerCount int
  // Container restart policy (ALWAYS, NEVER, ON_FAILURE)
  containerRestartPolicy string
  // Fault domain
  faultDomain string
  // Graceful shutdown timeout in seconds
  gracefulShutdownTimeoutInSeconds int
  // Number of attached volumes
  volumeCount int
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // Containers running on this instance
  containers() []oci.containerInstances.container
}

// Oracle Cloud Infrastructure (OCI) container within a container instance
//
// Single container running inside a container instance, identified by
// the image it runs (`imageUrl`), its CPU and memory limits, and whether
// it can reach OCI services through resource principal authentication
// (`isResourcePrincipalDisabled`). Useful for auditing the images
// deployed to serverless container workloads and the privileges granted
// to them.
private oci.containerInstances.container @defaults("name") {
  // Container OCID
  id string
  // Display name
  name string
  // Compartment containing the container
  compartment() oci.compartment
  // Availability domain
  availabilityDomain string
  // Lifecycle state (CREATING, ACTIVE, INACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Parent container instance OCID
  containerInstanceId string
  // Docker image URL
  imageUrl string
  // Whether resource principal access is disabled
  isResourcePrincipalDisabled bool
  // Resource limits
  //
  // Per-container CPU and memory caps, with keys `vcpusLimit`
  // (fractional vCPUs) and `memoryLimitInGBs`. Absent when the container
  // inherits the instance defaults.
  resourceConfig dict
  // UID the container process runs as
  //
  // Null when the container declares no security context, in which case the
  // effective user comes from the image. 0 means the process runs as root.
  runAsUser int
  // GID the container process runs as
  runAsGroup int
  // Whether the container is checked for running as a non-root user
  //
  // False also when the container declares no security context, since an
  // unchecked container is not protected either way.
  isNonRootUserCheckEnabled bool
  // Whether the container's root filesystem is mounted read-only
  isRootFileSystemReadonly bool
  // Linux capabilities added to the container beyond the default set
  //
  // Capabilities granted on top of the runtime default, for example
  // CAP_NET_ADMIN or CAP_SYS_ADMIN. A non-empty list widens what the container
  // can do on the host.
  addedCapabilities []string
  // Linux capabilities dropped from the container's default set
  droppedCapabilities []string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Database
//
// Database deployments and their backups across the tenancy. The
// dbSystems field lists VM and bare-metal Oracle Database systems
// (Oracle Database on virtual machines or bare metal),
// autonomousDatabases lists serverless and dedicated Autonomous
// Database instances, backups lists the manual and automatic backups
// taken from VM/BM databases, and autonomousDatabaseBackups lists
// backups belonging to Autonomous Databases. Each backup surfaces
// TDE/KMS encryption, retention, destination type, and lifecycle
// state, making this the entry point for auditing database
// encryption and recovery posture.
oci.database {
  // Database systems (VM/BM)
  dbSystems() []oci.database.dbSystem
  // Autonomous databases
  autonomousDatabases() []oci.database.autonomousDatabase
  // Database backups (VM/BM database backups across all regions)
  backups() []oci.database.backup
  // Autonomous database backups across all regions
  autonomousDatabaseBackups() []oci.database.autonomousDatabaseBackup
}

// Oracle Cloud Infrastructure (OCI) database backup (for VM/BM databases)
private oci.database.backup @defaults("name") {
  // Backup OCID
  id string
  // Backup display name
  name string
  // Compartment containing the backup
  compartment() oci.compartment
  // OCID of the source database (not the DB system)
  databaseId string
  // Availability domain where the backup is stored
  availabilityDomain string
  // Backup type (INCREMENTAL, FULL, VIRTUAL_FULL)
  type string
  // Backup destination (OBJECT_STORE, DBRS, AWS_S3)
  backupDestinationType string
  // Size of the source database at backup time, in GBs
  databaseSizeInGBs float
  // Database edition
  //
  // One of: STANDARD_EDITION, ENTERPRISE_EDITION, ENTERPRISE_EDITION_HIGH_PERFORMANCE, ENTERPRISE_EDITION_EXTREME_PERFORMANCE, ENTERPRISE_EDITION_DEVELOPER.
  databaseEdition string
  // Database version used by the source database
  version string
  // Shape of the source database
  shape string
  // KMS key used for TDE encryption of the backup
  kmsKey() oci.kms.key
  // KMS vault used for TDE encryption
  kmsVault() oci.kms.vault
  // Whether Oracle Managed Keys is required to restore the backup
  isUsingOracleManagedKeys bool
  // Retention period in days (long-term backups only)
  retentionPeriodInDays int
  // Retention period in years (long-term backups only)
  retentionPeriodInYears int
  // Scheduled expiration time for long-term backups
  timeExpiryScheduled time
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED, RESTORING, UPDATING, CANCELING, CANCELED)
  state string
  // Backup start time
  timeStarted time
  // Backup end time
  timeEnded time
}

// Oracle Cloud Infrastructure (OCI) autonomous database backup
private oci.database.autonomousDatabaseBackup @defaults("name") {
  // Backup OCID
  id string
  // Backup display name
  name string
  // Compartment containing the autonomous database backup
  compartment() oci.compartment
  // Autonomous database the backup belongs to
  autonomousDatabase() oci.database.autonomousDatabase
  // Backup type (INCREMENTAL, FULL, LONGTERM, VIRTUAL_FULL, CUMULATIVE_INCREMENTAL, ROLL_FORWARD_IMAGE_COPY)
  type string
  // Whether the backup was created automatically
  isAutomatic bool
  // Whether the backup can be used to restore the source database
  isRestorable bool
  // Retention period in days (long-term backups only)
  retentionPeriodInDays int
  // Timestamp until when the backup will be available
  timeAvailableTill time
  // Size of the database at backup time, in TBs
  databaseSizeInTBs float
  // Backup size in TBs
  sizeInTBs float
  // Autonomous database version
  dbVersion string
  // Infrastructure type (CLOUD, CLOUD_AT_CUSTOMER)
  infrastructureType string
  // KMS key used for encryption
  kmsKey() oci.kms.key
  // KMS vault used for encryption
  kmsVault() oci.kms.vault
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED, UPDATING)
  state string
  // Backup start time
  timeStarted time
  // Backup end time
  timeEnded time
}

// Oracle Cloud Infrastructure (OCI) database system (VM/BM)
private oci.database.dbSystem @defaults("name") {
  // DB system OCID
  id string
  // Display name
  name string
  // Compartment containing the db system
  compartment() oci.compartment
  // Availability domain
  availabilityDomain string
  // Compute shape
  shape string
  // Database edition
  //
  // One of STANDARD_EDITION, ENTERPRISE_EDITION, ENTERPRISE_EDITION_HIGH_PERFORMANCE, ENTERPRISE_EDITION_EXTREME_PERFORMANCE, or ENTERPRISE_EDITION_DEVELOPER.
  databaseEdition string
  // Disk redundancy (HIGH, NORMAL)
  diskRedundancy string
  // Hostname of the database system
  hostname string
  // DNS domain name configured on the database system
  domain string
  // Database listener port (e.g., 1521)
  listenerPort int
  // Single Client Access Name (SCAN) DNS name for the database system
  scanDnsName string
  // Source DB system this DB system was created from, when created from another DB system
  sourceDbSystem() oci.database.dbSystem
  // Oracle Database version
  version string
  // Number of CPU cores
  cpuCoreCount int
  // Number of nodes
  nodeCount int
  // Data storage size in GBs
  dataStorageSizeInGBs int
  // License model (LICENSE_INCLUDED, BRING_YOUR_OWN_LICENSE)
  licenseModel string
  // KMS key for encryption
  kmsKey() oci.kms.key
  // Subnet the DB system is in
  subnet() oci.network.subnet
  // Network Security Groups attached to the DB system
  securityGroups() []oci.network.networkSecurityGroup
  // Network Security Groups attached to the backup network
  backupSecurityGroups() []oci.network.networkSecurityGroup
  // Subnet carrying the DB system's backup traffic
  //
  // Null when the DB system runs a single network rather than separating
  // backup traffic onto its own subnet.
  backupSubnet() oci.network.subnet
  // Public keys authorized to sign in to the DB system hosts over SSH
  //
  // Every key here reaches the database hosts directly, outside the database
  // authentication that governs the data. Empty when no key was supplied at
  // launch.
  sshPublicKeys []string
  // Fault domains the DB system nodes are placed across
  faultDomains []string
  // Operating system version on the DB system hosts
  osVersion string
  // Time zone configured on the DB system
  timeZone string
  // Cluster name for an Exadata or RAC DB system
  //
  // Empty on a single-node DB system.
  clusterName string
  // How storage is managed (ASM, LVM)
  //
  // Empty when the DB system reports no storage management option.
  storageManagement string
  // Whether Oracle collects diagnostic events from the DB system
  isDiagnosticsEventsEnabled bool
  // Whether Oracle collects health metrics from the DB system
  isHealthMonitoringEnabled bool
  // Whether Oracle collects incident logs and trace files from the DB system
  //
  // Incident logs can carry query text and payload fragments, so enabling
  // collection sends database content to Oracle support tooling.
  isIncidentLogsEnabled bool
  // Zero Trust Packet Routing security attributes applied to the DB system
  //
  // Each key is a security attribute namespace and each value maps that
  // namespace's attribute names to this DB system's values.
  securityAttributes map[string]dict
  // Security attributes applied to this resource, resolved
  //
  // Each labelling as a row carrying its namespace, its definition and whether
  // that namespace enforces or only audits.
  appliedSecurityAttributes() []oci.securityAttributes.applied
  // Patching and maintenance schedule
  //
  // Keyed by `preference` (NO_PREFERENCE or CUSTOM_PREFERENCE),
  // `patchingMode` (ROLLING or NONROLLING), `isMonthlyPatchingEnabled`,
  // `isCustomActionTimeoutEnabled` with its `customActionTimeoutInMins`,
  // and the `months`, `weeksOfMonth`, `daysOfWeek`, and `hoursOfDay` that
  // schedule the window.
  maintenanceWindow dict
  // Lifecycle state
  //
  // For example: PROVISIONING, AVAILABLE, UPDATING, TERMINATING, TERMINATED, FAILED, MIGRATED, MAINTENANCE_IN_PROGRESS, NEEDS_ATTENTION, UPGRADING.
  state string
  // Detail behind the lifecycle state
  //
  // Empty on a healthy DB system.
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) autonomous database
private oci.database.autonomousDatabase @defaults("name") {
  // Autonomous database OCID
  id string
  // Display name
  name string
  // Compartment containing the autonomous database
  compartment() oci.compartment
  // Database name
  dbName string
  // Source Autonomous Database this database was provisioned or cloned from, when set
  sourceDatabase() oci.database.autonomousDatabase
  // Whether this database is a refreshable clone of another Autonomous Database
  isRefreshableClone bool
  // Database version
  dbVersion string
  // Database workload type (OLTP, DW, AJD, APEX, LH)
  dbWorkload string
  // Whether this is a dedicated Exadata infrastructure
  isDedicated bool
  // Whether this is a free-tier database
  isFreeTier bool
  // Number of CPU cores
  cpuCoreCount int
  // Data storage size in TBs
  dataStorageSizeInTBs int
  // Whether mTLS connection is required
  isMtlsConnectionRequired bool
  // Whether access control is enabled
  isAccessControlEnabled bool
  // Allowed client IP addresses (ACL)
  whitelistedIps []string
  // Whether the database is reachable from the public internet
  //
  // True when the database has a public endpoint (no private endpoint) and
  // either access control is disabled or its allow-list admits any address
  // (0.0.0.0/0 or 0.0.0.0). mTLS may still be required to connect, but the
  // listener itself is reachable from the internet.
  internetReachable() bool
  // Allowed client IP addresses for the Data Guard standby (ACL)
  //
  // The standby's own access control list. When it diverges from
  // whitelistedIps the standby may be reachable from networks the primary
  // blocks, so compare the two when auditing disaster-recovery exposure.
  standbyWhitelistedIps []string
  // Whether auto-scaling is enabled
  isAutoScalingEnabled bool
  // Whether local Data Guard is enabled
  isLocalDataGuardEnabled bool
  // Whether cross-region (remote) Autonomous Data Guard is enabled
  isRemoteDataGuardEnabled bool
  // Whether Autonomous Data Guard is enabled for the database
  isDataGuardEnabled bool
  // Retention period in days for long-term backups
  backupRetentionPeriodInDays int
  // Whether backup retention is locked
  //
  // When true, backups cannot be deleted before their retention period
  // elapses, protecting recovery points against tampering or premature
  // deletion.
  isBackupRetentionLocked bool
  // Oracle Data Safe status (REGISTERED, NOT_REGISTERED, REGISTERING, DEREGISTERING, FAILED)
  dataSafeStatus string
  // Open mode (READ_ONLY, READ_WRITE)
  openMode string
  // Permission level (RESTRICTED, UNRESTRICTED)
  permissionLevel string
  // License model (LICENSE_INCLUDED, BRING_YOUR_OWN_LICENSE)
  licenseModel string
  // KMS key for encryption
  kmsKey() oci.kms.key
  // KMS vault for encryption
  kmsVault() oci.kms.vault
  // Subnet for private endpoint
  subnet() oci.network.subnet
  // Network Security Groups attached to the autonomous database
  securityGroups() []oci.network.networkSecurityGroup
  // Private endpoint IP address
  privateEndpointIp string
  // Private endpoint label
  privateEndpointLabel string
  // Connection URLs for management and user interfaces
  //
  // Covers SQL Developer Web, APEX, GraphStudio, ORDS, MongoDB, Machine Learning Notebook/User Management, and Database Transforms. Populated when the database is publicly accessible or reachable via an allowed private endpoint.
  connectionUrls dict
  // Connection URLs reachable over the public internet
  //
  // The same management and user interfaces as connectionUrls, but the
  // copies served on the database's public endpoint. Populated only when
  // the database exposes a public endpoint, so a non-empty value means
  // those interfaces are reachable from the internet.
  publicConnectionUrls dict
  // Lifecycle state
  //
  // For example: PROVISIONING, AVAILABLE, STOPPING, STOPPED, STARTING, TERMINATING, TERMINATED, UNAVAILABLE, RESTORE_IN_PROGRESS, RESTORE_FAILED, BACKUP_IN_PROGRESS, SCALE_IN_PROGRESS, AVAILABLE_NEEDS_ATTENTION, UPDATING, MAINTENANCE_IN_PROGRESS, RESTARTING, RECREATING, ROLE_CHANGE_IN_PROGRESS, UPGRADING, INACCESSIBLE, STANDBY.
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
  // Backups taken of this autonomous database
  backups() []oci.database.autonomousDatabaseBackup
}

// Oracle Cloud Infrastructure (OCI) API Gateway
//
// API Gateway resources across the tenancy: the regional gateway
// appliances, the deployments (route configurations) bound to them,
// and the TLS certificates the service manages. The `gateways` field
// lists each gateway with its endpoint type, IP mode, hostname,
// response-cache setting, attached subnet, and certificate bindings;
// `deployments` lists the route configurations across all gateways;
// and `certificates` lists the API-gateway-managed TLS certs, which
// are separate from the tenancy-wide `oci.certificates.certificate`
// service.
oci.apigateway {
  // API gateways in the tenancy
  gateways() []oci.apigateway.gateway
  // API deployments across all gateways
  deployments() []oci.apigateway.deployment
  // API gateway TLS certificates (in the apigateway service, distinct from oci.certificates.certificate)
  certificates() []oci.apigateway.certificate
}

// Oracle Cloud Infrastructure (OCI) API gateway (regional network appliance)
//
// A regional API gateway appliance that fronts backend services and
// terminates TLS. Fields expose its network placement (the `subnet` it
// runs in, attached `networkSecurityGroups`, and assigned
// `ipAddresses`), its exposure (`endpointType` of PUBLIC or PRIVATE and
// `hostname`), and its TLS material (`certificate` and `caBundleIds`),
// so you can audit whether a gateway is publicly reachable and how it
// is secured.
private oci.apigateway.gateway @defaults("name") {
  // Gateway OCID
  id string
  // Display name
  name string
  // Compartment containing the gateway
  compartment() oci.compartment
  // Endpoint type (PUBLIC, PRIVATE)
  endpointType string
  // IP addressing mode (IPV4, IPV6, DUAL_STACK)
  ipMode string
  // Hostname assigned to the gateway
  hostname string
  // Whether a response cache is configured (true for EXTERNAL_RESP_CACHE; false for NONE or unset)
  hasResponseCache() bool
  // IP addresses assigned to the gateway
  ipAddresses() []string
  // Subnet the gateway is deployed in
  subnet() oci.network.subnet
  // Network Security Groups attached to the gateway
  networkSecurityGroups() []oci.network.networkSecurityGroup
  // TLS certificate used by the gateway (from the apigateway certificates service)
  certificate() oci.apigateway.certificate
  // OCIDs of CA bundles (from the certificatesmanagement service or the apigateway certificates service) used by the gateway for TLS validation
  caBundleIds() []string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) API gateway deployment
//
// A route configuration bound to a gateway at a `pathPrefix` and
// reachable at `endpoint`. The deployment carries the request-policy
// controls that gate access to backend routes: `authenticationType`
// and `isAnonymousAccessAllowed` for auth, `jwtIssuers` and
// `jwtAudiences` for token validation, `mtlsIsVerifiedCertificateRequired`
// and `mtlsAllowedSans` for client-certificate checks, the CORS
// settings, and the rate-limit configuration, so you can audit whether
// a route is authenticated and how it is protected.
private oci.apigateway.deployment @defaults("name pathPrefix") {
  // Deployment OCID
  id string
  // Display name
  name string
  // Compartment containing the deployment
  compartment() oci.compartment
  // Parent gateway
  gateway() oci.apigateway.gateway
  // Path prefix on the gateway that this deployment is deployed to
  pathPrefix string
  // Endpoint URL to reach this deployment
  endpoint string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Authentication method configured for this deployment: NONE, TOKEN_AUTHENTICATION, JWT_AUTHENTICATION, or CUSTOM_AUTHENTICATION
  authenticationType() string
  // Whether anonymous (unauthenticated) access is allowed
  isAnonymousAccessAllowed() bool
  // JWT audiences accepted when authenticationType is JWT_AUTHENTICATION
  jwtAudiences() []string
  // JWT issuers accepted when authenticationType is JWT_AUTHENTICATION
  jwtIssuers() []string
  // Whether client certificate verification is required on mTLS connections
  mtlsIsVerifiedCertificateRequired() bool
  // Allowed CNs or SANs for client certificates on mTLS connections
  mtlsAllowedSans() []string
  // Allowed origins for CORS (empty if CORS is not configured)
  corsAllowedOrigins() []string
  // Whether CORS requests may include credentials
  corsAllowCredentials() bool
  // Maximum number of requests per second allowed (0 if rate limiting is not configured)
  rateLimitPerSecond() int
  // Rate limiting grouping key (e.g., CLIENT_IP); empty when not configured
  rateLimitKey() string
  // Whether dynamic authentication (multiple auth options selected by request) is configured
  hasDynamicAuthentication() bool
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) API gateway TLS certificate (distinct from oci.certificates.certificate; managed by the apigateway service)
//
// A TLS certificate managed by the API Gateway service and bound to
// gateways for client-facing TLS. The `subjectNames` field lists the
// CN and SANs the certificate secures and `timeNotValidAfter` its
// expiration, so you can audit which hostnames are covered and flag
// certificates nearing expiry. This is separate from the tenancy-wide
// `oci.certificates.certificate` PKI service.
private oci.apigateway.certificate @defaults("name") {
  // Certificate OCID
  id string
  // Display name
  name string
  // Compartment containing the certificate
  compartment() oci.compartment
  // Subject names secured by the certificate (CN plus SANs)
  subjectNames []string
  // Certificate expiration time
  timeNotValidAfter time
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Certificates Management
//
// PKI material managed by the tenancy's Certificates service. The
// `certificates` collection holds the managed TLS certificates, each
// surfacing the issuer authority, subject and SAN list, validity
// window, KMS-backed key spec, and configuration origin (issued by an
// internal CA, managed externally, or imported). The
// `certificateAuthorities` collection holds the root and subordinate
// CAs, and `caBundles` holds the trust bundles applications import to
// validate peer certificates. Query these to audit certificate
// expiry, key strength, auto-renewal coverage, and CA trust chains.
oci.certificates {
  // Managed TLS certificates
  certificates() []oci.certificates.certificate
  // Certificate authorities (root or subordinate)
  certificateAuthorities() []oci.certificates.certificateAuthority
  // CA bundles (collections of trusted CA certificates)
  caBundles() []oci.certificates.caBundle
}

// Oracle Cloud Infrastructure (OCI) managed TLS certificate
private oci.certificates.certificate @defaults("name") {
  // Certificate OCID
  id string
  // Certificate name
  name string
  // Certificate description
  description string
  // Compartment containing the certificate
  compartment() oci.compartment
  // Configuration origin (ISSUED_BY_INTERNAL_CA, MANAGED_EXTERNALLY_ISSUED_BY_INTERNAL_CA, IMPORTED)
  configType string
  // Issuing certificate authority (set when configType is ISSUED_BY_INTERNAL_CA or MANAGED_EXTERNALLY_ISSUED_BY_INTERNAL_CA)
  issuerCertificateAuthority() oci.certificates.certificateAuthority
  // Subject common name (CN)
  subject string
  // Subject alternative names (DNS/IP entries from the current certificate version)
  sans []string
  // Start of the certificate's validity period
  validityNotBefore time
  // End of the certificate's validity period
  validityNotAfter time
  // Key algorithm (RSA2048, RSA4096, ECDSA_P256, ECDSA_P384)
  keyAlgorithm string
  // Signature algorithm (e.g., SHA256_WITH_RSA, SHA384_WITH_RSA, SHA512_WITH_RSA, SHA256_WITH_ECDSA, SHA384_WITH_ECDSA, SHA512_WITH_ECDSA)
  signatureAlgorithm string
  // Certificate profile type (e.g., TLS_SERVER_OR_CLIENT, TLS_SERVER, TLS_CLIENT, TLS_CODE_SIGN)
  profileType string
  // Current certificate version number
  currentVersion int
  // Whether auto-renewal is enabled (true when a CERTIFICATE_RENEWAL_RULE is present)
  autoRenewalEnabled bool
  // Renewal interval (ISO 8601 duration, e.g., P365D); empty when auto-renewal is not configured
  renewalInterval string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) certificate authority
private oci.certificates.certificateAuthority @defaults("name") {
  // CA OCID
  id string
  // CA name
  name string
  // CA description
  description string
  // Compartment containing the certificate authority
  compartment() oci.compartment
  // CA kind inferred from configType (ROOT for ROOT_CA_*, SUBORDINATE for SUBORDINATE_CA_*)
  kind string
  // Configuration origin
  //
  // One of: ROOT_CA_GENERATED_INTERNALLY, SUBORDINATE_CA_ISSUED_BY_INTERNAL_CA, ROOT_CA_MANAGED_EXTERNALLY, SUBORDINATE_CA_MANAGED_INTERNALLY_ISSUED_BY_EXTERNAL_CA.
  configType string
  // Parent CA for subordinate CAs (null for root CAs)
  issuerCertificateAuthority() oci.certificates.certificateAuthority
  // Subject common name (CN)
  subject string
  // Start of the CA certificate's validity period (from the current version)
  validityNotBefore time
  // End of the CA certificate's validity period (from the current version)
  validityNotAfter time
  // Signing algorithm used by this CA to sign certificates (e.g., SHA256_WITH_RSA)
  signingAlgorithm string
  // KMS key used to encrypt this CA's private material
  kmsKey() oci.kms.key
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED, PENDING_DELETION, SCHEDULING_DELETION, CANCELLING_DELETION)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) CA bundle (collection of trusted CA certificates)
private oci.certificates.caBundle @defaults("name") {
  // CA bundle OCID
  id string
  // CA bundle name
  name string
  // CA bundle description
  description string
  // Compartment containing the ca bundle
  compartment() oci.compartment
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Cache
//
// OCI Cache deployments in the tenancy, the managed in-memory cache
// service. `clusters` returns every cache cluster, each exposing the
// engine (Redis or Valkey) and version, node count and node memory,
// the primary, replica, and discovery endpoints, the attached subnet
// and network security group bindings, lifecycle state, and cluster
// mode. Query it to inventory cache deployments and audit their
// engine versions and network placement.
oci.redis {
  // OCI Cache clusters
  clusters() []oci.redis.cluster
}

// Oracle Cloud Infrastructure (OCI) Cache cluster (Redis or Valkey)
//
// A single managed in-memory cache cluster, selected by its OCID with
// `oci.redis.cluster(id: "ocid1.rediscluster.oc1...")`. Surfaces the
// cache engine and version, the sharding mode and shard/node counts,
// per-node memory, the primary, replica, and discovery endpoints, the
// subnet and network security groups governing reachability, and the
// lifecycle state. Use it to audit which engine versions are running,
// how clusters are placed on the network, and which security groups
// control access.
private oci.redis.cluster @defaults("name softwareVersion state") {
  init(id? string)
  // Cluster OCID
  id string
  // Cluster display name
  name string
  // Compartment containing the cluster
  compartment() oci.compartment
  // Cache engine version (e.g., REDIS_7_0, VALKEY_7_2, VALKEY_8_1)
  softwareVersion string
  // Cluster mode (SHARDED or NONSHARDED)
  clusterMode string
  // Number of nodes per shard when sharded; total node count otherwise
  nodeCount int
  // Memory allocated to each node, in gigabytes
  nodeMemoryInGBs float
  // Number of shards (only set when clusterMode is SHARDED)
  shardCount int
  // Subnet the cluster is deployed in
  subnet() oci.network.subnet
  // Network security groups attached to the cluster
  networkSecurityGroups() []oci.network.networkSecurityGroup
  // FQDN of the primary node API endpoint
  primaryFqdn string
  // Private IP of the primary node API endpoint
  primaryEndpointIpAddress string
  // FQDN of the replica nodes API endpoint
  replicasFqdn string
  // Private IP of the replica nodes API endpoint
  replicasEndpointIpAddress string
  // FQDN of the sharded cluster discovery endpoint
  discoveryFqdn string
  // Private IP of the sharded cluster discovery endpoint
  discoveryEndpointIpAddress string
  // Cluster lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Data Safe
//
// Data Safe coverage across the tenancy: the databases registered for
// monitoring, the assessments that score their configuration and user
// risk, the data-discovery catalog, and the masking definitions applied
// during refresh, clone, or export. `targetDatabases` lists the
// registered databases; `securityAssessments` and `userAssessments`
// hold the latest configuration-posture and user-risk findings;
// `sensitiveDataModels` and `sensitiveTypes` make up the data-discovery
// catalog; `maskingPolicies` defines how sensitive columns are masked;
// and `configurations` reports per-region service enablement.
oci.dataSafe {
  // Per-region Data Safe service enablement records, with the URL, lifecycle state, and global settings of each region where Data Safe has been turned on
  configurations() []oci.dataSafe.configuration
  // Databases registered under Data Safe across every region and compartment in the tenancy
  targetDatabases() []oci.dataSafe.targetDatabase
  // Security assessments (configuration-posture findings) produced by Data Safe
  securityAssessments() []oci.dataSafe.securityAssessment
  // User assessments (user-risk findings) produced by Data Safe
  userAssessments() []oci.dataSafe.userAssessment
  // Sensitive data models: the per-target catalogs of sensitive columns produced by data discovery
  sensitiveDataModels() []oci.dataSafe.sensitiveDataModel
  // Sensitive type definitions (predefined Oracle types and tenant-defined types) used during data discovery
  sensitiveTypes() []oci.dataSafe.sensitiveType
  // Masking policies that define how columns are masked during a sensitive-data masking job
  maskingPolicies() []oci.dataSafe.maskingPolicy
}

// Oracle Data Safe service enablement configuration for a single region
//
// Per-region Data Safe configuration: whether Data Safe is turned on in
// this region, the service URL and lifecycle state, and the
// `globalSettings` block that controls offline retention, online
// retention, and paid-usage flags.
private oci.dataSafe.configuration @defaults("region isEnabled lifecycleState") {
  // OCI region key the configuration was retrieved from (e.g. `us-ashburn-1`)
  region string
  // Whether Data Safe is enabled in this region
  isEnabled bool
  // URL of the Data Safe service in this region
  url string
  // Compartment containing the configuration
  compartment() oci.compartment
  // Lifecycle state of Data Safe in this region (CREATING, UPDATING, ACTIVE, INACTIVE, DELETING, DELETED, FAILED)
  lifecycleState string
  // NAT Gateway IP address used by Data Safe to reach customer databases in this region
  natGatewayIpAddress string
  // Global settings
  //
  // Tenancy-wide Data Safe settings for this region, with keys
  // `isPaidUsage` (whether the deployment uses paid Data Safe features),
  // `onlineRetentionPeriod` (online retention in months), and
  // `offlineRetentionPeriod` (offline retention in months).
  globalSettings dict
  // Time Data Safe was enabled in this region
  enabledAt time
  // Free-form tags applied to the configuration
  freeformTags map[string]string
  // Defined tags applied to the configuration
  definedTags map[string]map[string]string
}

// Oracle Data Safe target database
//
// Single database registered with Data Safe for monitoring and
// assessment. `infrastructureType` distinguishes Oracle DB systems on
// Exadata, Autonomous DB, base DB, or external/on-premises;
// `databaseType` reflects the deployment model (Autonomous Database, DB
// system, installed DB); `associatedResourceIds` lists the connected
// resources such as the database and private endpoint.
private oci.dataSafe.targetDatabase @defaults("displayName databaseType lifecycleState region") {
  // OCID of the target database
  id string
  // Compartment containing the target database
  compartment() oci.compartment
  // OCI region the target is registered in
  region string
  // Display name of the target in Data Safe
  displayName string
  // Description of the target
  description string
  // Infrastructure type (ORACLE_CLOUD, CLOUD_AT_CUSTOMER, ON_PREMISES, NON_ORACLE_CLOUD)
  infrastructureType string
  // Database type (DATABASE_CLOUD_SERVICE, AUTONOMOUS_DATABASE, INSTALLED_DATABASE)
  databaseType string
  // Lifecycle state (CREATING, UPDATING, ACTIVE, INACTIVE, NEEDS_ATTENTION, FAILED, DELETING, DELETED)
  lifecycleState string
  // Details about the current lifecycle state when state is FAILED or NEEDS_ATTENTION
  lifecycleDetails string
  // OCIDs of associated resources like the underlying database, Data Safe private endpoint, etc.
  associatedResourceIds []string
  // Time the target was registered
  created time
  // Free-form tags on the target
  freeformTags map[string]string
  // Defined tags on the target
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Data Safe security assessment
//
// Configuration-posture finding produced by Data Safe against one or
// more target databases. `type` distinguishes LATEST (auto-generated
// per target), SAVED (snapshot), SAVE_SCHEDULE (periodic save config),
// TEMPLATE (user-defined check set), TEMPLATE_BASELINE (template with
// severity caps), and COMPARTMENT (aggregate of every target in a
// compartment); `targetIds` names the targets the assessment covers.
private oci.dataSafe.securityAssessment @defaults("displayName type lifecycleState region") {
  // OCID of the security assessment
  id string
  // Compartment containing the security assessment
  compartment() oci.compartment
  // OCI region the assessment was produced in
  region string
  // Display name of the assessment
  displayName string
  // Lifecycle state (CREATING, SUCCEEDED, UPDATING, FAILED, DELETING, DELETED)
  lifecycleState string
  // Assessment type: LATEST, SAVED, SAVE_SCHEDULE, TEMPLATE, TEMPLATE_BASELINE, COMPARTMENT
  type string
  // OCIDs of the target databases the assessment covers
  targetIds []string
  // Whether this is a baseline assessment used as the comparison reference
  isBaseline bool
  // Whether the assessment deviates from its configured baseline
  isDeviatedFromBaseline bool
  // Time the assessment was created
  created time
  // Time the assessment was last updated
  timeUpdated time
  // Free-form tags on the assessment
  freeformTags map[string]string
  // Defined tags on the assessment
  definedTags map[string]map[string]string
}

// Oracle Data Safe user assessment
//
// User-risk finding produced against one or more target databases.
// `type` distinguishes LATEST, SAVED, SAVE_SCHEDULE, TEMPLATE,
// TEMPLATE_BASELINE, and COMPARTMENT (aggregate); `targetIds` names the
// targets covered.
private oci.dataSafe.userAssessment @defaults("displayName type lifecycleState region") {
  // OCID of the user assessment
  id string
  // Compartment containing the user assessment
  compartment() oci.compartment
  // OCI region the assessment was produced in
  region string
  // Display name of the assessment
  displayName string
  // Lifecycle state (CREATING, SUCCEEDED, UPDATING, FAILED, DELETING, DELETED)
  lifecycleState string
  // Assessment type: LATEST, SAVED, SAVE_SCHEDULE, TEMPLATE, TEMPLATE_BASELINE, COMPARTMENT
  type string
  // OCIDs of the target databases the assessment covers
  targetIds []string
  // Whether this is the baseline assessment used as the comparison reference
  isBaseline bool
  // Whether the assessment deviates from its configured baseline
  isDeviatedFromBaseline bool
  // Time the assessment was created
  created time
  // Time the assessment was last updated
  timeUpdated time
  // Free-form tags on the assessment
  freeformTags map[string]string
  // Defined tags on the assessment
  definedTags map[string]map[string]string
}

// Oracle Data Safe sensitive data model
//
// Per-target catalog of sensitive columns produced by data discovery.
// `targetId` is the reference database the model was discovered
// against; `appSuiteName` groups models that belong to the same
// application suite.
private oci.dataSafe.sensitiveDataModel @defaults("displayName lifecycleState appSuiteName region") {
  // OCID of the sensitive data model
  id string
  // Compartment containing the sensitive data model
  compartment() oci.compartment
  // OCI region the sensitive data model is hosted in
  region string
  // Display name of the model
  displayName string
  // Description of the model
  description string
  // Lifecycle state (CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  lifecycleState string
  // OCID of the reference target database the model was discovered against
  targetId string
  // Application suite name identifying a collection of applications, default GENERIC
  appSuiteName string
  // Time the model was created
  created time
  // Time the model was last updated
  timeUpdated time
  // Free-form tags on the model
  freeformTags map[string]string
  // Defined tags on the model
  definedTags map[string]map[string]string
}

// Oracle Data Safe sensitive type
//
// Sensitive type definition, either an Oracle predefined type or a
// tenant-defined custom type used during data discovery. `entityType`
// distinguishes a single regex-backed sensitive type (SENSITIVE_TYPE)
// from a sensitive category grouping multiple types (SENSITIVE_CATEGORY);
// `source` reflects whether the type ships with Oracle (ORACLE) or was
// defined by the tenant (USER).
private oci.dataSafe.sensitiveType @defaults("displayName entityType source region") {
  // OCID of the sensitive type
  id string
  // Compartment containing the sensitive type
  compartment() oci.compartment
  // OCI region the sensitive type is hosted in
  region string
  // Display name of the sensitive type
  displayName string
  // Short name of the sensitive type
  shortName string
  // Lifecycle state (CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  lifecycleState string
  // Whether the sensitive type is shipped by Oracle (ORACLE) or defined by the tenant (USER)
  source string
  // Whether the entry is a single regex-backed sensitive type (SENSITIVE_TYPE) or a sensitive category (SENSITIVE_CATEGORY) grouping multiple types
  entityType string
  // Time the sensitive type was created
  created time
  // Time the sensitive type was last updated
  timeUpdated time
  // Free-form tags on the sensitive type
  freeformTags map[string]string
  // Defined tags on the sensitive type
  definedTags map[string]map[string]string
}

// Oracle Data Safe masking policy
//
// Definition of how columns are masked during a sensitive-data masking
// job (refresh, clone, export). `columnSource` names the sensitive data
// model or target the policy's columns are sourced from; its shape
// varies by the configured source type.
private oci.dataSafe.maskingPolicy @defaults("displayName lifecycleState region") {
  // OCID of the masking policy
  id string
  // Compartment containing the masking policy
  compartment() oci.compartment
  // OCI region the masking policy is hosted in
  region string
  // Display name of the masking policy
  displayName string
  // Description of the masking policy
  description string
  // Lifecycle state (CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED, NEEDS_ATTENTION)
  lifecycleState string
  // Column source for the masking policy. Keys vary by source type: SENSITIVE_DATA_MODEL has `sensitiveDataModelId`; TARGET has `targetId`
  columnSource dict
  // Time the masking policy was created
  created time
  // Time the masking policy was last updated
  timeUpdated time
  // Free-form tags on the masking policy
  freeformTags map[string]string
  // Defined tags on the masking policy
  definedTags map[string]map[string]string
}

// Oracle Cloud Vulnerability Scanning Service
//
// Vulnerability Scanning Service (VSS) configuration and findings:
// scan recipes that describe what to scan, targets that pin recipes to
// compute instances or container image repositories, and scan results
// recording per-host vulnerabilities, open ports, CIS benchmark scores,
// endpoint protection state, and per-image container vulnerabilities.
// Also surfaces the CVE-deduplicated `vulnerabilities` rollup that
// aggregates a single CVE's impact across every scanned host and
// container in the tenancy.
oci.vulnerabilityScanning {
  // Host scan recipes describing what each agent scan covers
  hostScanRecipes() []oci.vulnerabilityScanning.hostScanRecipe
  // Host scan targets pinning a recipe to a set of compute instances
  hostScanTargets() []oci.vulnerabilityScanning.hostScanTarget
  // Container scan recipes describing what each container image scan covers
  containerScanRecipes() []oci.vulnerabilityScanning.containerScanRecipe
  // Container scan targets pinning a recipe to an OCIR registry
  containerScanTargets() []oci.vulnerabilityScanning.containerScanTarget
  // Per-instance agent vulnerability scan results
  hostAgentScanResults() []oci.vulnerabilityScanning.hostAgentScanResult
  // Per-instance open port scan results
  hostPortScanResults() []oci.vulnerabilityScanning.hostPortScanResult
  // Per-instance CIS benchmark scan results
  hostCisBenchmarkScanResults() []oci.vulnerabilityScanning.hostCisBenchmarkScanResult
  // Per-instance endpoint protection scan results
  hostEndpointProtectionScanResults() []oci.vulnerabilityScanning.hostEndpointProtectionScanResult
  // Per-image container scan results
  containerScanResults() []oci.vulnerabilityScanning.containerScanResult
  // CVE-deduplicated vulnerability rollup across all scanned hosts and containers
  vulnerabilities() []oci.vulnerabilityScanning.vulnerability
}

// Host scan recipe
//
// Policy object that defines what the agent scans on each host. Covers
// the four sub-scanner levels (port, agent CVE, CIS benchmark, endpoint
// protection), the application scan settings (recurrence and the per-OS
// folder list), and the scan schedule (daily or weekly with optional
// day-of-week).
private oci.vulnerabilityScanning.hostScanRecipe @defaults("name compartmentId portScanLevel agentScanLevel cisBenchmarkScanLevel endpointProtectionScanLevel") {
  // Recipe OCID
  id string
  // Recipe display name
  name string
  // Compartment OCID containing the recipe
  compartmentId string
  // Compartment containing the recipe
  compartment() oci.compartment
  // Lifecycle state (CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Port scan level (NONE, LIGHT, STANDARD)
  portScanLevel string
  // Agent CVE scan level (NONE, STANDARD)
  agentScanLevel string
  // CIS benchmark scan level (NONE, LIGHT, MEDIUM, STRICT)
  cisBenchmarkScanLevel string
  // Endpoint protection scan level (NONE, STANDARD)
  endpointProtectionScanLevel string
  // Whether application scanning is enabled
  applicationScanEnabled bool
  // Application scan recurrence as an ISO-8601 duration
  applicationScanRecurrence string
  // Per-OS folder list scanned for application vulnerabilities
  //
  // Each entry has `operatingSystem` (LINUX, WINDOWS) and `folder`
  // (absolute path scanned on that OS).
  applicationFoldersToScan []dict
  // Scan schedule type (DAILY, WEEKLY)
  scheduleType string
  // Day of week for weekly schedules (MONDAY..SUNDAY)
  scheduleDayOfWeek string
  // When the recipe was created
  created time
  // Free-form tags on the recipe
  freeformTags map[string]string
}

// Host scan target
//
// Binding that pins a host scan recipe to a list of compute instances
// within a compartment subtree. The recipe determines what gets
// scanned; the target determines where.
private oci.vulnerabilityScanning.hostScanTarget @defaults("name compartmentId state") {
  // Target OCID
  id string
  // Target display name
  name string
  // Target description
  description string
  // Compartment OCID containing the target
  compartmentId string
  // Compartment containing the target
  compartment() oci.compartment
  // Compartment OCID whose instances this target scans
  targetCompartmentId string
  // Compartment whose instances this target scans
  targetCompartment() oci.compartment
  // Host scan recipe OCID applied by this target
  hostScanRecipeId string
  // Host scan recipe applied by this target
  recipe() oci.vulnerabilityScanning.hostScanRecipe
  // Compute instance OCIDs scanned by this target
  instanceIds []string
  // Compute instances scanned by this target
  instances() []oci.compute.instance
  // Lifecycle state (CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // When the target was created
  created time
  // Free-form tags on the target
  freeformTags map[string]string
}

// Container scan recipe
//
// Policy object that defines what each container image scan covers,
// including the scan level (currently STANDARD only) and the number of
// images covered.
private oci.vulnerabilityScanning.containerScanRecipe @defaults("name compartmentId scanLevel imageCount") {
  // Recipe OCID
  id string
  // Recipe display name
  name string
  // Compartment OCID containing the recipe
  compartmentId string
  // Compartment containing the recipe
  compartment() oci.compartment
  // Lifecycle state
  state string
  // Container scan level (STANDARD)
  scanLevel string
  // Number of images covered by recipes referencing this scan recipe
  imageCount int
  // When the recipe was created
  created time
  // Free-form tags on the recipe
  freeformTags map[string]string
}

// Container scan target
//
// Binding that pins a container scan recipe to a registry. Today only
// OCIR is supported; `registryUrl` and `registryRepositories` describe
// the registry endpoint and the repositories scanned.
private oci.vulnerabilityScanning.containerScanTarget @defaults("name compartmentId registryUrl") {
  // Target OCID
  id string
  // Target display name
  name string
  // Target description
  description string
  // Compartment OCID containing the target
  compartmentId string
  // Compartment containing the target
  compartment() oci.compartment
  // Container scan recipe OCID applied by this target
  containerScanRecipeId string
  // Container scan recipe applied by this target
  recipe() oci.vulnerabilityScanning.containerScanRecipe
  // Registry type (OCIR)
  registryType string
  // Registry URL
  registryUrl string
  // Repositories scanned within the registry
  registryRepositories []string
  // Lifecycle state
  state string
  // When the target was created
  created time
  // Free-form tags on the target
  freeformTags map[string]string
}

// Host agent vulnerability scan result
//
// Most recent agent vulnerability scan for a compute instance. Reports
// the operating system and kernel version of the scanned host, the
// highest finding severity, the total problem count, and the per-CVE
// findings under `problems` (each with the vulnerable package list and
// a `vulnerability` rollup).
private oci.vulnerabilityScanning.hostAgentScanResult @defaults("name instanceId highestProblemSeverity problemCount") {
  // Scan result OCID
  id string
  // Scan result display name
  name string
  // Scanned compute instance OCID
  instanceId string
  // Scanned compute instance
  instance() oci.compute.instance
  // Compartment OCID containing the scan result
  compartmentId string
  // Compartment containing the scan result
  compartment() oci.compartment
  // Highest finding severity (NONE, LOW, MEDIUM, HIGH, CRITICAL)
  highestProblemSeverity string
  // Operating system of the scanned host
  operatingSystem string
  // Kernel version of the scanned host
  kernelVersion() string
  // Scanner vendor (OCI, QUALYS)
  vendor() string
  // Total number of problems found
  problemCount int
  // Lifecycle state
  state string
  // Per-CVE findings from the scan
  problems() []oci.vulnerabilityScanning.hostAgentScanResult.problem
  // Time the scan started
  //
  // Distinguishes a scan that never ran from one that ran and found
  // nothing, and lets an audit require a recent result.
  scanStarted time
  // Time the scan finished
  scanFinished time
}

// Host agent scan problem
//
// Single CVE finding from a host agent scan: the CVE reference,
// severity, remediation state, and the vulnerable packages installed on
// the host. `vulnerability` resolves the tenancy-wide rollup for the
// same CVE.
private oci.vulnerabilityScanning.hostAgentScanResult.problem @defaults("name severity state cveReference") {
  // Vulnerability name (typically the CVE id)
  name string
  // Finding severity (NONE, LOW, MEDIUM, HIGH, CRITICAL)
  severity string
  // Vulnerability state (OPEN, FIXED, NOT_APPLICABLE)
  state string
  // Long-form description of the finding
  description string
  // CVE reference (e.g., CVE-2023-1234)
  cveReference string
  // Internal issue identifier (scanner-specific)
  issueId int
  // Tenancy-wide rollup for the same vulnerability
  vulnerability() oci.vulnerabilityScanning.vulnerability
  // Installed packages affected by this vulnerability
  //
  // Each entry has `name`, `version`, `type` (RPM, DPKG, ...),
  // `cveFixVersion` (empty when no fix is available), and
  // `locations` (file paths where the package is installed).
  vulnerablePackages []dict
  // Time the problem was first detected
  firstDetected time
  // Time the problem was last detected
  lastDetected time
}

// Host port scan result
//
// Most recent open-port scan for a compute instance. Reports the
// highest severity across open ports and the per-port findings under
// `openPorts` (each pinned to the VNIC that exposes the port).
private oci.vulnerabilityScanning.hostPortScanResult @defaults("name instanceId openPortCount highestProblemSeverity") {
  // Scan result OCID
  id string
  // Scan result display name
  name string
  // Scanned compute instance OCID
  instanceId string
  // Scanned compute instance
  instance() oci.compute.instance
  // Compartment OCID containing the scan result
  compartmentId string
  // Compartment containing the scan result
  compartment() oci.compartment
  // Highest severity across open ports
  highestProblemSeverity string
  // Total number of open ports detected
  openPortCount int
  // Lifecycle state
  state string
  // Per-port findings from the scan
  openPorts() []oci.vulnerabilityScanning.hostPortScanResult.openPort
  // Time the scan started
  //
  // Distinguishes a scan that never ran from one that ran and found
  // nothing, and lets an audit require a recent result.
  scanStarted time
  // Time the scan finished
  scanFinished time
}

// Host port scan open port
//
// Single open port detected on a compute instance: the port number,
// protocol, IP address, and the VNIC that exposes the port. `vnic`
// resolves the network interface and its security groups.
private oci.vulnerabilityScanning.hostPortScanResult.openPort @defaults("port protocol service severity") {
  // Port number
  port int
  // Protocol (tcp, udp)
  protocol string
  // IP address the port is listening on
  ipAddress string
  // Detected service name (e.g., ssh, http), empty when unknown
  service string
  // Finding severity for this open port
  severity string
  // VNIC OCID exposing the port
  vnicId string
  // VNIC exposing the port
  vnic() oci.compute.vnic
}

// Host CIS benchmark scan result
//
// Most recent CIS benchmark scan for a compute instance. Reports the
// total issue count and the per-control pass/fail scores under
// `scores`.
private oci.vulnerabilityScanning.hostCisBenchmarkScanResult @defaults("name instanceId cisBenchmarkScanIssuesCount") {
  // Scan result OCID
  id string
  // Scan result display name
  name string
  // Scanned compute instance OCID
  instanceId string
  // Scanned compute instance
  instance() oci.compute.instance
  // Compartment OCID containing the scan result
  compartmentId string
  // Compartment containing the scan result
  compartment() oci.compartment
  // Total number of CIS benchmark issues found
  cisBenchmarkScanIssuesCount int
  // Lifecycle state
  state string
  // Per-control benchmark scores
  //
  // Each entry has `benchmarkIdentifier` (e.g., "1.1.1"),
  // `description` (the control statement), and `score` (PASS or
  // FAIL).
  scores() []dict
  // Time the scan started
  //
  // Distinguishes a scan that never ran from one that ran and found
  // nothing, and lets an audit require a recent result.
  scanStarted time
  // Time the scan finished
  scanFinished time
}

// Host endpoint protection scan result
//
// Most recent endpoint protection posture scan for a compute instance.
// Reports the overall problem severity, the number of endpoint
// protection services evaluated, and the per-service findings.
private oci.vulnerabilityScanning.hostEndpointProtectionScanResult @defaults("name instanceId problemSeverity endpointProtectionsCount") {
  // Scan result OCID
  id string
  // Scan result display name
  name string
  // Scanned compute instance OCID
  instanceId string
  // Scanned compute instance
  instance() oci.compute.instance
  // Compartment OCID containing the scan result
  compartmentId string
  // Compartment containing the scan result
  compartment() oci.compartment
  // Overall endpoint protection problem severity
  problemSeverity string
  // Number of endpoint protection services evaluated
  endpointProtectionsCount int
  // Lifecycle state
  state string
  // Per-service endpoint protection findings
  //
  // Each entry has `service` (the endpoint protection product name),
  // `configurationFinding` (the misconfiguration, if any), and
  // `severity`.
  endpointProtections() []dict
  // Time the scan started
  //
  // Distinguishes a scan that never ran from one that ran and found
  // nothing, and lets an audit require a recent result.
  scanStarted time
  // Time the scan finished
  scanFinished time
}

// Container vulnerability scan result
//
// Most recent container image scan. Reports the repository, image tag,
// registry URL, and the per-CVE findings under `problems`. `target`
// resolves the container scan target that produced the result.
private oci.vulnerabilityScanning.containerScanResult @defaults("repository image highestProblemSeverity problemCount") {
  // Scan result OCID
  id string
  // Container repository
  repository string
  // Image tag or digest
  image string
  // Registry URL the image was pulled from
  registryUrl() string
  // Compartment OCID containing the scan result
  compartmentId string
  // Compartment containing the scan result
  compartment() oci.compartment
  // Target compartment OCID (where the source image lives)
  targetCompartmentId() string
  // Container scan target OCID
  containerScanTargetId string
  // Container scan target that produced the result
  target() oci.vulnerabilityScanning.containerScanTarget
  // Highest finding severity across the image
  highestProblemSeverity string
  // Total number of problems found
  problemCount int
  // Per-CVE findings from the scan
  problems() []oci.vulnerabilityScanning.containerScanResult.problem
  // Time the scan started
  //
  // Distinguishes a scan that never ran from one that ran and found
  // nothing, and lets an audit require a recent result.
  scanStarted time
  // Time the scan finished
  scanFinished time
}

// Container scan problem
//
// Single CVE finding from a container image scan: the CVE reference,
// severity, remediation state, and the vulnerable packages baked into
// the image. `vulnerability` resolves the tenancy-wide rollup for the
// same CVE.
private oci.vulnerabilityScanning.containerScanResult.problem @defaults("name severity state cveReference") {
  // Vulnerability name (typically the CVE id)
  name string
  // Finding severity
  severity string
  // Vulnerability state (OPEN, FIXED, NOT_APPLICABLE)
  state string
  // Long-form description of the finding
  description string
  // CVE reference
  cveReference string
  // Tenancy-wide rollup for the same vulnerability
  vulnerability() oci.vulnerabilityScanning.vulnerability
  // Image layer packages affected by this vulnerability
  //
  // Each entry has `name`, `version`, `type`, `cveFixVersion`, and
  // `locations` (file paths inside the image where the package
  // resides).
  vulnerablePackages []dict
  // Time the problem was first detected
  firstDetected time
  // Time the problem was last detected
  lastDetected time
}

// Vulnerability rollup
//
// Single vulnerability aggregated across every scanned host and
// container in the tenancy. Each row collapses a single CVE (or
// proprietary issue) into the highest observed severity, the
// impacted-resource counts, and the CVE metadata (title, impact,
// threat, solution, exploitability). `impactedHosts` returns the
// compute instances affected.
private oci.vulnerabilityScanning.vulnerability @defaults("name severity vulnerabilityType state") {
  // Vulnerability identifier (CVE id or proprietary issue id)
  id string
  // Vulnerability name (typically the CVE id)
  name string
  // Compartment OCID owning the rollup row
  compartmentId string
  // Compartment owning the rollup row
  compartment() oci.compartment
  // Highest observed severity
  severity string
  // Vulnerability type (CVE, PROBLEM)
  vulnerabilityType string
  // Remediation state (OPEN, FIXED, NOT_APPLICABLE)
  state string
  // Authentication requirement for exploitation
  authentication string
  // Upstream reference identifier
  vulnerabilityReference string
  // CVE description from the upstream reference
  cveDescription string
  // CVE title
  cveTitle string
  // CVE impact summary
  cveImpact string
  // CVE threat summary
  cveThreat string
  // CVE remediation guidance
  cveSolution string
  // Whether a patch is available (yes, no, unknown)
  cvePatchable string
  // Whether a public exploit is known (yes, no, unknown)
  cveExploitable string
  // Related CVE reference, when applicable
  cveRelatedReference string
  // Upstream URL with full vulnerability details
  cveReferenceUrl string
  // CVSS v3 base score
  //
  // Reported by OCI as a string, so compare it as a number with
  // `cvss3.float`. Empty when the upstream reference carries no CVSS v3
  // score, which is common for non-CVE findings.
  cvss3 string
  // Time the CVE was published upstream
  cvePublished time
  // Time the CVE record was last updated upstream
  cveUpdated time
  // Number of compute instances affected
  impactedHostsCount int
  // Number of container images affected
  impactedContainersCount int
  // Lifecycle state
  lifecycleState string
  // Compute instances affected by this vulnerability
  impactedHosts() []oci.compute.instance
  // Container images affected by this vulnerability
  //
  // Each entry has `image`, `repository`, `registry`,
  // `containerScanTargetId`, and `lastContainerScanId`.
  impactedContainerImages() []dict
}

// Oracle Cloud Infrastructure (OCI) AI services
//
// Namespace and entry point for Oracle's managed AI and machine-learning
// services, spanning Generative AI Agents (`oci.ai.agents`), Data Science
// (`oci.ai.dataScience`), Generative AI (`oci.ai.generativeAi`), and the
// Language, Vision, Speech, and Document Understanding services. Each
// covers the resources that make up that service in the tenancy, from
// agents and knowledge bases to models, endpoints, and training projects.
oci.ai {
}

// Oracle Cloud Infrastructure (OCI) Generative AI Agents
//
// Generative AI Agents estate for the tenancy across every subscribed
// region. The `agents` are the configured agents, `agentEndpoints` are
// the endpoints clients connect to (with their content-moderation and
// guardrail settings), `knowledgeBases` and `dataSources` are the
// enterprise data wired into retrieval-augmented generation, and `tools`
// are the tools attached to agents, including the HTTP-endpoint and
// function-calling tools that let an agent reach external APIs.
oci.ai.agents {
  // Generative AI agents
  agents() []oci.ai.agents.agent
  // Agent endpoints
  agentEndpoints() []oci.ai.agents.endpoint
  // Knowledge bases
  knowledgeBases() []oci.ai.agents.knowledgeBase
  // Knowledge base data sources
  dataSources() []oci.ai.agents.dataSource
  // Tools attached to agents
  tools() []oci.ai.agents.tool
}

// Oracle Cloud Infrastructure (OCI) Generative AI agent
//
// Single Generative AI agent, the large-language-model application that
// answers questions over enterprise data. Select an agent by its OCID,
// for example `oci.ai.agents.agent(id: "ocid1.genaiagent.oc1...")`. The
// agent exposes its welcome message, the language-model routing
// configuration, and the compartment it lives in. Clients connect to an
// agent through one of its `oci.ai.agents.endpoint` resources.
private oci.ai.agents.agent @defaults("name state") {
  init(id? string)
  // Agent OCID
  id string
  // Agent display name
  name string
  // Compartment OCID containing the agent
  compartmentID string
  // Compartment containing the agent
  compartment() oci.compartment
  // Description of the agent's purpose
  description string
  // Greeting shown to users at the start of a conversation
  welcomeMessage string
  // Language-model routing and selection configuration
  llmConfig dict
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI agent endpoint
//
// Network-reachable entry point clients use to chat with a Generative AI
// agent. Select an endpoint by its OCID, for example
// `oci.ai.agents.endpoint(id: "ocid1.genaiagentendpoint.oc1...")`. The
// endpoint reports whether content moderation runs on input and output,
// the guardrail configuration that screens for content, prompt
// injection, and personally identifiable information, session behavior,
// and whether tracing and citations are returned in chat results, the
// controls that govern how safely the agent can be used.
private oci.ai.agents.endpoint @defaults("name state") {
  init(id? string)
  // Endpoint OCID
  id string
  // Endpoint display name
  name string
  // Compartment OCID containing the endpoint
  compartmentID string
  // Compartment containing the endpoint
  compartment() oci.compartment
  // OCID of the agent this endpoint serves
  agentID string
  // Agent this endpoint serves
  agent() oci.ai.agents.agent
  // Description of the endpoint
  description string
  // Whether content moderation runs on user input
  contentModerationOnInput bool
  // Whether content moderation runs on model output
  contentModerationOnOutput bool
  // Guardrail configuration screening content, prompt injection, and PII
  //
  // Has `contentModerationConfig`, `promptInjectionConfig`, and
  // `personallyIdentifiableInformationConfig`, each reporting whether
  // the guardrail is enabled and the action it takes.
  guardrailConfig dict
  // Idle timeout in seconds before a chat session expires
  sessionIdleTimeoutInSeconds int
  // Whether session-based chat is enabled
  sessionEnabled bool
  // Whether traces are returned in chat results
  traceEnabled bool
  // Whether citations are returned in chat results
  citationEnabled bool
  // Whether multi-language chat is enabled
  multiLanguageEnabled bool
  // Additional endpoint configuration key-value pairs
  metadata map[string]string
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI agent knowledge base
//
// Indexed corpus a Generative AI agent retrieves from to answer
// questions. Select a knowledge base by its OCID, for example
// `oci.ai.agents.knowledgeBase(id: "ocid1.genaiagentknowledgebase.oc1...")`.
// The tenancy's `dataSources` show which content feeds each knowledge
// base.
private oci.ai.agents.knowledgeBase @defaults("name state") {
  init(id? string)
  // Knowledge base OCID
  id string
  // Knowledge base display name
  name string
  // Compartment OCID containing the knowledge base
  compartmentID string
  // Compartment containing the knowledge base
  compartment() oci.compartment
  // Description of the knowledge base
  description string
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI agent data source
//
// Content ingested into a knowledge base for a Generative AI agent to
// retrieve. Select a data source by its OCID, for example
// `oci.ai.agents.dataSource(id: "ocid1.genaiagentdatasource.oc1...")`.
// The `knowledgeBase` field resolves the knowledge base this content
// feeds.
private oci.ai.agents.dataSource @defaults("name state") {
  init(id? string)
  // Data source OCID
  id string
  // Data source display name
  name string
  // Compartment OCID containing the data source
  compartmentID string
  // Compartment containing the data source
  compartment() oci.compartment
  // OCID of the knowledge base this data source feeds
  knowledgeBaseID string
  // Knowledge base this data source feeds
  knowledgeBase() oci.ai.agents.knowledgeBase
  // Description of the data source
  description string
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI agent tool
//
// Capability an agent can invoke while answering, such as retrieving from
// a knowledge base, querying a database, calling a function, or reaching
// an external HTTP endpoint. Select a tool by its OCID, for example
// `oci.ai.agents.tool(id: "ocid1.genaiagenttool.oc1...")`. The `toolType`
// field identifies the kind of tool. Use it to find tools that can reach
// outside the tenancy, for example
// `oci.ai.agents.tools.where(toolType == "HTTP_ENDPOINT_TOOL_CONFIG")`.
private oci.ai.agents.tool @defaults("name toolType state") {
  init(id? string)
  // Tool OCID
  id string
  // Tool display name
  name string
  // Compartment OCID containing the tool
  compartmentID string
  // Compartment containing the tool
  compartment() oci.compartment
  // OCID of the agent this tool is attached to
  agentID string
  // Agent this tool is attached to
  agent() oci.ai.agents.agent
  // Description of the tool
  description string
  // Tool type
  //
  // One of RAG_TOOL_CONFIG, SQL_TOOL_CONFIG, FUNCTION_CALLING_TOOL_CONFIG,
  // HTTP_ENDPOINT_TOOL_CONFIG, or AGENT_TOOL_CONFIG.
  toolType string
  // Full tool configuration, with shape determined by `toolType`
  toolConfig dict
  // Additional tool configuration key-value pairs
  metadata map[string]string
  // Lifecycle state (e.g., CREATING, UPDATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science
//
// Data Science machine learning estate for the tenancy across every
// subscribed region. The `projects` are the workspaces that organize ML
// work, `notebookSessions` are the managed JupyterLab environments (with
// their compute shape, network placement, and private-endpoint
// configuration), `models` and `modelVersionSets` are the model catalog,
// `modelDeployments` are the models served as HTTP endpoints (with their
// endpoint URL and logging configuration), and `jobs` and `pipelines`
// are the batch and orchestration workloads.
oci.ai.dataScience {
  // Data Science projects
  projects() []oci.ai.dataScience.project
  // Notebook sessions
  notebookSessions() []oci.ai.dataScience.notebookSession
  // Models in the model catalog
  models() []oci.ai.dataScience.model
  // Model version sets
  modelVersionSets() []oci.ai.dataScience.modelVersionSet
  // Model deployments
  modelDeployments() []oci.ai.dataScience.modelDeployment
  // Jobs
  jobs() []oci.ai.dataScience.job
  // Pipelines
  pipelines() []oci.ai.dataScience.pipeline
}

// Oracle Cloud Infrastructure (OCI) Data Science project
//
// Workspace that groups Data Science notebook sessions, models, jobs, and
// pipelines for a team or initiative. Select a project by its OCID, for
// example
// `oci.ai.dataScience.project(id: "ocid1.datascienceproject.oc1...")`.
private oci.ai.dataScience.project @defaults("name state") {
  init(id? string)
  // Project OCID
  id string
  // Project display name
  name string
  // Compartment OCID containing the project
  compartmentID string
  // Compartment containing the project
  compartment() oci.compartment
  // Description of the project
  description string
  // OCID of the principal that created the project
  createdBy string
  // Lifecycle state (e.g., ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science notebook session
//
// Managed JupyterLab environment where data scientists develop and train
// models. Select a session by its OCID, for example
// `oci.ai.dataScience.notebookSession(id: "ocid1.datasciencenotebooksession.oc1...")`.
// The session reports its compute shape, the subnet it runs in, the
// optional private endpoint that isolates it from the public internet,
// and its access URL, the controls that determine the session's network
// exposure.
private oci.ai.dataScience.notebookSession @defaults("name state") {
  init(id? string)
  // Notebook session OCID
  id string
  // Notebook session display name
  name string
  // Compartment OCID containing the session
  compartmentID string
  // Compartment containing the session
  compartment() oci.compartment
  // OCID of the project the session belongs to
  projectID string
  // Project the session belongs to
  project() oci.ai.dataScience.project
  // OCID of the principal that created the session
  createdBy string
  // Compute shape backing the session
  shape string
  // OCID of the subnet the session runs in
  subnetID string
  // Subnet the session runs in
  subnet() oci.network.subnet
  // Block storage attached to the session, in gigabytes
  blockStorageSizeInGBs int
  // OCID of the Data Science private endpoint isolating the session, when set
  privateEndpointID string
  // URL used to access the session
  notebookSessionUrl string
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED, INACTIVE, UPDATING)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science model
//
// Saved, versioned machine learning artifact in the model catalog. Select
// a model by its OCID, for example
// `oci.ai.dataScience.model(id: "ocid1.datasciencemodel.oc1...")`. The
// model reports its version within its model version set, its category,
// and whether its artifact is stored by reference in Object Storage
// rather than in the catalog.
private oci.ai.dataScience.model @defaults("name state") {
  init(id? string)
  // Model OCID
  id string
  // Model display name
  name string
  // Compartment OCID containing the model
  compartmentID string
  // Compartment containing the model
  compartment() oci.compartment
  // OCID of the project the model belongs to
  projectID string
  // Project the model belongs to
  project() oci.ai.dataScience.project
  // OCID of the principal that created the model
  createdBy string
  // OCID of the model version set the model belongs to
  modelVersionSetID string
  // Model version set the model belongs to
  modelVersionSet() oci.ai.dataScience.modelVersionSet
  // Name of the model version set the model belongs to
  modelVersionSetName string
  // Version number of the model within its version set
  versionId int
  // Version label of the model within its version set
  versionLabel string
  // Model category (USER or SERVICE)
  category string
  // Whether the model artifact is stored by reference in Object Storage
  isModelByReference bool
  // Lifecycle state (e.g., ACTIVE, DELETED, FAILED, INACTIVE)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science model version set
//
// Container that groups successive versions of a Data Science model.
// Select a version set by its OCID, for example
// `oci.ai.dataScience.modelVersionSet(id: "ocid1.datasciencemodelversionset.oc1...")`.
private oci.ai.dataScience.modelVersionSet @defaults("name state") {
  init(id? string)
  // Model version set OCID
  id string
  // Model version set name
  name string
  // Compartment OCID containing the version set
  compartmentID string
  // Compartment containing the version set
  compartment() oci.compartment
  // OCID of the project the version set belongs to
  projectID string
  // Project the version set belongs to
  project() oci.ai.dataScience.project
  // OCID of the principal that created the version set
  createdBy string
  // Version set category (USER or SERVICE)
  category string
  // Lifecycle state (e.g., ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science model deployment
//
// Catalog model served as an HTTP inference endpoint. Select a deployment
// by its OCID, for example
// `oci.ai.dataScience.modelDeployment(id: "ocid1.datasciencemodeldeployment.oc1...")`.
// The deployment reports its endpoint URL, its instance and model
// configuration, and the access and predict logging attached to it.
private oci.ai.dataScience.modelDeployment @defaults("name state") {
  init(id? string)
  // Model deployment OCID
  id string
  // Model deployment display name
  name string
  // Compartment OCID containing the deployment
  compartmentID string
  // Compartment containing the deployment
  compartment() oci.compartment
  // OCID of the project the deployment belongs to
  projectID string
  // Project the deployment belongs to
  project() oci.ai.dataScience.project
  // OCID of the principal that created the deployment
  createdBy string
  // Description of the deployment
  description string
  // URL of the inference endpoint
  modelDeploymentUrl string
  // Instance and model configuration of the deployment
  //
  // Includes the deployment type and, for a single-model deployment, the
  // model OCID, instance shape, instance count, and bandwidth.
  configuration dict
  // Access and predict logging attached to the deployment
  //
  // Has `access` and `predict`, each with the OCID of the log and log
  // group when logging is enabled.
  loggingConfiguration dict
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED, INACTIVE, UPDATING, NEEDS_ATTENTION)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science job
//
// Repeatable, self-contained machine learning task such as training or
// batch processing. Select a job by its OCID, for example
// `oci.ai.dataScience.job(id: "ocid1.datasciencejob.oc1...")`.
private oci.ai.dataScience.job @defaults("name state") {
  init(id? string)
  // Job OCID
  id string
  // Job display name
  name string
  // Compartment OCID containing the job
  compartmentID string
  // Compartment containing the job
  compartment() oci.compartment
  // OCID of the project the job belongs to
  projectID string
  // Project the job belongs to
  project() oci.ai.dataScience.project
  // OCID of the principal that created the job
  createdBy string
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Data Science pipeline
//
// End-to-end machine learning workflow that orchestrates multiple steps.
// Select a pipeline by its OCID, for example
// `oci.ai.dataScience.pipeline(id: "ocid1.datasciencepipeline.oc1...")`.
private oci.ai.dataScience.pipeline @defaults("name state") {
  init(id? string)
  // Pipeline OCID
  id string
  // Pipeline display name
  name string
  // Compartment OCID containing the pipeline
  compartmentID string
  // Compartment containing the pipeline
  compartment() oci.compartment
  // OCID of the project the pipeline belongs to
  projectID string
  // Project the pipeline belongs to
  project() oci.ai.dataScience.project
  // OCID of the principal that created the pipeline
  createdBy string
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI
//
// Generative AI foundation model estate for the tenancy across every
// subscribed region. The `dedicatedAiClusters` are the dedicated GPU
// capacity that hosts or fine-tunes models, `models` are the pretrained
// and custom (fine-tuned) models available in the compartment, and
// `endpoints` are the model-serving endpoints, each reporting its content
// moderation, prompt-injection, and PII-detection guardrails.
oci.ai.generativeAi {
  // Dedicated AI clusters
  dedicatedAiClusters() []oci.ai.generativeAi.dedicatedAiCluster
  // Pretrained and custom models
  models() []oci.ai.generativeAi.model
  // Model-serving endpoints
  endpoints() []oci.ai.generativeAi.endpoint
}

// Oracle Cloud Infrastructure (OCI) Generative AI dedicated AI cluster
//
// Reserved GPU capacity used to host or fine-tune Generative AI models.
// Select a cluster by its OCID, for example
// `oci.ai.generativeAi.dedicatedAiCluster(id: "ocid1.generativeaidedicatedaicluster.oc1...")`.
// The cluster reports its purpose (hosting or fine-tuning), the number
// and shape of its GPU units, and its committed capacity.
private oci.ai.generativeAi.dedicatedAiCluster @defaults("name type state") {
  init(id? string)
  // Cluster OCID
  id string
  // Cluster display name
  name string
  // Compartment OCID containing the cluster
  compartmentID string
  // Compartment containing the cluster
  compartment() oci.compartment
  // Cluster purpose (HOSTING or FINE_TUNING)
  type string
  // Number of GPU units allocated to the cluster
  unitCount int
  // Shape of the GPU units (e.g., LARGE_GENERIC, LLAMA2_70, A100_80G_X8)
  unitShape string
  // Committed capacity of the cluster
  //
  // Shape varies by cluster type; for a hosting cluster it includes the
  // total and used endpoint capacity.
  capacity dict
  // Description of the cluster
  description string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI model
//
// Pretrained foundation model or a custom model fine-tuned from one.
// Select a model by its OCID, for example
// `oci.ai.generativeAi.model(id: "ocid1.generativeaimodel.oc1...")`. The
// model reports its vendor and version, its capabilities (such as chat,
// text generation, or embeddings), and, for a custom model, the base
// model it was fine-tuned from and the fine-tuning details.
private oci.ai.generativeAi.model @defaults("name type state") {
  init(id? string)
  // Model OCID
  id string
  // Model display name
  name string
  // Compartment OCID containing the model
  compartmentID string
  // Compartment containing the model
  compartment() oci.compartment
  // Model type (BASE or CUSTOM)
  type string
  // Model capabilities
  //
  // Values include TEXT_GENERATION, TEXT_SUMMARIZATION, TEXT_EMBEDDINGS,
  // FINE_TUNE, CHAT, TEXT_RERANK, and the various image, audio, and video
  // modalities.
  capabilities []string
  // Vendor of the base model (e.g., cohere, meta)
  vendor string
  // Model version
  version string
  // OCID of the base model a custom model was fine-tuned from, when set
  baseModelID string
  // Base model a custom model was fine-tuned from, when set
  baseModel() oci.ai.generativeAi.model
  // Fine-tuning configuration for a custom model
  //
  // Includes the training dataset and the training-method hyperparameters.
  fineTuneDetails dict
  // Whether the model is offered with long-term support
  isLongTermSupported bool
  // Lifecycle state (e.g., ACTIVE, CREATING, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Time the model is scheduled to be deprecated, when set
  timeDeprecated time
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Generative AI endpoint
//
// Custom model served for inference on a dedicated AI cluster. Select an
// endpoint by its OCID, for example
// `oci.ai.generativeAi.endpoint(id: "ocid1.generativeaiendpoint.oc1...")`.
// The endpoint reports the model it serves, the cluster it runs on, the
// optional private endpoint that isolates it, and whether content
// moderation, prompt-injection detection, and PII detection guardrails
// are enabled. Use these to find endpoints serving models without safety
// controls.
private oci.ai.generativeAi.endpoint @defaults("name state") {
  init(id? string)
  // Endpoint OCID
  id string
  // Endpoint display name
  name string
  // Compartment OCID containing the endpoint
  compartmentID string
  // Compartment containing the endpoint
  compartment() oci.compartment
  // OCID of the model served by the endpoint
  modelID string
  // Model served by the endpoint
  model() oci.ai.generativeAi.model
  // OCID of the dedicated AI cluster hosting the endpoint
  dedicatedAiClusterID string
  // Dedicated AI cluster hosting the endpoint
  dedicatedAiCluster() oci.ai.generativeAi.dedicatedAiCluster
  // OCID of the Generative AI private endpoint isolating this endpoint, when set
  generativeAiPrivateEndpointID string
  // Description of the endpoint
  description string
  // Whether content moderation is enabled
  contentModerationEnabled bool
  // Content moderation mode (INFORM or BLOCK), when moderation is enabled
  contentModerationMode string
  // Whether prompt-injection detection is enabled
  promptInjectionEnabled bool
  // Prompt-injection detection mode (INFORM or BLOCK), when enabled
  promptInjectionMode string
  // Whether PII detection is enabled
  piiDetectionEnabled bool
  // PII detection mode (INFORM or BLOCK), when enabled
  piiDetectionMode string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Language AI
//
// Custom Language AI estate for the tenancy across every subscribed
// region. The `projects` are the workspaces that group Language models,
// `models` are the custom models trained for tasks such as text
// classification and named-entity recognition, and `endpoints` are the
// models served for inference.
oci.ai.language {
  // Language projects
  projects() []oci.ai.language.project
  // Custom Language models
  models() []oci.ai.language.model
  // Model-serving endpoints
  endpoints() []oci.ai.language.endpoint
}

// Oracle Cloud Infrastructure (OCI) Language AI project
//
// Workspace that groups custom Language models and their serving
// endpoints. Select a project by its OCID, for example
// `oci.ai.language.project(id: "ocid1.ailanguageproject.oc1...")`.
private oci.ai.language.project @defaults("name state") {
  init(id? string)
  // Project OCID
  id string
  // Project display name
  name string
  // Compartment OCID containing the project
  compartmentID string
  // Compartment containing the project
  compartment() oci.compartment
  // Description of the project
  description string
  // Lifecycle state (e.g., ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Language AI model
//
// Single custom Language model. Select a model by its OCID, for example
// `oci.ai.language.model(id: "ocid1.ailanguagemodel.oc1...")`.
private oci.ai.language.model @defaults("name state") {
  init(id? string)
  // Model OCID
  id string
  // Model display name
  name string
  // Compartment OCID containing the model
  compartmentID string
  // Compartment containing the model
  compartment() oci.compartment
  // OCID of the project the model belongs to
  projectID string
  // Project the model belongs to
  project() oci.ai.language.project
  // Description of the model
  description string
  // Model version
  version string
  // Model configuration
  //
  // Includes the model type (such as the training task) and the language
  // codes the model was trained on.
  modelDetails dict
  // Lifecycle state (e.g., ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Language AI endpoint
//
// Custom Language model served for inference. Select an endpoint by its
// OCID, for example
// `oci.ai.language.endpoint(id: "ocid1.ailanguageendpoint.oc1...")`.
private oci.ai.language.endpoint @defaults("name state") {
  init(id? string)
  // Endpoint OCID
  id string
  // Endpoint display name
  name string
  // Compartment OCID containing the endpoint
  compartmentID string
  // Compartment containing the endpoint
  compartment() oci.compartment
  // OCID of the project the endpoint belongs to
  projectID string
  // Project the endpoint belongs to
  project() oci.ai.language.project
  // OCID of the model served by the endpoint
  modelID string
  // Model served by the endpoint
  model() oci.ai.language.model
  // Alias of the endpoint
  alias string
  // Description of the endpoint
  description string
  // Number of inference units provisioned for the endpoint
  inferenceUnits int
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Vision AI
//
// Custom Vision AI estate for the tenancy across every subscribed region.
// The `projects` are the workspaces that group Vision models and
// `models` are the custom models trained for image classification and
// object detection.
oci.ai.vision {
  // Vision projects
  projects() []oci.ai.vision.project
  // Custom Vision models
  models() []oci.ai.vision.model
}

// Oracle Cloud Infrastructure (OCI) Vision AI project
//
// Workspace that groups custom Vision models. Select a project by its
// OCID, for example
// `oci.ai.vision.project(id: "ocid1.aivisionproject.oc1...")`.
private oci.ai.vision.project @defaults("name state") {
  init(id? string)
  // Project OCID
  id string
  // Project display name
  name string
  // Compartment OCID containing the project
  compartmentID string
  // Compartment containing the project
  compartment() oci.compartment
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Vision AI model
//
// Single custom Vision model. Select a model by its OCID, for example
// `oci.ai.vision.model(id: "ocid1.aivisionmodel.oc1...")`.
private oci.ai.vision.model @defaults("name modelType state") {
  init(id? string)
  // Model OCID
  id string
  // Model display name
  name string
  // Compartment OCID containing the model
  compartmentID string
  // Compartment containing the model
  compartment() oci.compartment
  // OCID of the project the model belongs to
  projectID string
  // Project the model belongs to
  project() oci.ai.vision.project
  // Model type (e.g., IMAGE_CLASSIFICATION, OBJECT_DETECTION)
  modelType string
  // Model version
  modelVersion string
  // Description of the model
  description string
  // Model precision (accuracy) achieved during training
  precision float
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Speech AI
//
// Speech AI customizations for the tenancy across every subscribed
// region. The `customizations` are the custom speech models that adapt
// transcription to domain-specific vocabulary.
oci.ai.speech {
  // Speech customizations (custom models)
  customizations() []oci.ai.speech.customization
}

// Oracle Cloud Infrastructure (OCI) Speech AI customization
//
// Custom Speech model that adapts transcription to domain-specific terms.
// Select a customization by its OCID, for example
// `oci.ai.speech.customization(id: "ocid1.aispeechcustomization.oc1...")`.
private oci.ai.speech.customization @defaults("name state") {
  init(id? string)
  // Customization OCID
  id string
  // Customization display name
  name string
  // Compartment OCID containing the customization
  compartmentID string
  // Compartment containing the customization
  compartment() oci.compartment
  // Alias of the customization
  alias string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Last update time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Document Understanding AI
//
// Custom Document Understanding AI estate for the tenancy across every
// subscribed region. The `projects` are the workspaces that group
// document models and `models` are the custom models trained for tasks
// such as key-value extraction and document classification.
oci.ai.document {
  // Document Understanding projects
  projects() []oci.ai.document.project
  // Custom Document Understanding models
  models() []oci.ai.document.model
}

// Oracle Cloud Infrastructure (OCI) Document Understanding AI project
//
// Workspace that groups custom Document Understanding models. Select a
// project by its OCID, for example
// `oci.ai.document.project(id: "ocid1.aidocumentproject.oc1...")`.
private oci.ai.document.project @defaults("name state") {
  init(id? string)
  // Project OCID
  id string
  // Project display name
  name string
  // Compartment OCID containing the project
  compartmentID string
  // Compartment containing the project
  compartment() oci.compartment
  // Description of the project
  description string
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Document Understanding AI model
//
// Single custom Document Understanding model. Select a model by its OCID,
// for example
// `oci.ai.document.model(id: "ocid1.aidocumentmodel.oc1...")`.
private oci.ai.document.model @defaults("name modelType state") {
  init(id? string)
  // Model OCID
  id string
  // Model display name
  name string
  // Compartment OCID containing the model
  compartmentID string
  // Compartment containing the model
  compartment() oci.compartment
  // OCID of the project the model belongs to
  projectID string
  // Project the model belongs to
  project() oci.ai.document.project
  // Model type (e.g., KEY_VALUE_EXTRACTION, DOCUMENT_CLASSIFICATION)
  modelType string
  // Model version
  modelVersion string
  // Description of the model
  description string
  // Number of inference units provisioned for the model
  inferenceUnits int
  // Model precision (accuracy) achieved during training
  precision float
  // Whether the model is composed of multiple component models
  isComposedModel bool
  // Lifecycle state (e.g., CREATING, ACTIVE, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Zero Trust Packet Routing
//
// Network authorization written against the security attributes a resource
// carries rather than against its address. ZPR policies sit above security
// lists and network security groups: where a namespace enforces, traffic those
// layers admit can still be denied, so a network posture read without them is
// incomplete for any tenancy that has onboarded ZPR. Exposes the tenancy's
// onboarding status and the policies written against it.
oci.zpr {
  // Zero Trust Packet Routing policies in the tenancy
  policies() []oci.zpr.policy
  // Tenancy-wide Zero Trust Packet Routing onboarding status
  configuration() oci.zpr.configuration
}

// Zero Trust Packet Routing policy
//
// A named set of statements written in the Zero Trust Packet Routing Policy
// Language, deciding which traffic between security-attributed resources is
// permitted. Statements are returned as authored, so an audit reads the same
// text an operator wrote. A policy only governs resources carrying the
// security attributes its statements name.
private oci.zpr.policy @defaults("name state") {
  // Policy OCID
  id string
  // Name of the policy, unique across the tenancy
  name string
  // Policy description
  description string
  // Compartment containing the policy
  compartment() oci.compartment
  // Zero Trust Packet Routing Policy Language statements
  //
  // Up to 25 statements per policy, returned as written.
  statements []string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Creation time
  created time
  // Time the policy was last updated
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace.
  systemTags map[string]dict
}

// Zero Trust Packet Routing tenancy configuration
//
// Whether Zero Trust Packet Routing has been onboarded to the tenancy. ZPR
// policies have no effect until this reports an enabled status, so this is the
// gate to check before reading any conclusion from the policies themselves.
private oci.zpr.configuration @defaults("zprStatus state") {
  // Configuration OCID
  id string
  // Compartment the configuration belongs to
  compartment() oci.compartment
  // Whether Zero Trust Packet Routing is enabled for the tenancy
  //
  // One of ENABLED or DISABLED. Policies do not take effect while disabled.
  zprStatus string
  // Lifecycle state (e.g., CREATING, ACTIVE, UPDATING, FAILED)
  state string
  // Detail message describing the current lifecycle state
  lifecycleDetails string
  // Time Zero Trust Packet Routing was onboarded to the tenancy
  created time
  // Time the configuration was last updated
  timeUpdated time
}

// Security attributes
//
// The vocabulary Zero Trust Packet Routing policies are written against.
// Namespaces group attribute definitions and carry the mode that decides
// whether their attributes are enforced or only audited; the attributes
// themselves are the keys resources are labelled with.
oci.securityAttributes {
  // Security attribute namespaces in the tenancy
  namespaces() []oci.securityAttributes.namespace
  // Security attribute definitions across all namespaces
  attributes() []oci.securityAttributes.attribute
}

// Security attribute applied to a resource
//
// One namespace/name/value labelling on a single resource, resolved against
// the namespace and attribute definition that give it meaning. `isEnforcing`
// reports whether the labelling constrains traffic or only records it, so a
// query can separate resources Zero Trust Packet Routing governs from those
// merely tagged. For example
// `oci.compute.instances.where(appliedSecurityAttributes.any(isEnforcing))`.
private oci.securityAttributes.applied @defaults("namespaceName name value isEnforcing") {
  // Name of the namespace the attribute belongs to
  namespaceName string
  // Name of the attribute
  name string
  // Value assigned on this resource
  //
  // Rendered as text regardless of the attribute's declared type, so a numeric
  // attribute compares against the string form of its value.
  value string
  // Whether the attribute's namespace enforces rather than audits
  //
  // True when the namespace's mode includes `enforce`. This describes the
  // namespace only: whether Zero Trust Packet Routing is switched on for the
  // tenancy is a separate fact, reported by `oci.zpr.configuration.zprStatus`
  // and combined with this one in `oci.network.exposure.zprEnforced`.
  isEnforcing bool
  // Namespace defining the attribute
  namespace() oci.securityAttributes.namespace
  // Definition of the attribute, including its validator
  attribute() oci.securityAttributes.attribute
}

// Security attribute namespace
//
// A named group of security attribute definitions, and the unit at which
// enforcement is decided. The `mode` field is the load-bearing one: a
// namespace in `enforce` mode has its attributes acted on by Zero Trust Packet
// Routing, while one in `audit` mode reports what it would have done and
// admits the traffic regardless.
private oci.securityAttributes.namespace @defaults("name mode isRetired") {
  // Namespace OCID
  id string
  // Name of the namespace, unique across the tenancy
  name string
  // Namespace description
  description string
  // Compartment containing the namespace
  compartment() oci.compartment
  // Modes the namespace's attributes can be set to
  //
  // Contains `enforce`, `audit`, or both. Attributes from a namespace that
  // does not include `enforce` never block traffic.
  mode []string
  // Whether the namespace is retired
  //
  // A retired namespace cannot be applied to new resources, though resources
  // already carrying its attributes keep them.
  isRetired bool
  // Lifecycle state (e.g., ACTIVE, INACTIVE, DELETING, DELETED)
  state string
  // Creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace.
  systemTags map[string]dict
}

// Security attribute definition
//
// A single key within a security attribute namespace, and one of the terms a
// Zero Trust Packet Routing statement can name. The validator records whether
// the attribute accepts free-form values or is restricted to an enumerated
// set, which is what stops a policy from being satisfied by an unexpected
// value.
private oci.securityAttributes.attribute @defaults("name isRetired") {
  // Attribute OCID
  id string
  // Name of the attribute, unique within its namespace
  name string
  // Attribute description
  description string
  // Namespace containing the attribute
  namespace() oci.securityAttributes.namespace
  // Compartment containing the attribute definition
  compartment() oci.compartment
  // Data type of the attribute
  type string
  // Whether the attribute is retired
  //
  // A retired attribute cannot be applied to new resources, though resources
  // already carrying it keep it.
  isRetired() bool
  // Value validator applied to the attribute
  //
  // Shape varies by validator type: {validatorType: "DEFAULT"} accepts any
  // value, while {validatorType: "ENUM", values: [...]} restricts the
  // attribute to the listed values. An attribute a Zero Trust Packet Routing
  // statement depends on is only as reliable as the values it will accept.
  validator() dict
  // Lifecycle state (e.g., ACTIVE, INACTIVE, DELETING, DELETED)
  state() string
  // Creation time
  created() time
}

// Oracle Cloud Infrastructure (OCI) Artifact Registry
//
// Container images and generic artifacts stored in the tenancy's registry.
// `containerRepositories` reports which repositories are readable without
// credentials and which reject overwriting a published tag,
// `containerImages` what is stored in them, and `containerImageSignatures`
// which of those images were signed and with which key. Together they answer
// whether an OKE cluster enforcing an image policy is pulling from
// repositories that can satisfy it. `repositories` and their artifacts cover
// the generic, non-container side of the same registry.
oci.artifacts {
  // Container repositories in the registry
  containerRepositories() []oci.artifacts.containerRepository
  // Container images stored in the registry
  containerImages() []oci.artifacts.containerImage
  // Signatures applied to container images
  containerImageSignatures() []oci.artifacts.containerImageSignature
  // Generic artifact repositories
  repositories() []oci.artifacts.repository
  // Registry namespace for the tenancy
  //
  // The first segment of every image path, so it is what a pull reference in
  // a deployment has to match.
  registryNamespace() string
  // Whether pushing to an unknown repository creates it
  //
  // With this on, any principal that can push can bring a new repository into
  // existence, so the set of repositories is not something an administrator
  // has approved in advance.
  isRepositoryCreatedOnFirstPush() bool
}

// Oracle Cloud Infrastructure (OCI) container repository
//
// Repository holding container images, and the place two registry-side
// controls are set. `isPublic` reports whether the repository can be pulled
// anonymously from the internet, and `isImmutable` whether a published tag
// can be moved to point at different content. A mutable tag means an image
// verified once is not necessarily the image that runs next.
private oci.artifacts.containerRepository @defaults("name isPublic imageCount") {
  // Container repository OCID
  id string
  // Repository name, which is the image path below the registry namespace
  name string
  // Compartment containing the repository
  compartment() oci.compartment
  // Registry namespace the repository sits under
  namespace string
  // Whether the repository can be pulled without credentials
  isPublic bool
  // Whether a published tag is prevented from being moved to other content
  isImmutable() bool
  // Images stored in the repository
  images() []oci.artifacts.containerImage
  // Number of images in the repository
  imageCount int
  // Number of layers across the repository
  layerCount int
  // Bytes consumed by the repository's layers
  layersSizeInBytes int
  // Billable size of the repository in gigabytes
  billableSizeInGBs int
  // Repository lifecycle state (AVAILABLE, DELETING, or DELETED)
  state string
  // Principal that created the repository
  createdBy() string
  // Time an image was last pushed to the repository
  timeLastPushed() time
  // Repository creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) container image
//
// One image manifest in a container repository, identified by its `digest`.
// `signatures` is what an image policy checks, so an image with none cannot
// satisfy a cluster that enforces one. `versions` lists the tags currently
// resolving to this digest, which is how a tag in a deployment is traced back
// to the content it actually pulls.
private oci.artifacts.containerImage @defaults("name digest state") {
  // Container image OCID
  id string
  // Image path and tag, as it appears in a pull reference
  name string
  // Content digest of the image manifest
  digest string
  // Tag this listing entry refers to
  version string
  // Repository the image is stored in
  repository() oci.artifacts.containerRepository
  // Name of the repository the image is stored in
  repositoryName string
  // Compartment containing the image
  compartment() oci.compartment
  // Signatures applied to the image
  //
  // An image with no signatures cannot satisfy a cluster whose image policy
  // requires one, whatever keys that policy names.
  signatures() []oci.artifacts.containerImageSignature
  // Tags currently resolving to this digest
  //
  // Each entry carries `version` with the tag, `createdBy` with the principal
  // that pushed it, and `timeCreated`.
  versions() []dict
  // Principal that pushed the image
  createdBy() string
  // Number of times the image has been pulled
  pullCount() int
  // Time the image was last pulled
  timeLastPulled() time
  // Bytes consumed by the image's layers
  layersSizeInBytes() int
  // Image lifecycle state (AVAILABLE, DELETING, or DELETED)
  state string
  // Image creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) container image signature
//
// Signature over a container image's digest, and the evidence an image policy
// evaluates. `kmsKey` is the key the signature was produced with, which is
// what a cluster's image policy names when it decides whose signatures it
// trusts, and `signingAlgorithm` is how it was produced. A signature made
// with a key the policy does not name satisfies nothing.
private oci.artifacts.containerImageSignature @defaults("name signingAlgorithm") {
  // Signature OCID
  id string
  // Signature display name
  name string
  // Compartment containing the signature
  compartment() oci.compartment
  // Image the signature covers
  image() oci.artifacts.containerImage
  // Vault key the signature was produced with
  kmsKey() oci.kms.key
  // Version of the vault key used
  //
  // Reported as an identifier because a key version is reachable only through
  // the key that owns it, which `kmsKey` resolves.
  kmsKeyVersionId string
  // How the signature was produced
  //
  // One of SHA_224_RSA_PKCS_PSS, SHA_256_RSA_PKCS_PSS,
  // SHA_384_RSA_PKCS_PSS, or SHA_512_RSA_PKCS_PSS.
  signingAlgorithm string
  // Base64-encoded payload that was signed
  message string
  // Base64-encoded signature over the message
  signature string
  // Signature lifecycle state (e.g., AVAILABLE, DELETING, DELETED)
  state string
  // Signature creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) artifact repository
//
// Repository holding generic, non-container artifacts. `isImmutable` reports
// whether an artifact already published at a path and version can be
// replaced, which is the same tamper question a container repository answers
// for tags.
private oci.artifacts.repository @defaults("name repositoryType isImmutable") {
  // Repository OCID
  id string
  // Repository display name
  name string
  // Repository description
  description string
  // Compartment containing the repository
  compartment() oci.compartment
  // Kind of repository
  //
  // GENERIC is the only kind the service offers today.
  repositoryType string
  // Whether an artifact already published cannot be replaced
  isImmutable bool
  // Artifacts stored in the repository
  artifacts() []oci.artifacts.genericArtifact
  // Repository lifecycle state (e.g., AVAILABLE, DELETING, DELETED)
  state string
  // Repository creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) generic artifact
//
// One file stored in an artifact repository, identified by its path and
// version. `sha256` is the content digest a consumer can verify a download
// against, and it is the only integrity evidence a generic artifact carries,
// since these are not signed the way container images are.
private oci.artifacts.genericArtifact @defaults("name version sizeInBytes") {
  // Artifact OCID
  id string
  // Artifact display name, which is the path and version
  name string
  // Compartment containing the artifact
  compartment() oci.compartment
  // Repository the artifact is stored in
  repository() oci.artifacts.repository
  // Path the artifact is published at
  artifactPath string
  // Version the artifact is published under
  version string
  // SHA-256 digest of the artifact's content
  sha256 string
  // Size of the artifact in bytes
  sizeInBytes int
  // Artifact lifecycle state (e.g., AVAILABLE, DELETING, DELETED)
  state string
  // Artifact creation time
  created time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) Operator Access Control
//
// Gate on Oracle's own operators reaching customer infrastructure. Exadata
// Cloud@Customer, Autonomous Database and the other managed platforms are
// serviced by Oracle staff, and an operator control decides whether that
// access needs an approval, how many approvers, and for how long. `controls`
// holds the rules, `controlAssignments` records which resources each rule is
// actually attached to, and `accessRequests` is the history of who asked for
// what and whether it was granted. The history is the evidence: a control
// that has never denied anything and a control nobody has ever tested look
// the same until the requests are read.
oci.operatorAccessControl {
  // Operator controls defined in the tenancy
  controls() []oci.operatorAccessControl.control
  // Assignments attaching a control to a resource
  controlAssignments() []oci.operatorAccessControl.controlAssignment
  // Access requests raised against controlled resources
  accessRequests() []oci.operatorAccessControl.accessRequest
  // Operator actions that can be requested
  actions() []oci.operatorAccessControl.action
}

// Oracle Cloud Infrastructure (OCI) operator control
//
// Rule governing Oracle operator access to a class of resource.
// `isFullyPreApproved` is the setting that matters most: with it set, every
// operator action proceeds without anyone approving it, so the control exists
// while enforcing nothing. `numberOfApprovers` is how many people must agree
// when it is not pre-approved.
private oci.operatorAccessControl.control @defaults("name isFullyPreApproved numberOfApprovers") {
  // Operator control OCID
  id string
  // Operator control name
  name string
  // Compartment containing the operator control
  compartment() oci.compartment
  // Whether every request under this control is approved without review
  isFullyPreApproved bool
  // Approvers required before an operator action proceeds
  numberOfApprovers int
  // Kind of resource the control governs (e.g., EXACC, EXADATAINFRASTRUCTURE, AUTONOMOUSVMCLUSTER, CCCINFRASTRUCTURE)
  resourceType string
  // Operator control lifecycle state (e.g., CREATED, ASSIGNED, UNASSIGNED, DELETED)
  state string
  // Operator control creation time
  created time
  // Last modification time
  timeOfModification time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) operator control assignment
//
// Attachment of an operator control to one resource, and the only thing that
// puts a control into effect. An unassigned control governs nothing, so this
// is where coverage is proved. `isEnforcedAlways` separates a permanent
// assignment from one bounded by `timeAssignmentFrom` and `timeAssignmentTo`,
// and `isLogForwarded` with `remoteSyslogServerAddress` reports whether
// operator activity is streamed off the resource for independent review.
private oci.operatorAccessControl.controlAssignment @defaults("resourceName operatorControlName state") {
  // Assignment OCID
  id string
  // Operator control this assignment applies
  operatorControl() oci.operatorAccessControl.control
  // Name of the operator control this assignment applies
  operatorControlName string
  // Compartment containing the assignment
  compartment() oci.compartment
  // OCID of the resource the control is attached to
  //
  // Which kind of resource this identifies is given by `resourceType`, and
  // the platforms it covers are managed database and Cloud@Customer
  // infrastructure rather than resources this provider models.
  resourceId string
  // Name of the resource the control is attached to
  resourceName string
  // Kind of resource the control is attached to (e.g., EXACC, EXADATAINFRASTRUCTURE, AUTONOMOUSVMCLUSTER, CCCINFRASTRUCTURE)
  resourceType string
  // Whether the control applies at all times rather than for a bounded window
  isEnforcedAlways bool
  // Start of the assignment window
  timeAssignmentFrom time
  // End of the assignment window
  timeAssignmentTo time
  // Whether operator activity is forwarded to a remote syslog server
  isLogForwarded bool
  // Address of the syslog server operator activity is forwarded to
  remoteSyslogServerAddress string
  // Port of the syslog server operator activity is forwarded to
  remoteSyslogServerPort int
  // Whether hypervisor logs are forwarded as well
  isHypervisorLogForwarded bool
  // Assignment lifecycle state (e.g., CREATED, APPLIED, APPLYFAILED, DELETED)
  state string
  // Detail behind the current lifecycle state
  lifecycleDetails string
  // Time the control was assigned
  timeOfAssignment time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) operator access request
//
// One request by an Oracle operator to reach a controlled resource, and the
// record of what happened to it. `isAutoApproved` marks a request nobody
// reviewed, `severity` and `duration` state how urgent and how long, and
// `actionRequests` names the specific operator actions asked for. Reading
// these is how an assertion about operator access moves from what the control
// permits to what actually took place.
private oci.operatorAccessControl.accessRequest @defaults("resourceName severity state") {
  // Access request OCID
  id string
  // Human-readable request identifier
  requestId string
  // Compartment containing the access request
  compartment() oci.compartment
  // Reason the operator gave for the request
  accessReasonSummary string
  // OCID of the resource access was requested to
  //
  // Which kind of resource this identifies is given by `resourceType`.
  resourceId string
  // Name of the resource access was requested to
  resourceName string
  // Kind of resource access was requested to (e.g., EXACC, EXADATAINFRASTRUCTURE, AUTONOMOUSVMCLUSTER, CCCINFRASTRUCTURE)
  resourceType string
  // Sub-resources named in the request
  subResources []string
  // Operator actions the request asks to perform
  actionRequests []string
  // Whether the request was approved without human review
  isAutoApproved bool
  // Urgency the operator claimed (e.g., S1, S2, S3, S4)
  severity string
  // Hours of access requested
  duration int
  // Additional hours requested as an extension
  extendDuration int
  // Access request lifecycle state (e.g., CREATED, APPROVED, APPROVEDFORFUTURE, REJECTED, DEPLOYED, CLOSED, EXPIRED, REVOKED)
  state string
  // Detail behind the current lifecycle state
  lifecycleDetails string
  // Time access was requested for, when scheduled ahead
  timeRequestedForFutureAccess time
  // Access request creation time
  created time
  // Last modification time
  timeOfModification time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
}

// Oracle Cloud Infrastructure (OCI) operator action
//
// One operation an Oracle operator can ask to perform, as published by the
// service. The catalog is what an operator control is written against, so it
// is the denominator for any question about which operations a control
// actually restricts. `component` names the part of the platform the action
// touches.
private oci.operatorAccessControl.action @defaults("name component resourceType") {
  // Operator action OCID
  id string
  // Operator action name
  name string
  // Operator action description
  description string
  // Part of the platform the action operates on
  component string
  // Compartment the action was listed from
  compartment() oci.compartment
  // Kind of resource the action applies to (e.g., EXACC, EXADATAINFRASTRUCTURE, AUTONOMOUSVMCLUSTER, CCCINFRASTRUCTURE)
  resourceType string
  // Operator action lifecycle state (e.g., ACTIVE, INACTIVE)
  state string
}

// Oracle Cloud Infrastructure (OCI) Delegate Access Control
//
// Gate on a managed service provider reaching customer infrastructure. Where
// operator access control covers Oracle's own staff, this covers third
// parties the tenancy has subscribed to: `serviceProviders` is who they are,
// `delegationSubscriptions` records which of them the tenancy has accepted,
// `delegationControls` is the rule attached to the resources they may reach,
// and `accessRequests` is what they asked for and what happened. A
// subscription with no matching delegation control is a provider admitted
// without a rule governing it.
oci.delegateAccessControl {
  // Delegation controls defined in the tenancy
  delegationControls() []oci.delegateAccessControl.delegationControl
  // Subscriptions to service providers
  delegationSubscriptions() []oci.delegateAccessControl.delegationSubscription
  // Access requests raised by service providers
  accessRequests() []oci.delegateAccessControl.accessRequest
  // Service providers offered to the tenancy
  serviceProviders() []oci.delegateAccessControl.serviceProvider
}

// Oracle Cloud Infrastructure (OCI) delegation control
//
// Rule governing which resources a subscribed service provider may reach and
// what it may do there. `resourceType` fixes the class of resource it covers,
// and the control has to exist and be active for delegated access to be
// governed at all.
private oci.delegateAccessControl.delegationControl @defaults("name resourceType state") {
  // Delegation control OCID
  id string
  // Delegation control display name
  name string
  // Compartment containing the delegation control
  compartment() oci.compartment
  // Kind of resource the control governs (e.g., VMCLUSTER, CLOUDVMCLUSTER)
  resourceType string
  // Delegation control lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED, NEEDS_ATTENTION)
  state string
  // Detail behind the current lifecycle state
  lifecycleStateDetails string
  // Delegation control creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) delegation subscription
//
// The tenancy's acceptance of one service provider for one class of service.
// A subscription is what admits an outside party at all, so the set of active
// subscriptions is the list of organizations that can be granted access to
// this tenancy's managed resources.
private oci.delegateAccessControl.delegationSubscription @defaults("name subscribedServiceType state") {
  // Delegation subscription OCID
  id string
  // Delegation subscription display name
  name string
  // Compartment containing the subscription
  compartment() oci.compartment
  // Service provider the tenancy is subscribed to
  serviceProvider() oci.delegateAccessControl.serviceProvider
  // Class of service the subscription covers (e.g., TROUBLESHOOTING, ASSISTED_PATCHING)
  subscribedServiceType string
  // Subscription lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED, NEEDS_ATTENTION)
  state string
  // Detail behind the current lifecycle state
  lifecycleStateDetails string
  // Subscription creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) delegated resource access request
//
// One request by a subscribed service provider to reach a customer resource.
// `isAutoApproved` marks a request nobody reviewed, `requesterType` separates
// a request raised by the provider from one raised on the customer's behalf,
// and `ticketNumbers` links it to the support case it claims to serve.
private oci.delegateAccessControl.accessRequest @defaults("name resourceName state") {
  // Access request OCID
  id string
  // Access request display name
  name string
  // Compartment containing the access request
  compartment() oci.compartment
  // Delegation control governing the request
  delegationControl() oci.delegateAccessControl.delegationControl
  // Reason the provider gave for the request
  reasonForRequest string
  // OCID of the resource access was requested to
  //
  // Which kind of resource this identifies is given by `resourceType`.
  resourceId string
  // Name of the resource access was requested to
  resourceName string
  // Kind of resource access was requested to (e.g., VMCLUSTER, CLOUDVMCLUSTER)
  resourceType string
  // Support tickets the request is associated with
  ticketNumbers []string
  // Actions the request asks to perform
  requestedActionNames []string
  // Who raised the request (e.g., CUSTOMER, DELEGATEE)
  requesterType string
  // Urgency claimed for the request (e.g., S1, S2, S3, S4)
  severity string
  // Hours of access requested
  durationInHours int
  // Additional hours requested as an extension
  extendDurationInHours int
  // Whether the request was approved without human review
  isAutoApproved bool
  // Where the request stands (e.g., CREATED, APPROVAL_WAITING, APPROVED, REJECTED, CLOSED, EXPIRED, REVOKED)
  requestStatus string
  // Access request lifecycle state (e.g., ACCEPTED, IN_PROGRESS, WAITING, FAILED, SUCCEEDED, CANCELING, CANCELED)
  state string
  // Detail behind the current lifecycle state
  lifecycleStateDetails string
  // Time access was requested
  timeAccessRequested time
  // Access request creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) delegate access service provider
//
// An organization the tenancy can subscribe to for delegated access.
// `serviceTypes` is what it may be engaged for and `supportedResourceTypes`
// what it may be engaged on, so together they bound what a subscription to it
// can ever permit.
private oci.delegateAccessControl.serviceProvider @defaults("name serviceProviderType state") {
  // Service provider OCID
  id string
  // Service provider name
  name string
  // Compartment the service provider was listed from
  compartment() oci.compartment
  // Kind of provider (e.g., ORACLE_PROVIDED, THIRD_PARTY)
  serviceProviderType string
  // Classes of service the provider offers (e.g., TROUBLESHOOTING, ASSISTED_PATCHING)
  serviceTypes []string
  // Kinds of resource the provider can be engaged on (e.g., VMCLUSTER, CLOUDVMCLUSTER)
  supportedResourceTypes []string
  // Service provider lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED, NEEDS_ATTENTION)
  state string
  // Detail behind the current lifecycle state
  lifecycleStateDetails string
  // Service provider creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Privileged API Control
//
// Approval gate on individual API operations. Where the other controls in
// this family govern who may reach a resource, this governs which operations
// may be called on it at all without someone signing off first.
// `privilegedApiControls` holds the rules and `privilegedApiRequests` the
// operations that were actually asked for, each naming the API and the
// attributes it would touch.
oci.apiAccessControl {
  // Privileged API controls defined in the tenancy
  privilegedApiControls() []oci.apiAccessControl.privilegedApiControl
  // Requests to invoke a privileged API operation
  privilegedApiRequests() []oci.apiAccessControl.privilegedApiRequest
}

// Oracle Cloud Infrastructure (OCI) privileged API control
//
// Rule putting a set of API operations behind approval. `numberOfApprovers`
// is the strength of the gate: a control requiring none is a record that the
// operations are watched rather than a constraint on calling them.
private oci.apiAccessControl.privilegedApiControl @defaults("name resourceType numberOfApprovers") {
  // Privileged API control OCID
  id string
  // Privileged API control display name
  name string
  // Compartment containing the control
  compartment() oci.compartment
  // Kind of resource the control governs
  resourceType string
  // Approvers required before a privileged operation proceeds
  numberOfApprovers int
  // Control lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail behind the current lifecycle state
  lifecycleDetails string
  // Control creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) privileged API request
//
// One request to invoke API operations that a privileged API control has put
// behind approval. `privilegedOperations` names each API and the attributes
// it would read or change, which is the closest thing OCI offers to a record
// of what a privileged caller intended to touch.
private oci.apiAccessControl.privilegedApiRequest @defaults("name resourceName state") {
  // Privileged API request OCID
  id string
  // Privileged API request display name
  name string
  // Human-readable request identifier
  requestId string
  // Compartment containing the request
  compartment() oci.compartment
  // Reason the caller gave for the request
  reasonSummary string
  // OCID of the resource the operations would be called on
  //
  // Which kind of resource this identifies is given by `resourceType`.
  resourceId string
  // Name of the resource the operations would be called on
  resourceName string
  // Kind of resource the operations would be called on
  resourceType string
  // Sub-resources named in the request
  subResourceNames []string
  // API operations the request asks to invoke
  //
  // Each entry carries `apiName` and, where the operation is attribute
  // scoped, `attributeNames` listing what it would touch.
  privilegedOperations []dict
  // Where the request stands (e.g., CREATED, APPROVAL_WAITING, APPROVED, REJECTED, CLOSED, EXPIRED, REVOKED)
  requestState string
  // Request lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail behind the current lifecycle state
  lifecycleDetails string
  // Urgency claimed for the request (e.g., S1, S2, S3, S4)
  severity string
  // Hours of access requested
  durationInHours int
  // Time access was requested for, when scheduled ahead
  timeRequestedForFutureAccess time
  // Request creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) Managed Access
//
// Approval workflow for Oracle access to a resource, known as Lockbox. A
// lockbox wraps one resource and points at the approval template deciding who
// must sign off before access is granted. `lockboxes` is what is wrapped and
// `approvalTemplates` the workflows, and each lockbox carries its own
// `accessRequests` history. A lockbox whose template auto-approves is a
// workflow that records access rather than gating it.
oci.lockbox {
  // Lockboxes protecting resources in the tenancy
  lockboxes() []oci.lockbox.lockbox
  // Approval templates lockboxes are governed by
  approvalTemplates() []oci.lockbox.approvalTemplate
}

// Oracle Cloud Infrastructure (OCI) lockbox
//
// Approval gate wrapping one resource. `maxAccessDuration` bounds how long a
// granted access lasts, and `approvalTemplate` is who has to agree before it
// is granted at all. `lockboxPartner` names which Oracle organization the
// lockbox admits.
private oci.lockbox.lockbox @defaults("name lockboxPartner state") {
  // Lockbox OCID
  id string
  // Lockbox display name
  name string
  // Compartment containing the lockbox
  compartment() oci.compartment
  // OCID of the resource the lockbox protects
  //
  // The lockbox wraps whichever service resource it was created for, which
  // may be of a kind this provider does not model.
  resourceId string
  // Approval template deciding who must sign off on access
  approvalTemplate() oci.lockbox.approvalTemplate
  // Oracle organization the lockbox admits (e.g., FAAAS, FUSIONAAAS)
  lockboxPartner string
  // OCID identifying the partner
  partnerId string
  // Compartment OCID on the partner side
  //
  // A compartment in the partner's tenancy rather than this one, so it is
  // reported as an identifier rather than resolved.
  partnerCompartmentId string
  // Longest access a single request can be granted
  maxAccessDuration string
  // Access requests raised against this lockbox
  accessRequests() []oci.lockbox.accessRequest
  // Lockbox lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Detail behind the current lifecycle state
  lifecycleDetails string
  // Lockbox creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) lockbox approval template
//
// Workflow deciding who signs off on a lockbox access request.
// `autoApprovalState` is the setting that decides whether the workflow gates
// anything: with it enabled, requests are granted without anyone reviewing
// them. `approverLevels` is the chain of approvers when it is not, and the
// number of levels is the two-person rule made explicit.
private oci.lockbox.approvalTemplate @defaults("name autoApprovalState state") {
  // Approval template OCID
  id string
  // Approval template display name
  name string
  // Compartment containing the approval template
  compartment() oci.compartment
  // Whether requests are granted without review (e.g., ENABLED, DISABLED)
  autoApprovalState string
  // Chain of approvers a request passes through
  //
  // Each entry carries `level` as its position in the chain, `approverType`
  // as the kind of principal, `approverId` naming it, and `domainId` for the
  // identity domain it belongs to. More than one level means no single
  // approver can grant access alone.
  approverLevels []dict
  // Approval template lifecycle state (e.g., CREATING, ACTIVE, UPDATING, DELETING, DELETED, FAILED)
  state string
  // Approval template creation time
  created time
  // Last modification time
  timeUpdated time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}

// Oracle Cloud Infrastructure (OCI) lockbox access request
//
// One request to reach the resource a lockbox protects, and what happened to
// it. `requestorLocation` and `ticketNumber` are the claimed origin and the
// support case behind it, `accessDuration` how long was asked for, and
// `timeExpired` when the granted access lapses. These are the evidence that a
// lockbox is exercised rather than merely configured.
private oci.lockbox.accessRequest @defaults("name state accessDuration") {
  // Access request OCID
  id string
  // Access request display name
  name string
  // Reason given for the request
  description string
  // Lockbox the request was raised against
  lockbox() oci.lockbox.lockbox
  // Identifier of the principal that raised the request
  //
  // An Oracle-side identity rather than a user in this tenancy.
  requestorId string
  // Location the requestor claimed to be operating from
  requestorLocation string
  // Access duration asked for
  accessDuration string
  // Support ticket the request is associated with
  ticketNumber string
  // Access request lifecycle state (e.g., CREATING, IN_PROGRESS, WAITING, SUCCEEDED, CANCELING, CANCELED, FAILED, EXPIRED)
  state string
  // Access request creation time
  created time
  // Last modification time
  timeUpdated time
  // Time the granted access lapses
  timeExpired time
  // Free-form tags for resource management
  freeformTags map[string]string
  // Defined tags for governance and cost tracking
  definedTags map[string]map[string]string
  // System tags
  //
  // Tags applied automatically by Oracle, keyed by namespace. The
  // `Oracle-Tags` namespace records the creating principal in `CreatedBy`
  // and the creation time in `CreatedOn`.
  systemTags map[string]dict
}
