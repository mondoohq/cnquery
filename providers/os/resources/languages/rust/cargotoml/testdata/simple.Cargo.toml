[package]
name = "myproject"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
tokio = { version = "1.32", features = ["full"] }
log = { version = "0.4", optional = true }

[dev-dependencies]
criterion = "0.5"
mockall = { version = "0.11" }

[build-dependencies]
cc = "1.0"
