[commands."uname -s"]
stdout = "DragonFly"

[commands."uname -m"]
stdout = "x86_64"

[commands."uname -r"]
stdout = "6.4-RELEASE"

[commands."hostname"]
exit_status = 1

[commands."sysctl -n kern.hostname"]
stdout = "dragonfly-vagrant.local"
