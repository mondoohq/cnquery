[commands."uname -s"]
stdout = "OpenBSD"

[commands."uname -m"]
stdout = "amd64"

[commands."uname -r"]
stdout = "7.4"

[commands."hostname"]
exit_status = 1

[commands."sysctl -n kern.hostname"]
stdout = "openbsd-vagrant.local"
